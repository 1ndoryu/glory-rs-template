# Tareas completadas — 2026-08-05

## Qué

- **297A-73 — Panel de versiones de Assets 3D (frontend):** servicio admin
  (`listVersions`/`import`/`updateMetadata`/`activate` + lectura binaria del GLB
  vía fetch directo), preview 3D aislado con GLTFLoader, panel de versiones en
  `game-settings` y endpoint backend para servir el GLB a admin. Gate PASS,
  type-check, 142 tests frontend y build PASS. (El código se commiteó en
  `91b5f13f` como parcial; el gate y el registro se cerraron hoy.)
- **297A-74 — Cierre Fase 4 (culling avanzado, batching por materiales y
  medición física de GPU/memoria):** `maxDistance` circular en
  `MapChunkCache.select`; `groupMeshesByMaterial` fusiona meshes del prototipo
  con misma geometría+material en un solo `InstancedMesh`; `game-gpu-probe.ts`
  lee identidad GPU, tiempo de frame con `EXT_disjoint_timer_query` y bytes
  estimados; la escena publica `data-gpu-*`/`data-batch-*`. Gate PASS,
  type-check, 196 tests frontend del juego y build PASS.
- **297A-75 — Fase 8 (dos salas concurrentes, métricas agregadas y runbook):**
  `GameRoomState` multi-sala por `map.map_version()`; `GameRoomMetrics` con
  contadores atómicos y `GET /api/game/metrics` público agregado; runbook de
  rollback de mapa/assets. Gate PASS (local-light; full diferido por cooldown),
  9/9 unit de `game_room`, 8/8 TCP, 51 tests backend de regresión y 665 tests
  frontend PASS.
- **Validación visual del fixture GAME-01 (cierre):** backend actualizado
  compilado y ejecutado en localhost:3000 con la BD de rama (todas las rutas
  del juego: `characters`, `metrics` → 200); Vite del proyecto en :5174.
  En navegador: `/forest-playable` monta el canvas del fixture con WebGL2,
  GPU detectada (`Intel Iris Xe` vía ANGLE/D3D11), datasets
  `character-id=forest-scout`, `character-tone=ink`, `player-name=Jugador`,
  `webgl-kind=webgl2`; movimiento funcional (pad ↑←↓→) sin errores de
  runtime; el 401 de `/api/game/profile` y el 404 de `/api/game/maps/bosque`
  son esperados sin sesión/mapa activo.

## Archivos

- Frontend: `map-streaming-contracts.ts`, `map-streaming.ts` (+test),
  `game-playable-visual-cache.ts` (+test), `game-gpu-probe.ts` (+test),
  `game-playable-scene.ts`, `game-playable.ts`.
- Backend: `src/services/game_room.rs`, `src/handlers/game_metrics_handler.rs`,
  `src/handlers/mod.rs`, `tests/game_ws_tcp.rs`.
- Docs: `roadmap.md`, `Agente/planes/plan-juego-bosque-multijugador-2026-08-01.md`,
  `Agente/documentacion/operacion/runbook-rollback-juego-2026-08-05.md`.
- Regenerado: OpenAPI + Orval (endpoint `GET /api/game/metrics`).

## Cierre documental del plan GAME-01 (checklists con evidencia)

- **Checklists del plan:** 81 casillas sin marcar → 18, todas con justificación
  (decisiones de producto tuyas, mediciones de entorno dedicado o Fase 9 futura).
  Se marcaron con evidencia las auditorías de cierre de Fases 0/1/2/3/4/5/6/7/8,
  los ítems de Fase 1 (bocetos), Fase 2 (contratos/mensajes/restauración/vertical
  slice/glory-render), Fase 3 (apertura/cierre), Fase 6 (auditoría) y el DoD
  (12 de 16 ítems con evidencia de los commits 297A-4x/5x/6x/7x).
- **Nuevo documento:** `Agente/documentacion/producto/decisiones-pendientes-bosque-2026-08-05.md`
  consolida la Fase 0 (referencia como atmósfera, gramática visual, capturas) y
  la sección 12 del plan (salas, cromática, controles móviles, invitado,
  publicación en vivo, escalado) con recomendaciones y tabla resumen.
- **ADR actualizado:** `adr-bosque-3d-assets-terreno-2d-2026-08-01.md` marca
  relieve (alturas 0–4, 297A-67) y cámara (isométrica limitada, 297A-30/70) como
  resueltos, y documenta el estado del presupuesto: medición local existente
  (297A-46/74/75) y pendiente formal en entorno dedicado + multi-viewport.
- **Índice documental** enlaza el nuevo documento de decisiones.

## Gotchas

- El gate exige que la tarea exista en roadmap/planes/completados antes de
  ejecutarse (`preflight.assertTaskExists`); una tarea commiteada como parcial
  sin registrar no pasa.
- El entorno se reinició: `frontend/node_modules` perdió vitest/three/lucide;
  reinstalar con `npm ci` y, si Cargo falla por el exe en uso
  (`glory-backend.exe` del puerto 3000), usar `CARGO_TARGET_DIR` temporal para
  `codegen-local.mjs` en vez de matar el proceso ajeno.
- `tokio::sync::RwLock::blocking_write()` dentro de un runtime async panics;
  usar `std::sync::RwLock` para accesos cortos sin `.await` mientras se
  mantiene el lock.
- El snapshot inicial del join no pasa por `broadcast_snapshot`, así que el
  contador `snapshots_sent` arranca en el primer tick (assert `>= 1`, no `>= 2`).
- `tools/sentinel` apareció como "cambiado" en un scope por suciedad transitoria
  de otro hilo (submódulo); VarSense rechaza directorios en `--files-from`.
  Re-ejecutar el gate con el submódulo limpio resuelve el falso positivo.
- La validación visual del fixture con datasets GPU/métricas queda pendiente de
  una sesión con backend actualizado (el binario del puerto 3000 no expone las
  rutas del juego) y del full CI cuando expire el cooldown.

- **297A-76 — Reclamación invitado→cuenta (limpieza de identidad temporal):**
  `GameTicketStore::revoke_guest` (elimina la entrada del store; exige firma y
  entrada vigente; fallo no toca la identidad), login/logout expiran la cookie
  `guest_game` y el handler del ticket revoca la cookie invitada cuando viaja
  con sesión autenticada (la cuenta es la autoridad). 11/11 unit de
  `game_ticket`, 6/6 HTTP de tickets y 8/8 TCP de regresión PASS; gate
  `task:check -- 297A-76` PASS.

- **297A-77 — Presencia avanzada (characterId + resync):**
  - `GameRealtimeEntity.character_id` (bounded 64, fail-closed, camelCase) en
    el modelo y el validador TS; el snapshot de cada jugador lleva su personaje.
  - Resolución del personaje al emitir el ticket (HTTP con BD) contra
    `game_profiles`; viaja server-side en `PendingTicket`/`GameTicketClaims`
    (nunca en el token firmado; `issue()`/`issue_at()` ganan el parámetro
    `Option<&str>` con validación `is_valid_id` fail-closed).
  - `GameRoomState::join_with_character` (default `forest-scout`);
    `RoomPlayer.character_id`; el handler WS usa `claims.character_id` con
    fallback al default si falta o es inválido (sin BD en el socket).
  - Frontend: `SnapshotEntity`/`PlayerState.characterId`; preservación en
    `createWorldState`/`normalizeState` (default `forest-scout`); tono
    `ink|middle|paper` → material compartido en `createFigure`; la escena
    recrea la figura si el personaje cambia; jugador local offline con su
    personaje; fix de resync (reset de `lastSnapshotSequence` + snapshots al
    recibir `joined`).
  - Gate `task:check -- 297A-77` PASS; 12/12 unit de `game_ticket`, 19 unit de
    room/realtime/ws-handler, 8/8 TCP, 54 backend HTTP de regresión y 668
    tests frontend + type-check PASS.
  - Pendiente documentado: validación visual en navegador (backend del puerto
    3000 desactualizado) y full CI tras el cooldown.

## Sentinel / GLORY

- Sentinel: 0 errores en los gates 297A-73/74/75 (warnings de
  `console-production` preexistentes en `game-playable.ts`).
- VarSense: PASS con el índice persistente; sin clases huérfanas nuevas.
- GLORY: sin cambios; los módulos agnósticos siguen en `frontend/src/features/game-core/`.

## Gobernanza del escritorio (038A-2) + soft delete de artículos (028A-12)

- **038A-2 — Guard de nodos de sistema + contenido publicado siempre visible:**
  - Backend: `SYSTEM_NODE_IDS` (`trash`, `admin`, `settings`, `profile`,
    `about`) y `first_missing_system_node` en `release_validation.rs`
    (commiteado en HEAD); `validate_release_tree` rechaza releases sin ellos.
  - Tests: `workspace_publish.rs` con fixture de summary corregido (`about`
    canónico, nodeCount=7) + test de regresión
    `publish_rejects_tree_without_system_nodes`.
  - Frontend: `isSystemNode`/`SYSTEM_NODE_IDS` compartidos en
    `overlay-mutations.ts`; `tombstoneNode`/`tombstoneSubtree` no-op con
    `console.warn` sobre nodos de sistema; comando `workspace:trash` hidden +
    doble guardia.
  - Sync envelope: `ArticleService::update` mapea `status` → `editorial`/
    `visibility` vía `update_resource_metadata` en la misma transacción
    (`published` → ready/public; `draft` → draft/private).
  - Release efectiva server-side: `find_public_content()` en `resource_repo.rs`
    (kind article/media, ready/public/active) + `materialize_content_nodes()`
    en `workspace_svc.rs` que inyecta `nota-{id}` (bajo Notas) y `media-{id}`
    (bajo Documentos/subcarpeta por tipo) en `get_active_release` y
    `get_release_by_version`, **sin mutar la release inmutable**, idempotente.
  - Frontend (B): `publishWorkspace` skipea nodos `nota-`/`media-` al hornear;
    `activateVersion` refresca `fetchWorkspaceRelease()` tras activar.
  - Backfill BD: 3 artículos legacy `status='published'` con envelope
    `draft/private` → `ready/public/active`.
  - Validación por stack (gate bloqueado por submódulo de 028A-6):
    `cargo build` EXIT 0, `cargo build --tests` EXIT 0, frontend sin errores TS.
- **028A-12 (heredado y terminado) — Soft delete de artículos:**
  - Migración `20260805000000_028a12_article_soft_delete` (`trashed` +
    `deleted_at` + índice).
  - `ArticleRepository::delete` transaccional (marca `trashed` + soft delete
    del envelope vía `soft_delete_kind_tx`), `restore`, listado de papelera,
    `update` con conexión.
  - Endpoints: `DELETE /api/admin/articles/{id}` (204), `GET
    /api/admin/articles/trashed`, `POST /api/admin/articles/{id}/restore`.
  - `tests/article_soft_delete.rs`: ciclo crear → delete → papelera → restore
    → envelope vuelve a active.

## Archivos del bloque

- Frontend: `frontend/src/features/runtime/workspace/overlay-mutations.ts`,
  `frontend/src/features/runtime/commands/workspace-node-commands.ts`,
  `frontend/src/features/runtime/workspace/workspace-store.ts`,
  `frontend/src/pages/admin-workspace.ts`.
- Backend: `src/models/resource.rs`, `src/repositories/resource_repo.rs`,
  `src/services/workspace_svc.rs`, `src/services/article.rs`,
  `src/handlers/articles.rs`, `src/models/article.rs`,
  `src/repositories/article.rs`, `tests/workspace_publish.rs`,
  `tests/article_soft_delete.rs`.
- Migraciones: `migrations/20260805000000_028a12_article_soft_delete.up.sql`
  y `.down.sql`.

## Gotchas del bloque

- El gate `task:check -- 038A-2` no pudo ejecutarse: submódulo
  `tools/sentinel` con checkout modificado sin patch declarado (hilo 028A-6,
  otro frente). `cargo check`/`test` directos bloqueados por el guard (exit
  78); `cargo build` y `cargo build --tests` NO están bloqueados y pasan.
- El `status='published'` legacy NO sincronizaba el envelope de `resources`
  hasta la Fase 4; el backfill corrigió los existentes. Artículos sin fila en
  `articles` (slug NULL) materializan `publicLocator` con slug vacío.
