# Tareas completadas — 2026-07-31

## 297A-12 — Interacción móvil y frontera de capacidades

### Qué
- Se conectó el long press móvil al mismo `CommandRegistry` y menú contextual usado por desktop.
- Se añadieron comandos compartidos `workspace:move-up` y `workspace:move-down`; el orden usa `mobileOrder` sin alterar bounds desktop ni el release público.
- Se corrigió la autorización para no inferir `admin` desde `isAuthenticated`: menús, workspace, route adapter y apertura programática consumen la capacidad confirmada por `/auth/me`.
- Se alineó `UserResponse.id/role` con `AuthService.me()`.
- Se corrigió la categoría analítica para distinguir `admin`.
- Se eliminó la carrera de supresión del click posterior al long press y se añadieron pruebas de cancelación/cleanup/click legítimo.
- Se extrajeron los comandos de reorder a `workspace-reorder-commands.ts`, eliminando el warning de tamaño de `workspace-commands.ts`.

### Archivos principales
- `frontend/src/features/mobile/mobile-gestures.ts`
- `frontend/src/features/mobile/mobile-gestures.test.ts`
- `frontend/src/features/mobile/mobile-shell.ts`
- `frontend/src/features/runtime/commands/workspace-reorder-commands.ts`
- `frontend/src/features/runtime/commands/workspace-commands.ts`
- `frontend/src/features/runtime/commands/index.ts`
- `frontend/src/features/runtime/route-app-adapter.ts`
- `frontend/src/features/runtime/commands/app-commands.ts`
- `frontend/src/features/runtime/workspace/stores.ts`
- `frontend/src/services/auth.service.ts`
- `frontend/src/main.ts`
- `src/models/user.rs`
- `Agente/planes/plan-experiencia-movil-launcher-2026-07-29.md`
- `roadmap.md`

### Evidencia
- `npx tsc --noEmit`: PASS.
- Vitest: **160/160 tests, 12 suites PASS**.
- `npm run task:check -- 297A-12 --fresh`: PASS.
  - Sentinel: 0 errores, 75 warnings heredados.
  - VarSense: 0 errores, 1 aviso informativo.
  - Rust, frontend, docs y custom: PASS.
- `npm run self-check -- -TareaId 297A-12`: PASS.
- Reporte: `.quality-reports/297A-12/latest.md`.

### Pendientes no falsamente cerrados
- Pruebas visuales/E2E reales en 320/360/390px y tablet 768px.
- Orientación, safe areas, teclado virtual, foco y zoom 200% en navegador.
- Preservación de scroll/formularios durante cambio de presentación.
- Persistencia remota/sync de overlay y apps críticas móviles.
- Captura/liberación explícita del puntero durante gestos, mejora no bloqueante.

## 297A-18 — Tema claro/oscuro del OS

### Qué
- Tokens semánticos `--sistema-*` centralizados en `variables.css`, bloque `[data-tema='oscuro']` y override scoped para superficies del OS (`.desktop-window`, `.movilApp`, `.movilLauncher`) que resuelve la legibilidad dark de las apps del OS; la navegación legacy queda deliberadamente fuera.
- Migrados 8 CSS del OS de tokens legacy (`--color-*`, `--borde`) a tokens semánticos.
- `theme-store.ts`: `ThemeMode system/claro/oscuro`, `resolveTheme`, `applyTheme` (data-tema), persistencia localStorage `wandorius:tema`, evento `theme_changed`.
- Comando global `theme:toggle` (Meta+Shift+L) y `ThemeToggleButton` compartido (Lucide 1px), integrado en barra superior (junto a la hora, 19px) y launcher móvil.
- Anti-flash: script inline en `index.html` que aplica `data-tema` antes de la primera pintura.
- Evento `ThemeEvent` tipado en el dispatcher de analítica.

### Archivos principales
- Nuevos: `frontend/src/features/runtime/theme-store.ts`, `frontend/src/features/runtime/commands/theme-commands.ts`, `frontend/src/components/ui/theme-toggle-button.ts`.
- Modificados: `frontend/index.html`, `frontend/src/main.ts`, `frontend/src/features/analytics/dispatcher.ts`, `frontend/src/features/desktop/components/desktop-menu-bar.ts`, `frontend/src/features/mobile/mobile-shell.ts`, `frontend/src/styles/variables.css`, 8 CSS de desktop y `mobile-prototype.css`.

### Gotchas
- El contenido legacy (`.desktop-content-window`) vive FUERA de `.desktop-windows-container`, por lo que el override scoped lo excluye deliberadamente.
- `store.ts`/`api/client.ts` (cambios de auth) quedaron excluidos del commit; los archivos mixtos (main.ts, mobile-shell.ts, etc.) se commitearon completos por indicación del usuario.

### Sentinel
- No aplica (0 errores; 75 warnings heredados).

### Evidencia
- `npm run task:check -- 297A-18`: PASS (VarSense 0 errores/1 aviso; frontend type-check PASS).
- Navegador: `data-tema="oscuro"`, ventana Perfil legible (blanco sobre negro), botón 19px junto a la hora (hora al extremo derecho).
- Commit `3ec2cba5` pusheado a `wandorius`.

### Pendientes no falsamente cerrados
- Sync remoto de preferencia (overlay), logout/login y conflictos → 297A-13.
- Matriz de validación completa (AA, reduced motion, zoom 200%, 1440x900…320px) y capturas para aprobación formal.

### GLORY
- No aplica (patrón reutilizable candidato a extraer si un segundo proyecto necesita tema; hoy es específico del OS).
