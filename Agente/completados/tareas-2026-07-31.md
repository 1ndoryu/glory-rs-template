# Tareas completadas — 2026-07-31

## Continuación 297A-12/297A-22 — launcher y validación de entorno

- Se extrajo `MobileLauncher` a `frontend/src/features/mobile/mobile-launcher.ts`; `mobile-shell.ts` queda como coordinador de stack, rutas y lifecycle.
- El launcher mantiene cleanup explícito de gestos, tema, cuenta y ghost; las columnas 2/3 se resuelven dinámicamente al soltar.
- TypeScript: PASS. Vitest: **278/278 tests en 34 suites PASS**. `task:check -- 297A-22 --fresh`: PASS. `self-check`: PASS.
- Navegador: `768×1024` confirmó escritorio/tablet, taskbar y `scrollWidth === innerWidth`; `401 /auth/me` y `404 /uploads/` fueron respuestas esperadas del entorno sin sesión/ruta de archivo.
- La automatización no produjo evidencia estable del gesto táctil móvil; queda pendiente E2E real 320/360/390, foco, teclado y transición móvil↔tablet.
- El warning estructural de overlay quedó resuelto sin suppression: `src/models/workspace_overlay/` separa fachada DTO, `validation.rs`, `public_locator.rs` y `tests.rs`; `src/models/workspace/` agrupa el release por dominio.


## 297A-12 — Interacción móvil y frontera de capacidades

### Tramo cerrado: preservación transitoria segura

- Se añadió snapshot efímero en memoria para preservar formularios no sensibles y scroll durante la reinstanciación móvil↔tablet.
- El contrato es opt-in: controles restaurables declaran `data-transient="true"` y contenedores de scroll declaran `data-transient-scroll`.
- Se excluyen contraseñas, archivos, campos ocultos y metadata sensible; no se persisten valores en URL ni localStorage.
- Legacy outlet y Perfil shell tienen claves y raíces separadas; las transiciones canceladas, fallidas o destruidas limpian snapshots.
- `RouteAppAdapter` y Perfil esperan el montaje asíncrono antes de restaurar.

### Archivos principales del tramo

- `frontend/src/features/runtime/transient-state.ts`
- `frontend/src/features/runtime/transient-state.test.ts`
- `frontend/src/main.ts`
- `frontend/src/features/mobile/mobile-shell.ts`
- `frontend/src/features/runtime/route-app-adapter.ts`
- `frontend/src/components/ui/input.ts`
- `frontend/src/components/ui/textarea.ts`
- `frontend/src/components/ui/select.ts`

### Evidencia del tramo

- TypeScript sin errores.
- Vitest: **203/203 tests, 19 suites PASS**.
- `npm run task:check -- 297A-12 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-12`: PASS.

### Pendientes no falsamente cerrados

- E2E real de transición móvil↔tablet, Back/Forward, orientación y `popstate`.
- Validación visual en 320/360/390px y tablet 768px.
- Safe areas, teclado virtual, foco, zoom y apps críticas.
- Revisión posterior del selector estructural de fallback si aparecen apps con wrappers dinámicos.

## 297A-19 — URLs canónicas, foco e historial (tramo implementado)

### Qué
- Se añadió `AppDeepLink` al `AppRegistry` sin crear un segundo router.
- Se implementó `createPathDeepLink()` con parser/serializer allowlisted.
- Reader, Finder/Galería, About y Projects declaran rutas públicas canónicas.
- Apps legacy sin contrato explícito no aceptan parámetros dinámicos.
- El `RouteAppAdapter` valida parámetros y capacidades antes de hidratar una app.
- Rutas inválidas, parámetros inseguros o apps sin capacidad terminan en not-found seguro.
- Se añadió `stableParamsKey()` y se eliminó la deduplicación frágil por concatenación de valores.
- Se añadieron `pushPath()` y `replacePath()` al router, reutilizando el mismo canal de navegación.
- Se añadió `window-url-sync`, que deriva la URL de la ventana desktop o entrada móvil enfocada, sin serializar geometría ni parámetros privados.
- Las aperturas intencionales usan `pushPath`; foco pasivo, transición de presentación y restauración usan `replacePath`/`history: 'none'`.
- El router marca entradas internas en `history.state` con `createdByPush`, preservando campos adicionales; MobileStack coordina el desapilado con `source: 'sync'` para no alterar la URL antes de `popstate`. Apps locales sin historial restauran la ruta inferior y un deep link inicial vuelve a `/` sin abandonar el sitio.
- Se cubrieron canonicalización, allowlist, segmentos peligrosos, legacy params, claves deterministas, notificaciones del History API, política móvil push/none, Back coordinado y reflow de grid.

### Archivos principales
- `frontend/src/features/runtime/deep-links.ts`
- `frontend/src/features/runtime/deep-links.test.ts`
- `frontend/src/features/runtime/app-registry.ts`
- `frontend/src/features/runtime/app-registration.ts`
- `frontend/src/features/runtime/route-app-adapter.ts`
- `frontend/src/features/runtime/route-app-adapter.test.ts`
- `frontend/src/features/runtime/window-manager.ts`
- `frontend/src/features/runtime/window-url-sync.ts`
- `frontend/src/features/runtime/commands/navigation-commands.ts`
- `frontend/src/features/runtime/commands/navigation-commands.test.ts`
- `frontend/src/features/desktop/components/desktop-window.ts`
- `frontend/src/features/runtime/window-url-sync.test.ts`
- `frontend/src/features/mobile/mobile-stack.ts`
- `frontend/src/features/mobile/mobile-stack.test.ts`
- `frontend/src/features/mobile/mobile-shell.ts`
- `frontend/src/main.ts`
- `frontend/src/styles/variables.css`
- `frontend/src/features/desktop/utils/icon-grid.ts`
- `frontend/src/router.ts`
- `frontend/src/router.test.ts`
- `Agente/planes/plan-deep-links-ventanas-2026-07-31.md`
- `roadmap.md`

### Evidencia
- `npx tsc --noEmit`: PASS.
- Vitest: **195/195 tests, 17 suites PASS**.
- Revisión code-reviewer-luna: PASS; sin bloqueantes estructurales.
- `npm run task:check -- 297A-19 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-19`: PASS.
- Sentinel: 0 errores, 75 warnings heredados.
- VarSense: 0 errores, 2 avisos informativos.
- Custom: 0 errores, 3 avisos informativos.
- Chrome: `/projects` cargó en desktop 1440×900 con `.desktop-shell` y `.desktop-taskbar`, sin errores JS; en móvil cargó el shell de Proyectos y conservó la URL. La interacción móvil completa, `goBack()` real, Forward y `popstate` siguen pendientes de E2E navegador.
- `npm run self-check -- -TareaId 297A-19`: PASS sin diferencias.
- `Copiar URL`: comando global, fallback de clipboard, disponibilidad por deep link y evento `share_url_copied` cubiertos por tests. El evento solo incluye `routeName`, `appId` allowlisted, `presentationMode` y `success`; nunca envía la URL completa ni estado privado.
- Reporte: `.quality-reports/297A-19/latest.md`.

### Pendientes no falsamente cerrados
- E2E real de RouteAppAdapter/WindowManager + MobileShell/popstate, Back/Forward y transición de breakpoint en navegador.
- Analítica `deep_link_opened` y `window_focus_changed`.
- Recursos privados, drafts, entitlements/grants y versionado de URLs.
- E2E real de copiar URL en sesión limpia, varias ventanas y viewports.
- E2E de navegador para MobileShell/popstate, Back/Forward y transición de breakpoint.

## 297A-23 — Capacidades centralizadas del runtime (tramo F2)

### Qué
- Se creó `frontend/src/features/runtime/capability.ts` como única fuente de verdad para `Capability`, niveles y comparación.
- `hasCapability()` falla cerrado ante capacidades desconocidas y trata `requires` ausente como público.
- Se migraron AppRegistry, RouteAppAdapter, comandos, menú desktop, CommandRegistry, ResourceTypeRegistry, workspace merge/types/stores y AuthState.
- `authenticated` permanece en el contrato para Cuenta y futuras superficies autenticadas; no se añadió una rama artificial.
- Se añadió regresión de workspace para impedir que un nodo con capacidad corrupta se exponga a un admin.

### Archivos principales
- `frontend/src/features/runtime/capability.ts`
- `frontend/src/features/runtime/capability.test.ts`
- `frontend/src/features/runtime/app-registry.ts`
- `frontend/src/features/runtime/route-app-adapter.ts`
- `frontend/src/features/runtime/commands/app-commands.ts`
- `frontend/src/features/runtime/command-registry.ts`
- `frontend/src/features/runtime/resource-type-registry.ts`
- `frontend/src/features/runtime/workspace/merge.ts`
- `frontend/src/features/runtime/workspace/merge.test.ts`
- `frontend/src/features/runtime/workspace/stores.ts`
- `frontend/src/features/runtime/workspace/types.ts`
- `frontend/src/store.ts`
- `frontend/src/features/desktop/components/desktop-menu-bar.ts`
- `Agente/planes/plan-deuda-solid-runtime-2026-07-31.md`
- `Agente/documentacion/arquitectura/guia-agregar-app-2026-07-31.md`
- `roadmap.md`

### Evidencia
- TypeScript: PASS.
- Vitest: **246/246 tests PASS en 28 archivos**.
- `npm run task:check -- 297A-23 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-23`: PASS.
- Sentinel: 0 errores; VarSense: 0 errores; Rust: 4 comandos PASS.
- Revisión code-reviewer-luna: PASS, sin bloqueantes.
- `git diff --check`: PASS.

### Tramo F3 — políticas de instancia y frontera de presentación
- Se extrajeron `validateRouteAccess`, `canOpenApp` y `findExistingWindow` a `app-instances.ts`.
- Se mantuvo separado el contrato de parámetros públicos de los parámetros internos del workspace (`folderId`/`resourceId`).
- Se extrajeron `setMobileOpenHandler`, `isMobilePresentationReady`, `openInMobileIfActive` y `clearRuntimePresentation` a `runtime-presentation.ts`.
- La limpieza desktop conserva `shell-profile`; la móvil vacía la pila con `source: 'sync'`.
- Se añadieron pruebas de acceso, fail-closed, singleton, deduplicación parametrizada, handler/teardown y cleanup desktop/mobile.

### Archivos principales añadidos en F3
- `frontend/src/features/runtime/app-instances.ts`
- `frontend/src/features/runtime/app-instances.test.ts`
- `frontend/src/features/runtime/runtime-presentation.ts`
- `frontend/src/features/runtime/runtime-presentation.test.ts`

### Evidencia F3
- TypeScript: PASS.
- Vitest: **256/256 tests PASS en 30 archivos**.
- `npm run task:check -- 297A-23 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-23`: PASS.
- Sentinel: 0 errores; VarSense: 0 errores; Rust: 4 comandos PASS.
- Revisión code-reviewer-luna: PASS, sin bloqueantes.
- `git diff --check`: PASS.

### Tramo F4 — contrato anti-drift workspace/AppRegistry
- `workspace-app-contract.ts` valida todos los nodos estáticos `type:'app'` de `DEFAULT_RELEASE` y `ADMIN_NODES` contra `AppRegistry`.
- Detecta referencias no registradas (`nodeId:unregistered-app:appId`) y nodos app sin `refId` (`nodeId:missing-refId`).
- No exige que toda app registrada tenga icono; omite folders y shortcuts para evitar falsos positivos legítimos.
- El caso histórico de app sin registro y el caso de `refId` ausente tienen regresiones explícitas.

### Archivos principales F4
- `frontend/src/features/runtime/workspace/workspace-app-contract.ts`
- `frontend/src/features/runtime/workspace/workspace-app-contract.test.ts`
- `frontend/src/features/runtime/workspace/default-release.ts`
- `frontend/src/features/runtime/app-registration.ts`

### Evidencia F4
- TypeScript: PASS.
- Tests específicos anti-drift: **7/7 PASS**.
- Vitest: **261/261 tests PASS en 31 archivos**.
- `npm run task:check -- 297A-23 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-23`: PASS.
- Sentinel: 0 errores; VarSense: 0 errores; Rust: 4 comandos PASS.
- Revisión code-reviewer-luna: PASS, sin bloqueantes.
- `git diff --check`: PASS.

### Cierre F5 y pendientes no falsamente cerrados
- Fase 5 técnica/documental cerrada: guía, roadmap, completados, evidencia S1–S5 y gate quedan sincronizados.
- La validación visual manual del runtime permanece pendiente separada en 297A-24; no se atribuye a la deuda SOLID.

## 297A-24 — Cierre automático de ventanas y foco de Perfil

### Qué
- Se confirmó la causa raíz: `window-url-sync` proyectaba `/` al enfocar una entrada shell sin URL y `route-app-adapter` interpretaba ese cambio como navegación fuera del runtime.
- Se implementó S1 con `hasOpenRuntimeApp()`: una app runtime abierta impide la proyección accidental de `/`; `shell-profile` queda fuera de `AppRegistry` y no participa en la URL.
- La guardia respeta la presentación activa: desktop/tablet consultan ventanas; móvil consulta `mobileStackStore`, evitando estado stale de la otra superficie.
- Perfil ya puede recibir foco sin cerrar About, Galería u otras apps; cerrar la última app runtime sí permite volver a `/`.

### Archivos principales
- `frontend/src/features/runtime/window-url-sync.ts`
- `frontend/src/features/runtime/window-url-sync.test.ts`
- `frontend/src/features/runtime/route-app-adapter.test.ts`
- `frontend/src/features/runtime/route-app-adapter.ts`
- `frontend/src/features/desktop/desktop-shell.ts`
- `Agente/planes/plan-cierre-automatico-ventanas-2026-07-31.md`
- `Agente/lecciones/lecciones-aprendidas.md`
- `roadmap.md`

### Evidencia
- TypeScript: PASS.
- Vitest: **241/241 tests PASS en 27 archivos**.
- Regresiones específicas: **12/12 tests PASS**.
- `npm run task:check -- 297A-24 --fresh`: PASS, 0 errores.
- `npm run self-check -- -TareaId 297A-24`: PASS.
- Revisión code-reviewer-luna: PASS, sin bloqueantes.
- `git diff --check`: PASS.

### Pendiente controlado
- Validación visual manual en navegador desktop/tablet/móvil de Perfil, refresh y Back/Home.
- Apps internas sin URL pública permanecen fuera del historial; la frontera `resourceId`/`slug` de Reader quedó protegida: `resourceId` no se reinterpreta como `slug` y recursos sin `publicLocator` muestran feedback seguro. Resolver recursos por UUID mediante una referencia pública pertenece a 297A-10/297A-19.

### Sentinel / VarSense
- Sentinel: PASS, 0 errores.
- VarSense: PASS, 0 errores.
- Custom: PASS, 0 errores; warnings CSS compartidos no bloqueantes.

## 297A-20 — Iconos de escritorio con posición libre (snap-grid)

### Qué
- Grid geométrico snap 88px: `getGridMetrics`/`getCellAt`/`findFreeCell`/`planPlacement`/`reflowPositions` en `icon-grid.ts`, consciente de RTL (col 0 = derecha; el grid CSS empaqueta columnas a la derecha y el hueco sobrante queda a la izquierda).
- `grid-auto-rows` fijado a `--sistema-icono-fila` (64px, token nuevo) en lugar de `max-content`: antes las filas vacías colapsaban a 0px y la geometría no coincidía con el CSS grid (el icono caía fuera de su celda y filas ≥3 no eran usables).
- Drag conectado a `moveNodePosition()`/`onPlaceCell`: el drop es geométrico (solo `getCellAt`), ya no se pierde cuando el puntero está sobre una ventana abierta — corregido el límite de soltar solo en la mitad de pantalla. El drag a carpeta/papelera se conserva.
- Colisiones: `planPlacement` pasa `avoid=target` a `findFreeCell` para que el ocupante nunca quede en la celda del arrastrado (2 iconos juntos). `reflowPositions` clampa y escanea por filas/columnas.
- Fórmula RTL de posición de celda corregida en highlight (`positionCellHighlight`) y en la cuadrícula de depuración: los iconos caen exactamente donde la cuadrícula roja muestra (antes se desviaban 16px×(col+1) a la izquierda).
- Persistencia en overlay personal (`fieldOverrides.position {col,row}` + localStorage); móvil (≤768px) ignora posiciones y conserva `mobileOrder` del launcher.
- Modo depuración temporal (Ctrl+Shift+G, capa `.desktop-icon-grid__debug` con celdas etiquetadas) que el usuario pidió mantener; marcado `[DEPURACION TEMPORAL] PENDIENTE: eliminar`.

### Archivos principales
- `frontend/src/features/desktop/utils/icon-grid.ts` (nuevo)
- `frontend/src/features/desktop/utils/icon-grid.test.ts` (nuevo)
- `frontend/src/features/desktop/utils/icon-grid-placement.test.ts` (nuevo)
- `frontend/src/features/desktop/utils/icon-reorder.ts`
- `frontend/src/features/desktop/utils/icon-drag.ts`
- `frontend/src/features/desktop/workspace-icon-grid.ts`
- `frontend/src/features/runtime/workspace/merge.ts` + `merge.test.ts`
- `frontend/src/features/runtime/workspace/overlay-mutations.ts`
- `frontend/src/styles/desktop/desktop-shell.css`
- `frontend/src/styles/desktop/desktop-responsive.css`
- `frontend/src/styles/variables.css`
- `Agente/planes/plan-iconos-libres-desktop-2026-07-31.md` (movido a `planes/completados/`)
- `roadmap.md`

### Evidencia
- `npx tsc --noEmit`: PASS.
- Vitest: **37/37 tests PASS** (icon-grid + icon-grid-placement + merge).
- Navegador: la cuadrícula debug coincide exactamente con los iconos reales; colisión resuelta (Snake→(0,2), Proyectos desplazado→(1,2), persistido en localStorage); **usuario confirmó manualmente: «ya probé, funciona bien, y los iconos no se juntan»** — cubre el drop bajo ventana abierta y el bug de iconos juntos.

### Gotchas
- El grid CSS usa `direction: rtl` para cubrir todo el escritorio; toda fórmula de posición debe invertirse (col 0 = derecha). Fórmula correcta: `right - (col+1)*cellWidth - col*columnGap`; la inversa en `getCellAt` ya era correcta.
- `getGridMetrics` lee `gridAutoRows` (parseFloat) con fallback a la altura del primer icono; si se vuelve a `max-content`, la geometría se rompe.
- El otro agente reescribió `icon-grid.test.ts` (solo cellKey/occupiedCells/findFreeCell) y movió mi test de regresión a `icon-grid-placement.test.ts`; no tocar su reorganización.

### Sentinel
- No aplica regla nueva (fue corrección de geometría/UX, sin patrón automatizable adicional).

### GLORY
- La lógica de grid/colisión es agnóstica, pero depende del modelo de overlay (`fieldOverrides`) específico de este proyecto; se mantuvo en el proyecto. No aplica.

## 297A-13 — Overlay remoto del workspace (tramo cerrado)

### Qué
- Se creó `user_workspace_overlays` con una fila por usuario, JSONB versionado, revisión optimista y timestamp.
- Se implementaron modelo, repository, service, handler autenticado y rutas OpenAPI en `/api/workspace/overlay`.
- El backend valida versión, tamaño máximo, cantidades, IDs, claves permitidas, labels, posiciones, `mobileOrder`, tipos de nodo, `refId`, `requires`, ciclos, tombstones duplicados y JSON persistido corrupto.
- La escritura inicial con una revisión distinta de `0` devuelve conflicto sin crear una fila fantasma.
- El overlay local continúa siendo la fuente inmediata y `localStorage` el fallback offline; nunca se persisten `ResolvedWorkspace`, ventanas, foco, z-index ni geometría derivada.
- `overlay-sync.ts` sincroniza solo cambios con `source: 'user'`, usa revisión optimista, rebasa respuestas contra el release actual y maneja offline/401/409 sin sobrescribir silenciosamente.
- `overlay-conflict-ui.ts` presenta una única resolución accesible `remote/local`; las mutaciones locales durante el conflicto se conservan hasta una decisión explícita.
- Los IDs publicados tienen precedencia frente a colisiones de `addedItems`; sus overrides tampoco pueden modificar indirectamente el nodo publicado.

### Archivos principales
- `migrations/20260731120000_297a13_workspace_overlays.up.sql`
- `migrations/20260731120000_297a13_workspace_overlays.down.sql`
- `src/models/workspace/mod.rs`
- `src/models/workspace_overlay/mod.rs`
- `src/models/workspace_overlay/validation.rs`
- `src/models/workspace_overlay/public_locator.rs`
- `src/models/workspace_overlay/tests.rs`
- `src/repositories/workspace_overlay_repo.rs`
- `src/services/workspace_overlay_svc.rs`
- `src/handlers/workspace_overlay_handler.rs`
- `src/models/mod.rs`
- `src/repositories/mod.rs`
- `src/services/mod.rs`
- `src/handlers/mod.rs`
- `frontend/src/services/workspace.service.ts`
- `frontend/src/features/runtime/workspace/overlay-sync.ts`
- `frontend/src/features/runtime/workspace/overlay-sync.test.ts`
- `frontend/src/features/runtime/workspace/overlay-conflict-ui.ts`
- `frontend/src/features/runtime/workspace/merge.ts`
- `frontend/src/features/runtime/workspace/merge.test.ts`
- `frontend/src/main.ts`
- `frontend/src/styles/components.css`
- `roadmap.md`
- `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`
- `Agente/planes/plan-modo-oscuro-os-2026-07-31.md`

### Evidencia
- Migración aplicada mediante `node scripts/run-with-db.mjs sqlx migrate run`, sin reset de la base local.
- `cargo fmt --all -- --check`: PASS.
- `cargo check`: PASS.
- Clippy estricto (`-D warnings`): PASS.
- Backend: 15 tests PASS.
- Frontend: TypeScript PASS; Vitest **221 tests PASS en 22 suites**.
- `npm run task:check -- 297A-13 --fresh`: PASS; 0 errores.
- `npm run self-check -- -TareaId 297A-13`: PASS.
- Revisión code-reviewer-luna final: PASS, sin bloqueantes.
- Reporte: `.quality-reports/297A-13/latest.md`.

### Gotchas y límites no falsamente cerrados
- La validación backend no cruza IDs contra el release: el backend valida forma y seguridad; el frontend rebasa contra el release publicado.
- Dos pestañas/dispositivos todavía requieren E2E real y una política de merge semántico para ediciones concurrentes no resolubles con `remote/local`.
- Cuenta base como app del OS ya está implementada en desktop/móvil; registro verificado, recuperación avanzada, MFA/auditoría y E2E visual permanecen pendientes en el roadmap.

### Cuenta base (tramo implementado)
- Cuenta se registró como app singleton pública en `AppRegistry`.
- La vista reactiva reutiliza `AuthService` y `authStore`, con estados invitado/autenticado/admin.
- `/login` abre la misma app mediante deep link; el wrapper legacy no duplica el formulario.
- Desktop y launcher móvil proyectan el mismo estado de sesión y exponen la acción para abrir Cuenta.
- Login exige confirmación de sesión; logout libera la sesión mediante el servicio existente.
- Lifecycle: la suscripción de la app y el control móvil se destruyen con `AbortSignal`/teardown.
- Registro verificado, recuperación, MFA, auditoría avanzada y E2E visual/multi-dispositivo no se marcan como terminados.

### Sentinel / VarSense / GLORY
- Sentinel: PASS, 0 errores, 98 warnings heredados.
- VarSense: PASS, 0 errores, 2 avisos informativos.
- Custom: PASS, 0 errores, 4 warnings informativos.
- Los avisos CSS del toolbar son informativos y preexistentes del contrato visual compartido; no bloquean el gate.
- La capa de sincronización y los contratos de overlay son agnósticos y reutilizables; la semántica de WorkspaceNode/Release permanece específica de wandori.us. GLORY no aplica.

### 297A-23 — Contrato público de recursos (`publicLocator`)

- Se separó explícitamente `refId`/`resourceId` interno del locator público `{ appId, params }`; nunca se convierte un UUID interno en slug.
- El locator se conserva en el release y al copiar nodos; cualquier locator aportado por un overlay queda limitado por la misma frontera pública segura y no puede exponer `refId`, tokens, grants ni rutas privadas. `requires` ausente equivale a `public`.
- Desktop, Finder y launcher móvil usan el mismo resolver allowlisted; una app inexistente, privada, sin deep-link o con parámetros no permitidos devuelve feedback seguro y no abre una ventana vacía.
- Backend valida estructura, límites, claves internas prohibidas y que el locator solo exista en nodos `resource`/`shortcut` públicos. La semántica de `appId`/deep-link permanece en el `AppRegistry` frontend para evitar duplicar el catálogo en Rust.

#### Evidencia final

- TypeScript: PASS.
- Vitest: **267/267 tests PASS en 32 archivos**.
- `cargo fmt --all -- --check`: PASS.
- `cargo check`: PASS.
- Tests unitarios `workspace_overlay`: **8/8 PASS**.
- `git diff --check`: PASS.
- `npm run task:check -- 297A-23 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-23`: PASS.
- Revisión arquitectónica: sin bloqueantes.
- `npm test` ejecuta `cargo test` mediante `run-with-db`, con la BD `glory_backend_wandorius` y migraciones instaladas: **17/17 tests PASS**. `cargo test` directo no es el flujo soportado porque no deriva la BD ni inyecta `DATABASE_URL`.
- E2E visual desktop/móvil y backfill de locators para recursos antiguos permanecen pendientes controlados del roadmap.

## 297A-14 — Vertical de artículos/editor

### Qué

- Se extrajo el editor de artículos/About del monolito Admin a la app lazy `article-editor`.
- La app requiere capacidad `admin`, es multiinstancia y no declara deep link público; `articleId` permanece como parámetro interno.
- La ventana monta loading inmediatamente; la carga de artículo y Tiptap ocurre después y se cancela con `AbortSignal`/`MountedView.destroy()`.
- Los errores de apertura, carga del editor y carga del Admin producen feedback visible y no rechazos no controlados.
- Después de crear, el editor conserva el ID devuelto y los siguientes guardados actualizan; el toolbar multimedia consulta el ID actual.
- El listado Admin se actualiza por evento tipado, invalida caché, descarta respuestas fuera de orden y libera suscripciones al desmontar.

### Archivos principales

- `frontend/src/features/desktop/apps/article-editor/article-editor.ts`
- `frontend/src/features/runtime/article-editor-events.ts`
- `frontend/src/features/runtime/article-editor-events.test.ts`
- `frontend/src/features/runtime/app-registration.ts`
- `frontend/src/features/runtime/app-registration.test.ts`
- `frontend/src/pages/admin-articles.ts`
- `frontend/src/pages/admin.ts`
- `Agente/planes/plan-programas-editoriales-2026-07-31.md`
- `roadmap.md`

### Evidencia

- Frontend typecheck: PASS.
- Vitest: **281/281 tests en 35 archivos PASS**.
- Build frontend: PASS.
- `npm run check:back`: PASS.
- `npm test`: **17/17 tests backend PASS**.
- `npm run task:check -- 297A-14 --fresh`: PASS.
- `npm run self-check -- -TareaId 297A-14`: PASS.
- Sentinel: 0 errores.
- VarSense: 2 avisos no bloqueantes.
- Custom: 5 avisos informativos no bloqueantes, incluidos avisos CSS compartidos de `modal.ts`.
- `git diff --check`: PASS.
- Revisión code-reviewer-luna: sin bloqueantes tras corregir create→update, carreras del listado y asociación multimedia.

### Límites no falsamente cerrados

- Editor de proyectos, productos versionados, biblioteca de media, paridad completa del Admin y E2E visual permanecen pendientes en 297A-14.
- No se marca cerrado el epic editorial completo.

## 297A-26 — Preferencias de tema embebidas en la ventana Cuenta

### Qué
- Petición del usuario: el modal global que aparecía al iniciar sesión (conflicto de preferencias) debe vivir dentro de la ventana Cuenta, no como modal del sistema.
- Se creó `preferences-panel.ts`: panel de preferencias **siempre visible** en la ventana Cuenta con selector de tema (sistema/claro/oscuro), etiqueta accesible y `aria-pressed` en el tema activo.
- El bloque de resolución de conflicto aparece **embebido bajo el selector** solo cuando `preferencesSyncStore` está en `conflict` (dispositivo ≠ cuenta), reutilizando las clases `.preferences-conflict*` existentes del sistema de diseño.
- Cambiar tema desde el panel sincroniza con la cuenta (`themeStore.set` con fuente `user` → `queueLocalUpdate`); los cambios externos de tema (barra superior, atajo, móvil) se reflejan en el panel vía `subscribeSimple`.
- Se eliminó el modal global: borrados `preferences-conflict-ui.ts` (+test) y `preferences-conflict-panel.ts` (+test); `main.ts` dejó de inicializarlo. La limpieza es íntegra, sin referencias residuales (grep confirmado).

### Archivos
- `frontend/src/features/runtime/preferences-panel.ts` (nuevo)
- `frontend/src/features/runtime/preferences-panel.test.ts` (nuevo, 8 tests)
- `frontend/src/features/runtime/account-view.ts` (panel embebido en Cuenta, destroy idempotente)
- `frontend/src/main.ts` (removido `initPreferencesConflictUI`)
- `frontend/src/styles/components.css` (`.preferences-panel*` con tokens existentes; `.campo-select` sin `20px` hardcodeado)
- Borrados: `preferences-conflict-ui.ts`, `preferences-conflict-ui.test.ts`, `preferences-conflict-panel.ts`, `preferences-conflict-panel.test.ts`

### Gotchas
- El primer diseño solo renderizaba el panel en conflicto; como dispositivo y cuenta coincidían, la ventana Cuenta no mostraba nada ("no veo las opciones de preferencia"). Solución: panel siempre visible + bloque de conflicto condicional.
- `main.ts` quedó con una referencia residual a `stopPreferencesConflictUI` (TS2552) tras el primer borrado; se removió en la limpieza final.
- Correr `npx vitest` desde la raíz del workspace dispara el prompt de instalación de npx y se cuelga; siempre ejecutar desde `frontend/`.
- VarSense siguió reportando `20px` hardcodeado en `.campo-select` aunque el literal ya no existe en el CSS (diagnóstico stale); la corrección `calc(var(--espacio-md) + var(--espacio-xs))` quedó aplicada.
- Flaky en el primer run de los tests del panel (2 fallos por `aria-pressed`); un segundo run limpio desde `frontend/` pasó 8/8. Suite completa: **286/286 PASS**.

### Sentinel
- No aplica (ninguna regla nueva; el código cumple el sistema existente).

### GLORY
- No aplica (panel específico de wandori.us; la receta de botones y tokens ya vive en el sistema del OS).

### Evidencia en navegador
- Panel visible en la ventana Cuenta: h2 "preferencias", etiqueta "tema" + 3 botones con el activo marcado.
- Cambiar tema desde el panel aplica y sincroniza con la cuenta (persiste tras reload sin conflicto).
- Flujo de conflicto verificado: cerrar sesión → cambiar tema local → iniciar sesión → el bloque "preferencia actualizada" aparece embebido en Cuenta (dispositivo: claro / cuenta: oscuro) sin modal; "usar preferencia de cuenta" resuelve y aplica el tema remoto.
