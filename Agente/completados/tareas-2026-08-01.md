# Tareas completadas — 2026-08-01

## 018A-1 — Barra de acciones inferior de ventana (experimento → regla aprobada)

- **Qué:** Las acciones primarias de una ventana viven en una franja inferior del chrome (`.desktop-window__actions`), hija directa de `.desktop-window` debajo del body padded — fuera de su padding y de su scroll. Slot opcional `actions` en `MountedView`/`WindowContent` propagado por toda la cadena (window-manager → desktop-shell → createDesktopWindow, que lo añade tras el body). Admin rellena la franja según su tab activo (`+ nuevo artículo/proyecto/producto`); tabs sin alta la ocultan. Botones alineados al final de la franja (`justify-content: flex-end`) tras refino del usuario. Las listas absorben su scroll (`overflow-y: auto`) para que la franja quede fija con contenido largo.
- **Archivos:** `frontend/src/core/lifecycle.ts`, `frontend/src/features/runtime/window-store.ts`, `frontend/src/features/runtime/window-manager.ts`, `frontend/src/features/desktop/components/desktop-window.ts`, `frontend/src/features/desktop/desktop-shell.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/pages/admin.ts`, `frontend/src/pages/admin-projects.ts`, `frontend/src/pages/admin-products.ts`, `frontend/src/styles/desktop/desktop-window.css`, `frontend/src/styles/pages.css`, `frontend/src/styles/components.css`.
- **Gotchas:** `createEl` no soporta `hidden` → se setea `el.hidden = true` directamente y CSS `[hidden] { display:none }` (necesario porque `display:flex` anula el hidden nativo). `renderAdmin` legacy del router se conserva como wrapper de `createAdminWindowView()` (la franja pertenece al chrome de la ventana, no a la página). La franja debe crearse síncrona para que el shell la tenga al abrir la ventana.
- **Sentinel:** no aplica (regla de diseño, no lint).
- **GLORY:** el slot `actions` del runtime es agnóstico y reutilizable (la franja podría extraerse a glory-rs si se generaliza); por ahora queda en el proyecto como regla visual con manual.

## 018A-2 — Manual de identidad actualizado con la regla de barra de acciones

- **Qué:** Regla formal en `manual-identidad-visual-os-2026-07-29.md` §9 (diagrama de ventana con franja inferior + subsección "Barra de acciones de la ventana (chrome inferior)": posición, botones al final/derecha, scroll del contenido, ocultación, slot opcional, móvil). Guía de agregar-app actualizada: `actions?` en `MountedView` y regla del contrato.
- **Archivos:** `Agente/documentacion/design-system/manual-identidad-visual-os-2026-07-29.md`, `Agente/documentacion/arquitectura/guia-agregar-app-2026-07-31.md`.
- **Gotchas:** El manual es la fuente canónica de identidad; la regla se añadió en §9 (chrome) y no se duplicó en §13.
- **Sentinel:** no aplica.
- **GLORY:** No aplica (documentación del proyecto).

## 018A-3 — Plan de ajustes que implica la regla

- **Qué:** Plan `Agente/planes/plan-barra-acciones-ventanas-2026-08-01.md` con 5 fases: base (hecha), alcance móvil del slot (Admin móvil no debe perder el botón de alta), migración de editores a la franja, inventario de ventanas restantes y prevención automatizable. Roadmap actualizado con la sección 018A y sus pendientes.
- **Archivos:** `Agente/planes/plan-barra-acciones-ventanas-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** Riesgo clave detectado: en móvil la franja es desktop-only, así que Admin móvil hoy perdería el botón de alta — la Fase 1 del plan decide el mecanismo antes de migrar más apps.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 018A-4 — Rendimiento local de Sentinel/VarSense y tests frontend

- **Qué:** Se limitó Vitest a un worker sin paralelismo de archivos, se añadió selección segura de tests modificados, lock fail-fast, fingerprint de caché versionado por runtime/plataforma y captura acotada de stdout/stderr con marcador visible de truncado. Scope ahora trata borrados/renombres como full y se validan globs, caché, lock y salida ruidosa.
- **Archivos:** `frontend/package.json`, `frontend/vitest.config.ts`, `quality.config.json`, `scripts/quality/preflight.mjs`, `scripts/quality/task-check.mjs`, `scripts/quality/cache.mjs`, `scripts/quality/runner.mjs`, `scripts/quality/scope.mjs`, `scripts/quality/run-frontend-tests.mjs`, `scripts/quality/tests/cache.test.mjs`, `scripts/quality/tests/lock.test.mjs`, `scripts/quality/tests/runner.test.mjs`, `scripts/quality/tests/scope.test.mjs`, `roadmap-sentinel.md`.
- **Gotchas:** `npm test` y `test:full` conservan la suite completa; `test:changed` es explícito y solo selecciona tests modificados cuando no hay cambios ambiguos. No se detuvieron procesos Node/Vite ajenos del workspace.
- **Sentinel:** se reforzó el orquestador; no se añadieron reglas de dominio al core.
- **GLORY:** No aplica; la capacidad es reusable en el quality runner, mientras la política de selección vive en el proyecto.

## 018A-5 — Migración portable y recordatorio condicional de commit

- **Qué:** Se añadió el recordatorio de cierre condicional: los bloques intermedios o compartidos no fuerzan commit; los bloques entregables siguen el protocolo de staging/commit/push. Se migraron reglas reutilizables al core de Sentinel (boundaries DOM/window, servicios, logger, shell seguro, barrels, singleton e ISP) con configuración portable y fixtures. VarSense ahora ofrece `all` con provider cacheado y reglas `token-duplicate`/`token-unused`. El quality runner usa un adapter declarativo común (`structured-tool.mjs`) y prueba escrituras atómicas concurrentes.
- **Archivos:** `scripts/quality/reminders.mjs`, `scripts/quality/custom-rules.mjs`, `scripts/quality/adapters/custom.mjs`, `scripts/quality/adapters/structured-tool.mjs`, `scripts/quality/adapters/sentinel.mjs`, `scripts/quality/adapters/varsense.mjs`, `scripts/quality/stage-runner.mjs`, `scripts/quality/preflight.mjs`, `scripts/quality/scope.mjs`, `scripts/quality/adapters/common.mjs`, `scripts/quality/tests/`, `sentinel.config.json`, `varsense.config.json`, `quality-tools.json`, `roadmap-sentinel.md`, `Agente/documentacion/herramientas/matriz-paridad-sentinel-varsense-2026-08-01.md`, `.quality-tools/sentinel`, `.quality-tools/varsense`.
- **Gotchas:** Sentinel (`107be9b6`) y VarSense (`b1aa3f06`) quedan fijados por manifiesto; ambos repositorios upstream conservan cambios ajenos sin stagear. La suite VarSense terminó con 46 pruebas y Sentinel con sus fixtures portables; no se reinstaló VS Code ni se hizo deploy.
- **Sentinel:** reglas portables, contrato de findings con remediation/confidence/analyzerVersion, bridge local filtrado para evitar duplicados y reportes con schema versionado.
- **GLORY:** No aplica; el código reusable permanece en los repositorios agnósticos y las boundaries/severidades viven en configuración del proyecto.

## 018A-6 — Desbloqueo del roadmap principal

- **Qué:** Se separó el mínimo operativo obligatorio del backlog avanzado de Sentinel/VarSense. El gate reproducible, los contratos estructurados, la caché/lock, la redacción, el reporte y los commits fijados quedan como única barrera inmediata; benchmarks, paridad completa, reglas de baja frecuencia y releases se difieren explícitamente.
- **Archivos:** `roadmap-sentinel.md`, `roadmap.md`.
- **Gotchas:** Los pendientes diferidos solo vuelven a prioridad si aparece una regresión real o una tarea del producto depende de ellos. Los cambios no relacionados de frontend permanecen fuera de este bloque.
- **Sentinel:** No añade reglas; ajusta prioridad y criterios de salida.
- **GLORY:** No aplica.

## 317A-5 — Persistencia de sesión de ventanas

- **Qué:** Se cerró la persistencia de ventanas desktop/tablet y del stack móvil. La sesión se restaura antes del router y la raíz inicial ya no limpia las ventanas restauradas; la URL sigue enfocando la app correspondiente sin duplicarla.
- **Archivos:** `frontend/src/features/runtime/route-app-adapter.ts`, `frontend/src/features/runtime/route-app-adapter.test.ts`, `frontend/src/main.ts`, `frontend/src/features/runtime/window-session.ts`, `frontend/src/features/runtime/window-session-restore.ts`, `Agente/planes/plan-persistencia-sesion-ventanas-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** La validación visual confirmó 1024×768 (Perfil + Galería) y 390×844 (Galería móvil). El commit queda pendiente de agrupar porque `main.ts` y el runtime ya contienen cambios compartidos de otro agente.
- **Sentinel:** `task:check -- 317A-5 --fresh` PASS; warnings no bloqueantes ya existentes/documentados.
- **GLORY:** No aplica; el módulo depende del runtime específico del OS y conserva la frontera agnóstica del window manager.

## 018A-8 — Analítica de deep links y cambios de foco

- **Qué:** Se añadieron `deep_link_opened` y `window_focus_changed` al catálogo tipado. El RouteAppAdapter mide solo rutas válidas/autorizadas y `window-url-sync` centraliza cambios de foco en desktop, tablet y móvil para evitar duplicados.
- **Archivos:** `frontend/src/features/analytics/dispatcher.ts`, `frontend/src/features/runtime/route-app-adapter.ts`, `frontend/src/features/runtime/window-url-sync.ts`, comandos de foco y sus tests, `Agente/planes/plan-deep-links-ventanas-2026-07-31.md`, `roadmap.md`.
- **Gotchas:** Se eliminaron emisiones directas de comandos que duplicaban el evento; quedan pendientes los E2E de historial, permisos, viewports y recursos privados.
- **Sentinel:** `task:check -- 297A-19 --fresh` PASS; sin errores bloqueantes.
- **GLORY:** No aplica; el catálogo y el adapter son reutilizables y la política de rutas permanece específica del OS.

## 018A-9 — Comercio privado, analytics y auditoría de sesión

- **Qué:** Se implementó el primer bloque autónomo de comercio: órdenes con idempotencia, eventos Stripe repetibles, versiones adquiridas, entitlements con token hash, outbox deduplicado y endpoint privado de descarga con expiración y confinamiento al storage. El checkout frontend reenvía la misma clave en body/header. Analytics ahora valida lotes, inserta en una sola sentencia multi-fila y deduplica reintentos por `event_id`; el tracker evita flush concurrente y deja fallos observables. Login registra éxito/fallo con hash de IP y sin credenciales.
- **Archivos:** `migrations/20260801000000_297a15_commerce_security.*`, `migrations/20260801020000_297a16_analytics_idempotency.*`, `migrations/20260801030000_297a13_auth_audit.*`, `src/handlers/download_handler.rs`, `src/handlers/stripe_webhook.rs`, `src/handlers/auth.rs`, `src/handlers/mod.rs`, `src/models/product.rs`, `src/models/settings.rs`, `src/repositories/commerce_repo.rs`, `src/repositories/product_repo.rs`, `src/repositories/analytics_repo.rs`, `src/repositories/auth_audit_repo.rs`, `src/services/commerce.rs`, `src/services/email.rs`, `src/services/settings_svc.rs`, `src/handlers/settings_handler.rs`, `frontend/src/api/client.ts`, `frontend/src/services/product.service.ts`, `frontend/src/features/analytics/tracker.ts`, `frontend/src/services/analytics.service.ts`, `frontend/src/api/types.ts`, `Agente/planes/plan-comercio-seguro-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** El worker outbox, secretos reales de Stripe/Resend, UI de Tienda/Pedidos, reembolsos y retirada de `/uploads` público siguen pendientes por requerir intervención/operación. La migración conserva columnas legacy para compatibilidad.
- **Sentinel:** `cargo check`, `npm test` (28 Rust), type-check y suite frontend (382/49) pasan; el gate completo se ejecuta al cierre del bloque.
- **GLORY:** Helpers de token y repositorios son candidatos agnósticos, pero permanecen en este proyecto hasta que exista un contrato de comercio compartido.

## 018A-10 — Consolidación de checklist SOLID y porcentaje ejecutable

- **Qué:** Se actualizaron los detalles duplicados del roadmap para reflejar evidencia real, se marcaron las revisiones S1–S5 aplicables a los bloques cerrados y se dejaron únicamente pendientes humanos/visuales/operativos. El roadmap queda en 173 casillas cerradas de 214 (80,8%); el porcentaje no oculta E2E, credenciales, UI de comercio ni eliminación legacy pendientes.
- **Archivos:** `roadmap.md`.
- **Gotchas:** Las casillas SOLID marcadas corresponden a la revisión técnica del bloque, no implican que el epic completo esté listo para producción.
- **Sentinel:** no aplica; la evidencia de cada bloque ya pasó `task:check`.
- **GLORY:** No aplica.

## 018A-11 — Base de notificaciones del OS

- **Qué:** Se añadió la app pública `Novedades` con ruta `/notifications`, fuente derivada del release público, estado leído local idempotente y contador de no leídas. La campana Lucide de 1px se integró en la barra desktop y el launcher móvil; la vista ofrece recarga, foco accesible y errores visibles.
- **Archivos:** `frontend/src/features/notifications/notifications-store.ts`, `frontend/src/features/notifications/notifications-store.test.ts`, `frontend/src/features/notifications/notifications-view.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/desktop/components/desktop-menu-bar.ts`, `frontend/src/features/mobile/mobile-launcher.ts`, `frontend/src/styles/components.css`, `frontend/src/styles/desktop/desktop-menu.css`, `frontend/src/styles/mobile/mobile-prototype.css`, `Agente/planes/plan-notificaciones-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** La entrega persistente por cuenta, el panel admin y los casos E2E se mantienen pendientes; no se creó una API paralela ni se expusieron overlays/drafts.
- **Sentinel:** `task:check -- 297A-21` PASS; type-check y suite frontend 385/50 PASS.
- **GLORY:** No aplica; la fuente depende del release específico de wandori.us.

## 018A-12 — Privacidad y paneles de Analytics

- **Qué:** Analytics quedó opt-in con banner accesible y estados `unknown/granted/denied`; tracker y backend bloquean envíos sin consentimiento. IP/user-agent se almacenan solo como SHA-256, la migración elimina user-agent histórico en claro y Admin dispone de purga acotada de 30–730 días. Estadísticas se expusieron como programa admin independiente con paneles Overview/Content/OS/Commerce/Reliability y exportación JSON.
- **Archivos:** `migrations/20260801050000_297a16_analytics_privacy.*`, `src/models/settings.rs`, `src/repositories/analytics_repo.rs`, `src/services/settings_svc.rs`, `src/handlers/settings_handler.rs`, `frontend/src/services/analytics.service.ts`, `frontend/src/features/analytics/consent-store.ts`, `frontend/src/features/analytics/consent-banner.ts`, `frontend/src/features/analytics/consent-store.test.ts`, `frontend/src/features/analytics/analytics-panel.ts`, `frontend/src/features/analytics/analytics-panel.test.ts`, `frontend/src/features/analytics/tracker.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/workspace/default-release.ts`, `frontend/src/styles/components.css`, `Agente/planes/plan-analytics-privacidad-2026-08-01.md`, `Agente/documentacion/indice-documentacion-2026-07-29.md`, `roadmap.md`.
- **Gotchas:** Revisión legal del texto y E2E de dos cuentas quedan pendientes; el `down` no restaura user-agents por diseño de privacidad.
- **Sentinel:** `task:check -- 297A-16 --fresh` PASS; Sentinel/VarSense sin errores, Rust 28 tests, type-check y suite frontend 390/52 PASS.
- **GLORY:** No aplica; la política de privacidad depende del producto, aunque el patrón de doble frontera es reusable.

## 018A-13 — Persistencia server-side de notificaciones

- **Qué:** Se añadió el catálogo persistente `notifications` y las lecturas por usuario `notification_reads`. Los releases crean una novedad publicada dentro de la misma transacción; Admin puede listar, crear, publicar y archivar avisos. La app frontend consume la API pública/por cuenta y conserva fallback offline local.
- **Archivos:** `migrations/20260801060000_297a21_notifications.*`, `src/models/notification.rs`, `src/repositories/notification_repo.rs`, `src/services/notification_svc.rs`, `src/handlers/notifications.rs`, módulos de registro, `src/services/workspace_svc.rs`, `frontend/src/services/notifications.service.ts`, `frontend/src/features/notifications/notifications-store.ts`, `Agente/planes/plan-notificaciones-2026-08-01.md`, `roadmap.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** El panel visual Admin y E2E multi-cuenta/dispositivo quedan diferidos para no bloquear el siguiente bloque de comercio; drafts nunca aparecen en endpoints públicos.
- **Sentinel:** `task:check -- 297A-21 --fresh` y `self-check -- -TareaId 297A-21` PASS; Sentinel/VarSense 0 errores, Rust 28 tests y type-check frontend pasan; suite específica de notificaciones 3/3.
- **GLORY:** No aplica; la política de avisos y el catálogo de releases son específicos de wandori.us.

## 018A-14 — Programas OS de comercio

- **Qué:** Se registraron Tienda, Pedidos y Descargas como apps compartidas por desktop/tablet/móvil. Tienda carga el catálogo público filtrado por SQL, valida el correo y abre el checkout real con idempotencia; Pedidos/Descargas muestran estados vacíos sin inventar historial. Se añadió una migración aditiva para incorporar los nodos al release existente.
- **Archivos:** `src/repositories/product_repo.rs`, `src/services/product_svc.rs`, `src/handlers/products_handler.rs`, `migrations/20260801070000_297a15_store_apps.*`, `frontend/src/services/product.service.ts`, `frontend/src/features/commerce/store-view.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/workspace/default-release.ts`, `frontend/src/features/runtime/workspace/workspace-app-contract.test.ts`, `frontend/src/styles/components.css`, `Agente/planes/plan-comercio-seguro-2026-08-01.md`, `roadmap.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Worker/outbox, proveedor Stripe/Resend real, historial autenticado, reembolsos/chargebacks, migración de uploads y E2E quedan diferidos.
- **Sentinel:** `task:check -- 297A-15 --fresh` y `self-check -- -TareaId 297A-15` PASS; Sentinel/VarSense 0 errores, Rust 28 tests, type-check y contrato AppRegistry 5/5 pasan.
- **GLORY:** No aplica; la app y la política de catálogo son específicas del producto.

## 018A-15 — Panel admin de novedades

- **Qué:** La app Novedades ahora proyecta un panel adicional para admins: listar avisos, crear borradores/publicaciones y cambiar estado a `draft`, `published` o `archived`. Reutiliza `NotificationsService`, el mismo catálogo y las capacidades server-side; visitantes solo reciben la vista pública.
- **Archivos:** `frontend/src/features/notifications/notifications-admin.ts`, `frontend/src/features/notifications/notifications-view.ts`, `frontend/src/services/notifications.service.ts`, `frontend/src/styles/components.css`, `Agente/planes/plan-notificaciones-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** E2E de permisos, dos dispositivos y overlay personalizado sigue diferido; el panel no permite publicar drafts desde el cliente sin AdminUser.
- **Sentinel:** `task:check -- 297A-21 --fresh` y `self-check -- -TareaId 297A-21` PASS; Sentinel/VarSense sin errores, type-check y tests de notificaciones 3/3 pasan. Queda advertencia preexistente de tamaño de `components.css`.
- **GLORY:** No aplica; el panel es específico del producto.

## 018A-16 — Registro verificado y recuperación básica

- **Qué:** Se añadieron tokens opacos con hash y consumo atómico para verificación de email y recuperación de contraseña. El registro queda detrás de `registration_enabled=false`, el login rechaza cuentas no verificadas, la recuperación no enumera usuarios y el cambio de contraseña revoca sesiones activas. Se añadieron contratos OpenAPI, servicio frontend y envío opcional mediante Resend sin activar proveedor real.
- **Archivos:** `migrations/20260801080000_297a13_verified_auth.*`, `src/models/user.rs`, `src/repositories/auth_token_repo.rs`, `src/repositories/user.rs`, `src/services/auth.rs`, `src/services/email.rs`, `src/handlers/auth.rs`, `src/handlers/mod.rs`, `frontend/src/services/auth.service.ts`, `Agente/planes/plan-auth-verificado-2026-08-01.md`, `Agente/documentacion/indice-documentacion-2026-07-29.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `roadmap.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** UI de registro/verificación/recuperación, proveedor Resend/SMTP configurado, rate limits específicos, MFA/passkey y E2E quedan diferidos; el registro permanece cerrado hasta cerrar esas dependencias.
- **Sentinel:** `task:check -- 297A-13 --fresh` y `self-check -- -TareaId 297A-13` PASS; Sentinel/VarSense sin errores, Rust 30 tests y type-check frontend pasan. Se añadieron pruebas unitarias de opacidad, hash y consumo único.
- **GLORY:** No aplica; las rutas y política de alta pertenecen a wandori.us, mientras que el patrón de token puede extraerse a Glory cuando exista segundo consumidor real.

## 018A-17 — OpenAPI y Orval tags-split sin servidor

- **Qué:** `main` acepta `--emit-openapi` antes de conectar PostgreSQL y el comando `npm run codegen:local` exporta el contrato temporal y ejecuta Orval con `tags-split` sin procesos persistentes. Se añadió `OPENAPI_INPUT` para usar el export local, se corrigieron referencias OpenAPI estables (`ErrorResponse`, `WorkspaceOverlayDocument`, JSON dinámico) y el wrapper PowerShell omite limpieza solo cuando el script opcional no existe.
- **Archivos:** `src/main.rs`, `src/models/workspace_overlay/mod.rs`, `src/handlers/articles.rs`, `src/handlers/auth.rs`, `src/handlers/notes.rs`, `src/handlers/preferences_handler.rs`, `src/handlers/workspace_handler.rs`, `src/handlers/workspace_overlay_handler.rs`, `frontend/orval.config.ts`, `scripts/codegen-local.mjs`, `scripts/emit-openapi.ps1`, `package.json`, `.gitignore`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Los clientes generados permanecen ignorados/regenerables; la especificación todavía no cubre todos los endpoints consumidos y el cliente manual no se retira hasta completar paridad. No se arrancó servidor ni BD.
- **Sentinel:** `task:check -- 018A-17 --fresh` y `self-check -- -TareaId 018A-17` PASS; Sentinel/VarSense sin errores, Rust y type-check frontend pasan. La primera ejecución encontró referencias huérfanas y un wrapper de limpieza inexistente, ambos corregidos en el bloque.
- **GLORY:** No aplica; el exportador es agnóstico y candidato a Glory si otro proyecto usa el mismo contrato Rust/utoipa.

## 018A-18 — Retiro del fallback JWT

- **Qué:** Se eliminó la autenticación Bearer/JWT del middleware, servicios, modelos, configuración, dependencia `jsonwebtoken`, secreto `.env` y fixtures. La sesión opaca `HttpOnly` es ahora la única autoridad y se añadió una prueba HTTP que confirma que un Bearer legacy devuelve `401`.
- **Archivos:** `src/middleware/auth.rs`, `src/services/auth.rs`, `src/models/user.rs`, `src/models/mod.rs`, `src/config/mod.rs`, `src/lib.rs`, `src/handlers/mod.rs`, fixtures de `src/handlers/{media_handler,preferences_handler,workspace_overlay_handler}.rs`, `Cargo.toml`, `Cargo.lock`, `.env.example`, `README.md`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** `/admin`, uploads públicos y CSS/contratos legacy permanecen para su propia matriz de paridad; no se eliminó ningún asset sin reemplazo.
- **Sentinel:** `task:check -- 018A-18 --fresh` y `self-check -- -TareaId 018A-18` PASS; Sentinel/VarSense sin errores, Rust 30 tests, clippy y type-check frontend pasan.
- **GLORY:** No aplica; el contrato de sesión de wandori.us usa capacidades y overlays específicos del producto.

## 018A-19 — Alinear OpenAPI con sesión opaca

- **Qué:** Swagger/utoipa y las anotaciones de endpoints protegidos usan `session_cookie` (`session_id` HttpOnly) en lugar de `bearer_auth`; se actualizaron los comentarios de middleware, README, `.env.example` y se eliminó el tipo frontend `AuthResponse` obsoleto.
- **Archivos:** `src/handlers/mod.rs`, `src/handlers/articles.rs`, `src/handlers/notes.rs`, `src/handlers/preferences_handler.rs`, `frontend/src/api/types.ts`, `README.md`, `.env.example`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** OpenAPI todavía no cubre todos los handlers del producto y el cliente manual se conserva hasta que exista paridad; `/admin` y `/uploads` no se tocaron.
- **Sentinel:** `npm run task:check -- 018A-19 --fresh` y `npm run self-check -- -TareaId 018A-19` deben quedar PASS; no se añadió una regla nueva.
- **GLORY:** No aplica; el contrato es específico del backend de wandori.us aunque la práctica de documentar sesiones opacas es reutilizable.

## 018A-20 — Paridad OpenAPI de artículos

- **Qué:** Se corrigieron las anotaciones utoipa de crear/obtener/actualizar/eliminar artículos admin para usar `/api/admin/articles`, se añadieron todos los handlers de artículos y sus DTOs a `ApiDoc`, y se marcaron los cuerpos TipTap JSON como objetos OpenAPI. `npm run codegen:local` ahora genera el cliente `frontend/src/api/generated/articles/articles.ts` sin referencias huérfanas.
- **Archivos:** `src/handlers/articles.rs`, `src/handlers/mod.rs`, `src/models/article.rs`, `roadmap.md`, `Agente/lecciones/lecciones-aprendidas.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** Proyectos, productos, media, analytics, settings y workspace aún necesitan anotaciones/paridad; no se migró todavía el servicio manual.
- **Sentinel:** `npm run task:check -- 018A-20 --fresh` y `npm run self-check -- -TareaId 018A-20` deben quedar PASS; codegen local pasó tras corregir `JsonValue`.
- **GLORY:** No aplica; el contrato de artículos es específico del producto.

## 018A-21 — Paridad OpenAPI de proyectos

- **Qué:** Se documentaron catálogo público y CRUD admin de proyectos con `session_cookie`, se registraron sus handlers/DTOs/enums en `ApiDoc` y `npm run codegen:local` genera `frontend/src/api/generated/projects/projects.ts` sin referencias faltantes.
- **Archivos:** `src/handlers/projects_handler.rs`, `src/handlers/mod.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Productos, media, analytics, settings, workspace y auth de sesión aún necesitan cobertura completa; el servicio manual no se retira todavía.
- **Sentinel:** `npm run task:check -- 018A-21 --fresh` y `npm run self-check -- -TareaId 018A-21` deben quedar PASS; codegen local pasó tras registrar `ProjectUrlUpdate`.
- **GLORY:** No aplica; el dominio de proyectos pertenece al producto.

## 018A-22 — Paridad OpenAPI de productos y checkout

- **Qué:** Se documentaron catálogo público, CRUD admin y checkout; la respuesta dinámica de Stripe ahora usa `CheckoutResponse` tipado (`checkout_url`, `order_id`, `session_id`) y Orval genera `frontend/src/api/generated/products/products.ts`.
- **Archivos:** `src/handlers/products_handler.rs`, `src/handlers/mod.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Media/uploads, analytics, settings y workspace aún no están en `ApiDoc`; el cliente manual no se retira hasta completar esa paridad.
- **Sentinel:** `npm run task:check -- 018A-22 --fresh` y `npm run self-check -- -TareaId 018A-22` deben quedar PASS; codegen local pasó con la respuesta tipada.
- **GLORY:** No aplica; checkout y Stripe son específicos de wandori.us.

## 018A-23 — Paridad OpenAPI de notificaciones, analytics y settings

- **Qué:** Se anotaron endpoints públicos, de cuenta y admin de notificaciones; ingesta/purga/estadísticas de analytics; y lectura/actualización de settings. `npm run codegen:local` genera los módulos `notifications/` y `settings-handler/` junto con los contratos de respuesta agregada.
- **Archivos:** `src/handlers/notifications.rs`, `src/handlers/settings_handler.rs`, `src/handlers/mod.rs`, `src/models/settings.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** El upload multipart/media, auth de sesiones adicionales y workspace todavía no están completos en `ApiDoc`; el servicio manual sigue vigente.
- **Sentinel:** `npm run task:check -- 018A-23 --fresh` y `npm run self-check -- -TareaId 018A-23` deben quedar PASS; codegen local pasó tras registrar `RecentEvent` y `TopArticle`.
- **GLORY:** No aplica; las políticas de métricas y notificaciones son específicas del producto.

## 018A-24 — Paridad OpenAPI de sesión y workspace

- **Qué:** Se añadieron a `ApiDoc` `me/logout/sessions/revoke` con `session_cookie`, se registró `Session` y `UserRole`, y se corrigieron las rutas OpenAPI de historial/publicación de workspace para incluir `/api`. Los árboles JSON se modelan como objetos para que Orval regenere sin `JsonValue` huérfano.
- **Archivos:** `src/handlers/auth.rs`, `src/handlers/workspace_handler.rs`, `src/handlers/mod.rs`, `src/services/session.rs`, `src/models/user.rs`, `src/models/workspace/mod.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Upload multipart/media, webhooks y MFA/passkey permanecen fuera; no se migró aún el cliente manual.
- **Sentinel:** `npm run task:check -- 018A-24 --fresh` y `npm run self-check -- -TareaId 018A-24` deben quedar PASS; codegen local pasó tras registrar `Session`, `UserRole` y objetos JSON.
- **GLORY:** No aplica; la sesión y el workspace son contratos específicos del producto.

## 018A-25 — Paridad OpenAPI de media

- **Qué:** Se anotaron Galería pública, Biblioteca admin, papelera, restore y upload; filtros públicos/admin son `IntoParams`, estados de asset y `Media` están en `ApiDoc`, y Orval genera `media-handler/`.
- **Archivos:** `src/handlers/media_handler.rs`, `src/models/media.rs`, `src/handlers/mod.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** El cuerpo multipart continúa manual para mantener validación de extensión/tamaño server-side; descargas, webhook Stripe y auth avanzada quedan fuera.
- **Sentinel:** `npm run task:check -- 018A-25 --fresh` y `npm run self-check -- -TareaId 018A-25` deben quedar PASS; codegen local pasó.
- **GLORY:** No aplica; media y su política de assets son específicas del producto.

## 018A-26 — Retirar ruta Admin legacy

- **Qué:** Se eliminó la ruta `/admin` del router y el export `renderAdmin` sin ventana. Admin continúa disponible como app interna singleton en `AppRegistry`, protegida por capacidad `admin`; el comando “Nuevo proyecto” abre `project-editor` con `openAppWindow` y no navega a una ruta legacy.
- **Archivos:** `frontend/src/main.ts`, `frontend/src/pages/admin.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/commands/toolbar-commands.ts`, `frontend/src/features/runtime/app-registration.test.ts`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** La página `admin.ts` no se elimina porque es el contenido del programa interno; uploads públicos, CSS legacy y contratos manuales requieren una matriz de paridad posterior.
- **Sentinel:** El gate debe confirmar que no quedan referencias a la ruta `/admin` en el frontend y que el registro mantiene `requires: 'admin'` sin `routePatterns`; no se añadió regla nueva.
- **GLORY:** No aplica; la composición del programa Admin es específica del producto.

## 018A-27 — Completar OpenAPI de comercio server-side

- **Qué:** Se documentaron el endpoint de descarga privada (`/api/downloads/{token}`) con grant opaco, contenido binario y errores de autorización, y el webhook Stripe (`/api/webhook/stripe`) con firma HMAC, cuerpo JSON y estados de error. Ambos handlers se registraron en `ApiDoc` sin exponer rutas de storage, tokens persistidos ni secretos.
- **Archivos:** `src/handlers/download_handler.rs`, `src/handlers/stripe_webhook.rs`, `src/handlers/mod.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** No se generó cliente frontend para estos endpoints server-to-server/descarga; el multipart de media sigue manual para conservar la validación de tipo y tamaño en el boundary.
- **Sentinel:** El gate debe comprobar que cada handler agregado a `ApiDoc` mantiene path con prefijo `/api` y que los schemas de error son estables; no se añadió regla nueva.
- **GLORY:** No aplica; los contratos de comercio y entrega pertenecen a wandori.us.

## 018A-28 — Sustituir serving público de uploads

- **Qué:** Se retiró `ServeDir` de `/uploads`. Media ahora se sirve mediante previews autorizados: público solo cuando el envelope está `active + public` y el asset `clean`; admin mediante `AdminUser` para assets activos. Los paths se resuelven con confinamiento canónico al storage y las respuestas de listado/upload exponen la URL del preview, no la storage key.
- **Archivos:** `src/handlers/media_handler.rs`, `src/handlers/mod.rs`, `src/repositories/media_repo.rs`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/lecciones/lecciones-aprendidas.md`.
- **Gotchas:** Se conserva temporalmente el nombre de campo `file_path` por compatibilidad del frontend, pero su valor ya es una URL de preview; el DTO público/admin separado queda como cleanup posterior. Assets estáticos del frontend (por ejemplo el perfil de marca) no dependen del storage privado del backend.
- **Sentinel:** El gate debe bloquear cualquier reintroducción de `nest_service("/uploads")` y exigir que los previews usen repository + confinamiento de path; no se añadió regla nueva.
- **GLORY:** No aplica; la política de exposición de media es específica del producto.

## 018A-29 — Separar DTOs de media
- **Qué:** Se separaron el modelo interno de storage y las respuestas HTTP. Público expone solo URL y metadata de presentación; admin/upload añaden IDs y estado necesarios para mutaciones. Ninguna respuesta serializa `file_path`. Los servicios de galería, perfil y biblioteca usan el contrato nuevo.
- **Archivos:** `src/models/media.rs`, `src/handlers/media_handler.rs`, `src/handlers/mod.rs`, `frontend/src/api/types.ts`, `frontend/src/services/media.service.ts`, `frontend/src/utils/upload.ts`, `frontend/src/pages/gallery.ts`, `frontend/src/features/settings/profile-settings.ts`, `frontend/src/features/desktop/apps/media-library/media-library.ts`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** `file_path` permanece únicamente en el modelo/repository interno y en servicios de descargas; `admin_url` permite previsualizar assets que no son públicos sin convertirlos en recursos públicos.
- **Sentinel:** `npm run task:check -- 018A-29 --fresh` y `npm run self-check -- -TareaId 018A-29`; no se añadió una regla nueva porque la separación se verifica por contrato OpenAPI y compilación.
- **GLORY:** No aplica; el contrato de media pertenece a wandori.us.

## 018A-30 — Alinear referencias del roadmap
- **Qué:** Se actualizaron las fuentes canónicas para retirar referencias al shape `Vec<Media>`, marcar el DTO de media como completado y ordenar el siguiente bloque técnico. Las pruebas visuales/E2E quedan explícitamente diferidas para evitar reabrir frentes cerrados.
- **Archivos:** `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`, `Agente/planes/plan-programas-editoriales-2026-07-31.md`.
- **Gotchas:** Es una corrección documental; no cambia runtime ni contratos.
- **Sentinel:** `npm run task:check -- 018A-30 --fresh` y `npm run self-check -- -TareaId 018A-30`; sin regla nueva.
- **GLORY:** No aplica.

## 018A-31 — Limpiar CSS huérfano confirmado
- **Qué:** Se eliminaron los bloques sin consumidores de `arrow-select` y `.font-panel`, junto con la referencia obsoleta en los tokens. Las clases dinámicas del launcher y de estados de media permanecen porque el runtime sí las genera.
- **Archivos:** `frontend/src/styles/layout.css`, `frontend/src/styles/pages.css`, `frontend/src/styles/variables.css`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** VarSense no puede inferir todos los nombres construidos dinámicamente; no se eliminó CSS usado por `createThemeToggleButton()` ni por badges de estado.
- **Sentinel:** `npm run task:check -- 018A-31 --fresh` y `npm run self-check -- -TareaId 018A-31`; sin regla nueva.
- **GLORY:** No aplica.

## 018A-32 — Mutator Orval y migración de MediaService
- **Qué:** Se añadió un mutator único para clientes Orval. Las peticiones generadas heredan cookie de sesión, CSRF, `VITE_API_URL` y respuestas con estado; `MediaService` dejó de llamar al cliente manual y consume las funciones generadas. CI regenera los clientes antes del quality gate.
- **Archivos:** `frontend/orval.config.ts`, `frontend/src/api/client.ts`, `frontend/src/api/generated-fetch.ts`, `frontend/src/api/client.test.ts`, `frontend/src/api/types.ts`, `frontend/src/services/media.service.ts`, `.github/workflows/quality.yml`, `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** Los clientes generados siguen ignorados por Git y se reconstruyen con `npm run codegen:local`; el resto de servicios conserva temporalmente el cliente manual hasta su bloque de migración.
- **Sentinel:** `npm run task:check -- 018A-32 --fresh` y `npm run self-check -- -TareaId 018A-32`; sin regla nueva.
- **GLORY:** No aplica; el adaptador de contrato pertenece al frontend de wandori.us.

## 018A-33 — Servicios editoriales con cliente generado
- **Qué:** `ArticleService`, `ProjectService` y `ProductService` dejaron de usar el cliente manual. CRUD, listados, alias, checkout y el header de idempotencia pasan por Orval y `unwrapGeneratedResponse`; `ProjectUrlUpdate` se traduce en el boundary sin duplicar lógica en editores.
- **Archivos:** `frontend/src/services/article.service.ts`, `frontend/src/services/project.service.ts`, `frontend/src/services/product.service.ts`, `roadmap.md`, plan maestro.
- **Gotchas:** el contrato de actualización de artículo exige `content`, por lo que el servicio normaliza el valor ausente a un objeto vacío; la suite completa sigue diferida por el timeout documentado del bloque anterior.
- **Sentinel:** type-check y gate de tarea; no se añadió regla nueva.
- **GLORY:** No aplica; el adaptador usa contratos específicos de wandori.us.

## 018A-34 — Servicios de cuenta y observabilidad con cliente generado
- **Qué:** Auth, preferencias, notificaciones, settings y analytics dejaron de llamar al cliente manual. Se conservaron la limpieza de sesión, la sincronización de preferencias, el consentimiento explícito de analytics y los estados HTTP del contrato generado.
- **Archivos:** `frontend/src/services/auth.service.ts`, `frontend/src/services/preferences.service.ts`, `frontend/src/services/notifications.service.ts`, `frontend/src/services/settings.service.ts`, `frontend/src/services/analytics.service.ts`, `roadmap.md`, plan maestro.
- **Gotchas:** `WorkspaceService` no se mezcló porque sus DTOs de release/overlay requieren una adaptación de tipos y un bloque propio; los servicios no convierten fallos de catálogo en éxitos silenciosos.
- **Sentinel:** type-check y gate de tarea; no se añadió regla nueva.
- **GLORY:** No aplica; las sincronizaciones y capacidades son específicas de wandori.us.

## 018A-35 — Workspace con cliente generado
- **Qué:** `WorkspaceService` dejó de usar `api`. Releases, publicación y overlay usan funciones Orval; las conversiones `toReleaseInfo`/`toOverlayResponse` mantienen el modelo rico del runtime en el boundary y convierten únicamente el 404 de release activo en `null`.
- **Archivos:** `frontend/src/services/workspace.service.ts`, `roadmap.md`, plan maestro.
- **Gotchas:** los esquemas OpenAPI representan árboles y overlays como mapas genéricos; las aserciones quedan centralizadas en dos funciones pequeñas, no repartidas por consumidores.
- **Sentinel:** type-check y gate de tarea; no se añadió regla nueva.
- **GLORY:** No aplica; el modelo de workspace es específico de wandori.us.

## 018A-36 — Retirar cliente manual frontend
- **Qué:** Se eliminó el objeto `api` y el request genérico manual. `client.ts` conserva únicamente `ApiError`, `generatedFetcher`, la sesión cookie/CSRF y `unwrapGeneratedResponse`; no quedan consumidores de métodos HTTP manuales.
- **Archivos:** `frontend/src/api/client.ts`, `roadmap.md`, plan maestro.
- **Gotchas:** `ApiError` sigue siendo compartido por sincronización de preferencias/overlay; no se eliminó el módulo completo ni se duplicó el manejo de 401.
- **Sentinel:** type-check, tests de transporte/auth y gate de tarea; no se añadió regla nueva.
- **GLORY:** No aplica; el boundary es específico de la aplicación frontend.

## 018A-37 — Tests frontend incrementales y escritura atómica Windows
- **Qué:** `test:changed` ahora construye un grafo local de imports y ejecuta solo los tests dependientes del código modificado; cambios de configuración, borrados y renombres conservan `test:full`. `writeAtomic` reintenta carreras `EPERM/EBUSY/EEXIST` en Windows.
- **Archivos:** `scripts/quality/frontend-test-selection.mjs`, `scripts/quality/run-frontend-tests.mjs`, `scripts/quality/atomic-file.mjs`, `scripts/quality/tests/frontend-test-selection.test.mjs`, `roadmap-sentinel.md`, `roadmap.md`, plan maestro.
- **Gotchas:** aliases no relativos no se adivinan y siguen cubiertos por type-check o suite completa explícita; un cambio sin test dependiente informa la omisión en vez de consumir todos los workers.
- **Sentinel:** `npm run quality:test` 28/28 PASS; pruebas de selector 3/3 PASS; dry-run real seleccionó dependencias de `client.ts`.
- **GLORY:** No aplica; el selector y la escritura atómica son tooling reusable del orquestador.

## 018A-38 — Contrato Article separado por ISP
- **Qué:** `Article` conserva su API pública, pero compone `ArticleIdentity`, `ArticleContent` y `ArticlePublication`; cada interfaz expresa una sola responsabilidad y queda bajo el límite de campos de Sentinel.
- **Archivos:** `frontend/src/api/types.ts`, `roadmap.md`, plan maestro.
- **Gotchas:** no se alteraron nombres, valores ni serialización; únicamente se sustituyó una interfaz grande por composición TypeScript.
- **Sentinel:** type-check y gate de tarea; el warning `large-interface-isp` queda resuelto en el archivo modificado.
- **GLORY:** No aplica; el DTO pertenece al contrato frontend específico del proyecto.

## 018A-39 — Acción y app de Propiedades
- **Qué:** Se materializó `resource:properties` para todos los recursos declarados por el registry y se registró la app reutilizable `properties`. La ventana muestra nombre, tipo, ubicación, estado y origen del nodo desde el workspace local; no consulta red ni expone `refId` interno.
- **Archivos:** `frontend/src/features/desktop/apps/properties/properties-preview.ts`, `frontend/src/styles/desktop/desktop-properties.css`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/commands/resource-commands.ts`, `frontend/src/features/runtime/commands/resource-commands.test.ts`, `frontend/src/main.ts`, `Agente/documentacion/arquitectura/matriz-paridad-admin-2026-07-31.md`, `roadmap.md`, plan maestro, plan editorial.
- **Gotchas:** La vista depende de un `nodeId` de sesión y falla cerrado con mensaje visible si el nodo ya no existe. Metadatos remotos quedan fuera hasta que exista un DTO público específico.
- **Sentinel:** `resource:properties` reutiliza el registry y la frontera `openAppWindow`; gate y self-check deben confirmar que ninguna acción declarada queda sin ejecutor.
- **GLORY:** No aplica; la vista y el comando dependen del workspace de wandori.us.

## 018A-40 — Limpieza CSS huérfana segura
- **Qué:** Se retiraron tres utilidades CSS (`gap-sm`, `mt-md`, `mb-lg`) que no tenían consumidores en fuentes, estilos ni tests. Las clases dinámicas de badges de media, tabs y tema móvil se conservaron con evidencia de uso.
- **Archivos:** `frontend/src/styles/components.css`, `roadmap.md`, plan maestro.
- **Gotchas:** VarSense sigue reportando falsos positivos para clases construidas dinámicamente; no se eliminaron sin verificar sus consumidores.
- **Sentinel:** La búsqueda estática y el gate validan que no se reintroduzcan las utilidades retiradas; no se añadió una regla nueva.
- **GLORY:** No aplica; es limpieza CSS específica del frontend de wandori.us.

## 018A-41 — Separar registro público y administrativo
- **Qué:** `app-registration.ts` conserva el catálogo público y carga el registro administrativo desde `app-registration-admin.ts`. Se mantienen IDs, rutas, capacidades, lazy loading, acciones y lifecycle; ambos módulos quedan bajo el límite de 300 líneas.
- **Archivos:** `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/app-registration-admin.ts`, `roadmap.md`, plan maestro, plan de refactorización.
- **Gotchas:** El registro administrativo se importa como módulo de efectos laterales antes de inicializar el registry; no se duplicaron entradas ni se añadió un segundo catálogo.
- **Sentinel:** El gate comprueba límites de archivo, type-check y tests de AppRegistry; no se añadió suppression.
- **GLORY:** No aplica; el catálogo es específico del runtime de wandori.us.

## 018A-42 — Worker de entrega commerce/outbox
- **Qué:** El webhook de Stripe ya no llama a Resend dentro de la petición. Se añadió un worker de ejecución única (`--process-commerce-outbox`) con claim `FOR UPDATE SKIP LOCKED`, backoff acotado, `processed_at` posterior al envío y rotación del hash del grant para reintentos. Los tokens raw viven solo en memoria.
- **Archivos:** `src/services/commerce_outbox.rs`, `src/repositories/commerce_repo.rs`, `src/handlers/stripe_webhook.rs`, `src/services/mod.rs`, `src/main.rs`, plan commerce, `roadmap.md`, plan maestro.
- **Gotchas:** El scheduler de producción y secretos reales de Resend/Stripe no se activan; el comando es deliberadamente acotado para Coolify/cron futuro. Un evento desconocido se consume con warning para no bloquear la cola.
- **Sentinel:** `cargo fmt`, `npm test` (34/34), `npm run check:back`; quality gate y self-check de 018A-42 deben confirmar que no se guardan tokens raw ni se ejecuta correo dentro del webhook.
- **GLORY:** No aplica; el flujo de commerce es específico de wandori.us.

## 018A-43 — Cerrar alcance mínimo de Sentinel/VarSense
- **Qué:** Se actualizó el estado canónico del roadmap de calidad y se marcaron como diferidos los backlog de reglas, benchmarks, paridad avanzada y publicación upstream que no son necesarios para continuar el roadmap principal. Prevención refleja los contratos de media, storage y commerce ya cerrados.
- **Archivos:** `roadmap-sentinel.md`, `roadmap.md`, `Agente/planes/plan-mejora-quality-tool-2026-07-30.md`, `Agente/prevencion/prevencion-wandori-sentinel-varsense-2026-07-29.md`.
- **Gotchas:** Las casillas abiertas de las fases globales siguen siendo trabajo futuro; no representan un bloqueo. El gate mínimo se mantiene obligatorio en cada tarea.
- **Sentinel:** `npm run quality:test` (28/28), `npm run task:check -- 018A-42 --fresh` y `npm run self-check -- -TareaId 018A-42` pasan; no se añadió suppression ni regla local.
- **GLORY:** No aplica; se ajustó documentación y política del tooling del proyecto.

## 018A-44 — Retirar alias legacy de Configuración
- **Qué:** Se eliminó `font-panel.ts`, que ya no contenía lógica de fuentes y solo delegaba al panel de Perfil. La app Configuración conserva su registro, lazy loading, capacidades y contenido; ahora usa `settings-panel.ts`/`createSettingsPanel` con un nombre coherente.
- **Archivos:** `frontend/src/features/settings/settings-panel.ts`, eliminación de `frontend/src/features/settings/font-panel.ts`, `frontend/src/features/runtime/app-registration-admin.ts`, `frontend/src/pages/admin.ts`, matriz de paridad, plan de Configuración, plan de refactorización, `roadmap.md` y plan maestro.
- **Gotchas:** El tab de compatibilidad de Configuración no se eliminó; únicamente se retiró el nombre que sugería que aún existía un panel de fuentes. La evolución de Configuración sigue diferida por decisión del usuario.
- **Sentinel:** TypeScript PASS; Vitest dirigido 10/10; `task:check` y `self-check` del bloque deben confirmar imports, límites y documentación sin suppression.
- **GLORY:** No aplica; es limpieza específica del frontend de wandori.us.

## 018A-45 — Limpiar tokens CSS sin consumidores
- **Qué:** Se eliminaron siete variables CSS legacy que VarSense reportaba como no usadas y una búsqueda global confirmó que solo estaban definidas en `variables.css`. Se conservó el token de contenido y todos los tokens activos del chrome.
- **Archivos:** `frontend/src/styles/variables.css`, `roadmap.md`, plan maestro y completados/lecciones.
- **Gotchas:** No se eliminaron tokens dinámicos de perfil, tema o z-index del OS; la limpieza se limitó a nombres sin consumidores reales.
- **Sentinel:** VarSense, type-check, gate y self-check del bloque deben confirmar ausencia de referencias y cero errores bloqueantes.
- **GLORY:** No aplica; son tokens visuales específicos del proyecto.

## 018A-46 — Separar contratos públicos y privados de productos
- **Qué:** Se añadieron `ProductAdminResponse` y `ProductPublicResponse` y los handlers dejaron de serializar el modelo SQL interno. Catálogo y artículos no exponen `download_path`, `stripe_product_id` ni `stripe_price_id`; el webhook/outbox y checkout siguen usando `Product` internamente.
- **Archivos:** `src/models/product.rs`, `src/handlers/products_handler.rs`, `src/handlers/mod.rs`, plan commerce, prevención, roadmap y plan maestro.
- **Gotchas:** El contrato admin y público conservan los campos que ya consume el frontend (`id`, artículo, nombre, descripción, precio, moneda, activo y fecha); no se rompe la UI ni se mezclan DTOs con repositorios.
- **Sentinel:** `cargo fmt`, `npm run check:back`, `npm run codegen:local`, tests Rust de serialización, gate y self-check del bloque.
- **GLORY:** No aplica; el contrato de productos es específico de wandori.us.

## 018A-47 — Restringir configuración pública
- **Qué:** `GET /api/settings` ahora consulta una allowlist explícita de claves de presentación. Se mantiene `get_all` únicamente para el boundary interno de autenticación y el frontend usa `SettingsService.getPublic()` para evitar que el nombre sugiera acceso a configuración administrativa.
- **Archivos:** `src/repositories/settings_repo.rs`, `src/services/settings_svc.rs`, `src/handlers/settings_handler.rs`, `frontend/src/services/settings.service.ts`, consumidores de settings, roadmap, prevención, plan maestro y lecciones.
- **Gotchas:** `registration_enabled` y futuras claves admin no aparecen en la respuesta pública; `about_content` y preferencias visuales actuales sí permanecen disponibles para las páginas públicas.
- **Sentinel:** `cargo fmt`, `npm run check:back`, type-check, test unitario de allowlist, gate y self-check del bloque.
- **GLORY:** No aplica; es el boundary específico de configuración de wandori.us.

## 018A-48 — Separar contratos públicos y administrativos de proyectos
- **Qué:** `Project` deja de serializarse directamente. El catálogo público usa `ProjectPublicResponse` sin `sort_order` ni `is_visible`; las rutas admin usan `ProjectAdminResponse` y conservan los metadatos necesarios para editar y publicar. El servicio frontend y la página pública consumen el contrato correcto y el cliente Orval se regenera.
- **Archivos:** `src/models/project.rs`, `src/handlers/projects_handler.rs`, `src/handlers/mod.rs`, `src/repositories/settings_repo.rs`, `frontend/src/api/types.ts`, `frontend/src/services/project.service.ts`, `frontend/src/pages/projects.ts`, roadmap, plan maestro, lecciones y completados.
- **Gotchas:** El endpoint público ya devuelve proyectos filtrados/ordenados por repository; quitar el filtrado duplicado del navegador evita que el cliente vuelva a depender de metadatos internos. La consulta de settings usa query dinámica para mantener el exportador OpenAPI sin BD viva.
- **Sentinel:** `cargo fmt`, `npm run codegen:local`, `npm run check:back`, `npm test`, type-check y gate del bloque; no se añadió suppression ni regla local.
- **GLORY:** No aplica; el boundary de proyectos es específico de wandori.us.

## 018A-49 — Separar contratos de notificaciones por capacidad
- **Qué:** El modelo SQL `Notification` dejó de serializarse directamente. Visitantes reciben `NotificationPublicResponse` sin `status` ni `created_by`; cuentas reciben `NotificationAccountResponse` con su lectura resuelta; el panel admin usa `NotificationAdminResponse` y conserva la información editorial completa. Las listas y OpenAPI quedaron tipadas por boundary.
- **Archivos:** `src/models/notification.rs`, `src/models/mod.rs`, `src/services/notification_svc.rs`, `src/handlers/notifications.rs`, `src/handlers/mod.rs`, `frontend/src/services/notifications.service.ts`, `frontend/src/features/notifications/notifications-admin.ts`, roadmap, plan maestro, lecciones y completados.
- **Gotchas:** El frontend mantiene un tipo base con campos admin opcionales para poder fusionar el listado público con las lecturas de cuenta; el panel admin usa una variante estricta y no pierde el estado editorial.
- **Sentinel:** `cargo fmt`, `npm run codegen:local`, `npm run check:back`, type-check, test de serialización pública, gate y self-check; sin suppressions.
- **GLORY:** No aplica; el contrato de novedades es específico de wandori.us.

## 018A-50 — Cerrar paridad automatizable y diferir deuda visual
- **Qué:** Se verificó la matriz de boundaries públicos/admin de media, artículos, productos, proyectos, notificaciones y settings. La parte automatizable queda cerrada; CSS/clases huérfanas y la retirada amplia de fachadas TypeScript manuales quedan explícitamente diferidas porque requieren revisión visual o migración de mayor alcance.
- **Archivos:** `roadmap.md`, plan maestro, completados y lecciones.
- **Gotchas:** No se marca como hecha la limpieza visual pendiente; el siguiente bloque habilitado pasa a 297A-17 sin saltarse una dependencia técnica.
- **Sentinel:** `task:check` documental y `self-check` confirman que el gate mínimo sigue activo; no se añadieron suppressions.
- **GLORY:** No aplica; es la planificación del proyecto.

## 018A-51 — Suite frontend completa únicamente en CI
- **Qué:** El adaptador frontend del quality gate conserva `type-check` como validación local y, cuando recibe `--ci`, ejecuta además `npm --prefix frontend run test:full`. Ambas salidas se combinan en un único log/reporte; la suite CI tiene timeout propio de 300 s para no confundir runners lentos con fallos.
- **Archivos:** `scripts/quality/preflight.mjs`, `scripts/quality/adapters/frontend.mjs`, roadmap, plan maestro, completados y lecciones.
- **Gotchas:** No se activa `test:full` para agentes locales, evitando que cada tarea levante todos los workers/tests; CI sí obtiene la cobertura completa antes de publicar el reporte.
- **Sentinel:** `quality:test` 28/28, gate local y gate CI del bloque, self-check; no se añadió suppression.
- **GLORY:** No aplica; es una mejora del orquestador específico del proyecto.

## 018A-52 — Aislar caché local y CI
- **Qué:** La huella del cache ahora incluye el modo `local`/`ci` y sube la versión de formato. Un resultado de `test:full` en CI ya no puede satisfacer un gate local que solo ejecuta type-check; se añadió una regresión específica.
- **Archivos:** `scripts/quality/cache.mjs`, `scripts/quality/tests/cache.test.mjs`, roadmap, plan maestro, completados y lecciones.
- **Gotchas:** Los caches anteriores quedan inválidos por la versión 3 y se regeneran de forma segura; no se borra ningún artefacto de trabajo.
- **Sentinel:** `quality:test` 29/29, gate CI/local y self-check; no se añadió suppression.
- **GLORY:** No aplica; es una mejora del orquestador específico del proyecto.

## 018A-53 — Automatizar budgets de rendimiento en CI
- **Qué:** El adaptador frontend mantiene el flujo local ligero y, en modo CI, ejecuta type-check, suite completa, build y un verificador gzip. Los límites configurables cubren entry JS (65 KB), entry CSS (16 KB) y chunk JS mayor (120 KB); los assets faltantes o excedentes bloquean el gate.
- **Archivos:** `scripts/quality/performance-budget.mjs`, `scripts/quality/adapters/frontend.mjs`, `scripts/quality/preflight.mjs`, tests del quality gate, `quality.config.json`, `roadmap.md`, plan maestro y lecciones.
- **Gotchas:** El build/budget no se ejecuta en la ruta local para evitar consumo innecesario; revisión visual, E2E y observabilidad real siguen diferidas. El presupuesto mide gzip de artefactos, no tamaños fuente.
- **Sentinel:** `quality:test`, gate CI/local y self-check; sin suppression ni regla específica del producto.
- **GLORY:** No aplica; es una mejora del orquestador del proyecto.

## 018A-54 — Documentar runbook operativo sin SSH
- **Qué:** Se documentó el preflight y el flujo seguro de Coolify Manager para deploy, health, backup/restore y rollback, con señales de salida y límites claros. No se ejecutó producción ni se habilitó un canal SSH.
- **Archivos:** `Agente/documentacion/herramientas/runbook-coolify-backup-rollback-2026-08-01.md`, índice, roadmap, plan maestro y lecciones.
- **Gotchas:** El runbook no afirma que exista evidencia operativa hasta probarlo en un entorno autorizado; E2E, proveedores reales, observabilidad y deploy permanecen pendientes.
- **Sentinel:** `task:check` y `self-check` documentales; sin suppression.
- **GLORY:** No aplica; es procedimiento específico de este proyecto y su despliegue Coolify.

## 018A-55 — Sincronizar resumen del quality gate
- **Qué:** El resumen canónico del roadmap ahora refleja los 31 tests del orquestador y distingue la evidencia local incremental de suite/build/budgets completa en CI.
- **Archivos:** `roadmap.md`.
- **Gotchas:** No cambia reglas ni ejecución; corrige únicamente una cifra y el estado resumido después de 018A-53.
- **Sentinel:** Gate documental y self-check; sin suppression.
- **GLORY:** No aplica.

## 018A-56 — Completar referencia del ajuste documental
- **Qué:** Se añadió la referencia `[018A-55]` en las dos listas canónicas del roadmap para que el quality gate pueda verificar la sincronización documental.
- **Archivos:** `roadmap.md`.
- **Gotchas:** Es una corrección posterior al primer commit de 018A-55; no modifica comportamiento ni reglas.
- **Sentinel:** Gate documental y self-check; sin suppression.
- **GLORY:** No aplica.

## 018A-57 — Integrar registro y recuperación en Cuenta
- **Qué:** La app Cuenta ahora permite alternar entre entrar, crear cuenta y solicitar recuperación. Valida campos, confirma contraseñas, muestra feedback no enumerable y usa `AuthService`; el backend conserva el flag de registro apagado y no se manipulan tokens en el cliente.
- **Archivos:** `frontend/src/features/runtime/account-view.ts`, `frontend/src/features/runtime/account-view.test.ts`, `frontend/src/styles/pages.css`, plan de auth, plan maestro y `roadmap.md`.
- **Gotchas:** La UI de consumo de tokens, Resend/SMTP, MFA y E2E siguen pendientes; no se habilita registro público solo por mostrar el formulario.
- **Sentinel:** Type-check y `account-view.test.ts` 9/9; gate y self-check del bloque; sin suppression.
- **GLORY:** No aplica; es la app de identidad específica de wandori.us.

## 018A-58 — Sincronizar alcance documental de autenticación
- **Qué:** Se corrigieron las salidas del roadmap y plan maestro para distinguir formularios internos ya implementados de la UI de tokens, correo real, MFA y E2E que siguen pendientes.
- **Archivos:** `roadmap.md`, `Agente/planes/plan-escritorio-persistente-cuentas-admin-apps-2026-07-29.md`.
- **Gotchas:** No cambia rutas, permisos ni activación de registro; evita que la documentación prometa verificación de correo desde la UI.
- **Sentinel:** Gate documental y self-check; sin suppression.
- **GLORY:** No aplica.

## 018A-59 — Separar rate limits de autenticación
- **Qué:** Registro, solicitud de recuperación y confirmación de recuperación ahora usan un almacén propio por IP con 3 intentos cada 5 minutos. Login conserva su bucket independiente de 5 intentos por minuto; los buckets limpian expiraciones sin contaminarse.
- **Archivos:** `src/handlers/auth.rs`, `src/lib.rs`, `src/handlers/mod.rs`, fixtures de `AppState` en handlers, plan de auth, plan maestro y `roadmap.md`.
- **Gotchas:** El límite es local al proceso; un rate limit distribuido y la auditoría específica de cada acción requieren infraestructura/decisión posterior. El feature flag de registro continúa apagado.
- **Sentinel:** `cargo fmt`, tests Rust de auth, gate y self-check; sin suppression.
- **GLORY:** No aplica; es seguridad específica de wandori.us.

## 018A-60 — Auditar acciones sensibles de autenticación
- **Qué:** Registro, verificación y recuperación escriben eventos server-side con resultado, tipo, usuario solo cuando corresponde e IP hasheada. Los tokens, contraseñas y emails no entran en la auditoría; los errores de persistencia no se silencian.
- **Archivos:** `src/handlers/auth.rs`, plan de auth, plan maestro, `roadmap.md` y lecciones.
- **Gotchas:** La auditoría usa la tabla existente y sigue siendo local; la retención/consulta administrativa y rate limit distribuido quedan para otra fase.
- **Sentinel:** `cargo fmt`, tests Rust, gate y self-check; sin suppression.
- **GLORY:** No aplica; es seguridad específica de wandori.us.

## 018A-61 — Unificar comandos de navegación y geometría
- **Qué:** El taskbar y el launcher móvil delegan el toggle de navegación en `navigation:toggle-external-nav`; titlebar, doble clic y toolbar delegan maximizar/restaurar en `window:maximize`; `window:reframe-all` reencuadra todas las ventanas en una sola mutación y el `ResizeObserver` lo usa al cambiar el workspace. Se añadieron eventos tipados para navegación, maximización y reencuadre.
- **Archivos:** `frontend/src/features/runtime/commands/navigation-commands.ts`, `frontend/src/features/runtime/commands/window-commands.ts`, `frontend/src/features/runtime/commands/window-commands.test.ts`, `frontend/src/features/runtime/window-manager.ts`, `frontend/src/features/desktop/reactive-taskbar.ts`, `frontend/src/features/desktop/desktop-shell.ts`, `frontend/src/features/desktop/utils/drag-resize.ts`, `frontend/src/main.ts`, `frontend/src/features/analytics/dispatcher.ts`, plan de contratos, roadmap y lecciones.
- **Gotchas:** El ID canónico usa `namespace:verb` (`navigation:toggle-external-nav`, `window:reframe-all`); el nombre con puntos del plan se conserva solo como alias documental. La validación visual/E2E de viewports sigue siendo pendiente de 297A-17.
- **Sentinel:** quality gate, type-check y tests frontend del bloque; sin suppression ni regla específica nueva.
- **GLORY:** No aplica; el contrato de comandos es específico del runtime de wandori.us.

## 018A-62 — Fix migración notificaciones: ON CONFLICT contra índice parcial (42P10)
- **Qué:** La migración `20260801060000_297a21_notifications` fallaba con `42P10: no hay restricción única o de exclusión que coincida con la especificación ON CONFLICT` en el seed de la release publicada. El índice parcial `notifications_release_version_uq ... WHERE release_version IS NOT NULL` exige repetir su predicado en el arbiter: `ON CONFLICT (release_version) WHERE release_version IS NOT NULL DO NOTHING`. Corregido en sitio — la migración falla en cualquier entorno, por lo que no hay drift de checksum.
- **Archivos:** `migrations/20260801060000_297a21_notifications.up.sql`.
- **Gotchas:** Un `ON CONFLICT` contra un índice parcial necesita el `WHERE` exacto del índice; omitirlo produce 42P10 aunque la columna esté única de facto.
- **Sentinel:** no aplica (SQL no cubierto por Sentinel).
- **GLORY:** No aplica.

## 018A-63 — Login: contrato 204 con cookie (el frontend no mostraba falso error)
- **Qué:** `login` responde 204 con la sesión en cookie `Set-Cookie`, pero utoipa declaraba 200 y el cliente generado tipaba `loginResponse200`, por lo que `unwrapGeneratedResponse` lanzaba `ApiError` ante el 204 y el frontend mostraba "credenciales incorrectas" pese al login exitoso. Se alineó la anotación utoipa a 204 (consistente con logout/resetPassword) y `AuthService.login()` acepta `[204]`; la capacidad se confirma en `/me`. Verificado en navegador: login como admin y sesión activa.
- **Archivos:** `src/handlers/auth.rs`, `frontend/src/services/auth.service.ts`.
- **Gotchas:** Los desajustes entre anotación utoipa, cliente generado y servicio manual causan fallos silenciosos del frontend pese a HTTP correcto. Pendiente: regenerar el cliente Orval (`npm run codegen:local`) tras compilar el backend para que `loginResponse200` desaparezca del tipo generado.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 018A-64 — UI: sin subrayado en botones y etiquetas de conflicto simplificadas
- **Qué:** Se eliminó `text-decoration: underline` de todos los botones del OS (conflictos de overlay/preferencias, selector de tema, botones de cuenta y menú de la barra); los enlaces de redes siguen subrayados (convención de links, no botones). Las etiquetas de los diálogos de conflicto se simplificaron a "conservar lo de este dispositivo" / "usar lo de mi cuenta" (paralelas y sin jerga de dominio), tanto en overlay (`overlay-conflict-ui.ts`) como en preferencias (`preferences-panel.ts`). Tests actualizados.
- **Archivos:** `frontend/src/features/runtime/workspace/overlay-conflict-ui.ts`, `frontend/src/features/runtime/preferences-panel.ts`, `frontend/src/styles/components.css`, `frontend/src/styles/desktop/desktop-menu.css`, `frontend/src/styles/pages.css`, `frontend/src/features/runtime/preferences-panel.test.ts`, `frontend/src/features/runtime/workspace/overlay-sync.test.ts`.
- **Gotchas:** El subrayado de `.desktop-menu-bar__entry--open` era el único indicador de "menú abierto"; se retiró igualmente (el desplegable es la señal) por decisión del usuario de no subrayar ningún botón. Verificado por computed styles en navegador: 0 botones subrayados, links intactos. Lints Sentinel pre-existentes (especificación visual local en `components.css`/`desktop-menu.css`, límite de 600 líneas) quedan como deuda del sistema de diseño, no causados por este bloque.
- **Sentinel:** no aplica (cambio de diseño; los lints pre-existentes quedan registrados como deuda).
- **GLORY:** No aplica.

## 018A-65 — Receta compartida para botones OS con icono+texto
- **Qué:** Los botones de la app de cuenta (`cerrar sesión`, `entrar`, `registrar`, `recuperar acceso`) mostraban el SVG desalineado: la regla de superficie `.desktop-window .boton { display: inline-block }` (0,2,0) sobreescribía el `display: inline-flex; align-items: center; gap` de `.account-app__action` (0,1,0), dejando icono y texto inline con alineación por baseline. Se creó la receta compartida `.boton-con-icono` en `components.css` (misma especificidad de superficie, definida después) que restaura `display: inline-flex; align-items: center; gap: var(--espacio-sm)`; `account-app` la consume en submit y acciones, y se limpió el flex muerto de `pages.css` (quedó solo `align-self`). Además el icono se redujo 1px vía `calc(var(--espacio-md) - 1px)` (16→15px) por petición del usuario.
- **Archivos:** `frontend/src/styles/components.css`, `frontend/src/styles/pages.css`, `frontend/src/features/runtime/account-view.ts`.
- **Gotchas:** La receta debe definirse DESPUÉS de la regla de superficie y con los mismos selectores de superficie para ganar por orden de fuente. Verificado en navegador: `display: flex`, offsets de icono y texto = 0, tamaño 15×15. El diálogo de conflicto de overlay reaparece al recargar la página (comportamiento esperado de sync, no relacionado); el usuario decide qué estado conservar. Gate `task:check 018A-65` PASS (sentinel 0 errores, varsense 0 errores, type-check OK); tests 25/25.
- **Sentinel:** no aplica (receta de diseño compartida). Lints pre-existentes de `components.css` (límite 600 líneas, especificaciones locales) siguen como deuda registrada.
- **GLORY:** la receta `.boton-con-icono` es agnóstica y candidata a moverse a glory-rs si se generaliza fuera de wandori.us.

## 018A-66 — Separar overlay personal de la sesión admin
- **Qué:** La sincronización de `user_workspace_overlays` ahora corta fail-closed para `capability: 'admin'`; el admin conserva el overlay local para publicar el release global. La UI también cierra cualquier modal stale durante una transición de autenticación y se añadió una regresión que confirma que no hay request ni estado `conflict` admin.
- **Archivos:** `frontend/src/features/runtime/workspace/overlay-sync.ts`, `frontend/src/features/runtime/workspace/overlay-conflict-ui.ts`, `frontend/src/features/runtime/workspace/overlay-sync.test.ts`, `roadmap.md`.
- **Gotchas:** No se borra el overlay local: sigue siendo la superficie de edición que `workspace:publish` convierte en release; solo se desactiva el transporte personal por cuenta para admin. La comprobación visual después de recargar queda pendiente.
- **Sentinel:** regresión frontend y quality gate; sin suppression ni regla nueva.
- **GLORY:** No aplica; la separación admin/overlay es específica del runtime de wandori.us.

## 018A-67 — Coherencia visual de botones con iconos (media y analytics)
- **Qué:** Los botones de acción del ítem de la biblioteca de media (Copiar URL / Restaurar / Eliminar) pasaron de `.boton boton-pequeno` + SVG Lucide 24px a la receta canónica `.boton-icono` (caja 20px, SVG 14px del token, sin borde, coherente con las toolbars del editor). El botón Exportar de Estadísticas usaba `.boton` a secas con icono+texto y la superficie inline-block rompía la línea (mismo bug que "subir archivo"): pasó a `boton boton-con-icono` (flex + SVG 14px del token). Se barrió todo el código en busca de botones `.boton` con SVG crudo: `comercio__comprar` y `notificaciones__recargar` ya tenían su regla local con token (no rotos) y los botones de admin legacy son solo texto.
- **Archivos:** `frontend/src/features/desktop/apps/media-library/media-library.ts`, `frontend/src/features/analytics/analytics-panel.ts`, `frontend/src/styles/components.css`.
- **Gotchas:** La regla `> svg` de la receta `.boton-con-icono` solo afecta al hijo directo: los consumidores que envuelven el icono en un span (account-app) dimensionan su envoltorio y no se ven afectados. `Restaurar` no se pudo verificar visualmente (papelera vacía), pero es el mismo cambio de clase que los otros dos. Durante el cierre el type-check falló por un cliente Orval stale (`loginResponse200` en disco vs 204 en el contrato Rust commiteado): `npm run codegen:local` regeneró `auth.ts` a 204 y el gate pasó. Los generados están en `.gitignore` (no se commitean).
- **Sentinel:** no aplica (receta de diseño). Warnings pre-existentes registrados (dom-access, límite 600 líneas, specs locales de notificaciones).
- **GLORY:** no aplica; los consumidores son específicos del OS y la receta ya vive en `components.css` del proyecto (candidata a glory-rs si se generaliza).

## 018A-68 — Receta general de toolbar de contenido + controles segmentados
- **Qué:** Se estableció la regla de diseño general para toolbars de contenido: el contenedor usa la receta compartida `.barra-herramientas` (flex, gap-md, borde inferior) y los filtros/modos de vista usan `.control-segmentado` (grupo de opciones pequeñas con borde 1px y el activo invertido, Mac clásico). Prohibido `.campo`/`.campo-select` y `.boton` con borde de superficie dentro de una toolbar. La biblioteca de media se migró: el filtro `tipo` (todos/imágenes/audio/video) y la vista `biblioteca/papelera` ahora son controles segmentados con `createSegmentedControl` (componente compartido en `components/ui/`, estado activo gestionado, `aria-pressed`, `role=group`); se retiró `createSelect` y el toggle de papelera que cambiaba su etiqueta.
- **Archivos:** `frontend/src/components/ui/segmented-control.ts` (nuevo), `frontend/src/features/desktop/apps/media-library/media-library.ts`, `frontend/src/styles/components.css`, `frontend/src/styles/desktop/desktop-media-library.css`, `Agente/documentacion/design-system/manual-identidad-visual-os-2026-07-29.md`.
- **Gotchas:** El activo del segmentado se gestiona dentro del componente (no depende del re-render del padre); `createEl` no soporta `dataset`, así que el valor de la opción se pasa como `data-value`. Verificado en navegador: grupo flex con borde 1px, activo invertido (negro/blanco), `aria-pressed` correcto, papelera/biblioteca alternan estado y listado.
- **Sentinel:** no aplica (receta de diseño). Warning pre-existente `limite-lineas` de `components.css` (ahora 664 líneas) queda como deuda registrada; dividir el archivo es refactor pendiente.
- **GLORY:** la receta `.barra-herramientas`/`.control-segmentado` y el componente `createSegmentedControl` son agnósticos y candidatos a glory-rs si se generalizan fuera de wandori.us.

## 018A-69 — Restauración de ventanas conserva acciones
- **Qué:** `openRestoredWindow()` ahora copia `MountedView.actions` al `WindowEntry`, igual que `openWindow()`. Así el shell vuelve a montar `.desktop-window__actions` cuando una app se reconstruye después de recargar. Se añadió una app fixture con acciones y una regresión de restauración.
- **Archivos:** `frontend/src/features/runtime/window-manager.ts`, `frontend/src/features/runtime/window-session-restore.test.ts`, `Agente/planes/plan-persistencia-sesion-ventanas-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** Las acciones no se persisten como DOM; se derivan al reinstanciar `MountedView`. La comprobación visual en el navegador del proyecto queda pendiente porque la pestaña disponible resolvió otra aplicación local.
- **Sentinel:** pendiente ejecutar el gate del bloque; no se añadió suppression ni regla nueva.
- **GLORY:** no aplica; es la integración específica del shell de wandori.us.

## 018A-71 — Controles de vista en el app toolbar real (grupo "Ver")
- **Qué:** Los controles de vista de la biblioteca de media pasaron de un botón falso estilizado en el body a un grupo de menú genuino en el app toolbar real de la ventana (`desktop-app-toolbar`, junto a "Ventana"). Se añadió `isActive?: (ctx) => boolean` al contrato de comandos y `createAppToolbar` proyecta el activo como icono `Check` (patrón de checkmark de menú de OS, evaluado en cada apertura). `media-commands.ts` registra `media:filter-all/image/audio/video` y `media:view-library/trash` (orden 10-13 y 20-21, `contexts: ['toolbar']`, analítica `media.filter`/`media.view`, fail-closed) con un puente `setMediaViewHandler` fail-closed hacia la app. La app declara el grupo `{ label: 'Ver', items: [...] }` con `'---'` como separador, elimina el toolbar falso del body y limpia `destroy()`. Se retiró la receta `.barra-herramientas` huérfana y se actualizó el manual de identidad (prohibido toolbars falsos/`.campo`/controles segmentados en el body; acciones del body = `.boton-icono`/`.boton-con-icono`; alta primaria en `.desktop-window__actions`). Los separadores del menú contextual quedaron sin bordes entre items (solo el `.desktop-context-menu__separator` divide) por petición del usuario.
- **Archivos:** `frontend/src/features/runtime/command-registry.ts`, `frontend/src/features/desktop/components/desktop-window.ts`, `frontend/src/features/runtime/commands/media-commands.ts` (nuevo), `frontend/src/features/runtime/app-registration-admin.ts`, `frontend/src/features/desktop/apps/media-library/media-library.ts`, `frontend/src/styles/components.css`, `frontend/src/styles/desktop/desktop-media-library.css`, `frontend/src/styles/desktop/desktop-context-menu.css`, `Agente/documentacion/design-system/manual-identidad-visual-os-2026-07-29.md`, `roadmap.md`. El componente `segmented-control.ts` fue eliminado por el usuario (superado por el grupo de menú).
- **Gotchas:** El gate falló una vez por `TS2440` en `media-library.ts`: quedó una `type MediaFilter` local duplicada junto al import de `media-commands`; se eliminó la local. El checkmark se evalúa en cada apertura del menú (no al abrir la ventana) para reflejar estado fresco. La verificación de navegador confirmó el grupo "Ver" con separador y Check en el activo; los items del menú contextual solo existen en menús reales (los iconos del escritorio usan `.desktop-icon`, sin efecto lateral).
- **Sentinel:** gate `task:check -- 018A-71` PASS (scope full, 12 archivos; sentinel/varsense/rust/frontend/docs/custom PASS; warnings no bloqueantes ya documentados).
- **GLORY:** el patrón `isActive` + `Check` del CommandRegistry es agnóstico y candidato a glory-rs si se generaliza; los comandos de media y el puente son específicos del OS.

## 018A-72 — Modo oscuro: bordes de campos legacy y flecha del select se invierten
- **Qué:** En modo oscuro los bordes de los inputs del editor de artículos (`.campo-entrada`, `.campo-textarea`, `.campo-select`) desaparecían. Causa raíz: `--borde: 1px solid var(--color-borde)` está declarado en `:root` y CSS resuelve el `var(--color-borde)` interno **donde se declara `--borde`** (`:root`, siempre negro), no donde se usa; el scope oscuro de ventanas sobreescribía `--color-borde` pero eso jamás llegaba a `--borde`. Fix: redefinir `--borde: 1px solid var(--color-borde)` dentro del scope `[data-tema='oscuro'] .desktop-window/.movilApp/.movilLauncher`, que computa blanco ahí y lo hereda a todos los descendientes legacy (mismo mecanismo por el que `--sistema-borde` ya invertía: `--sistema-texto` se sobreescribe en `:root`). Además la flecha del select era un data URI hardcodeado negro (invisible en oscuro); se convirtió en token `--color-select-flecha` en `:root` con override blanco en el scope oscuro y `.campo-select` consume `var(--color-select-flecha)`. Verificado en navegador: bordes blancos y flecha visible en oscuro; sin regresión en claro (borde negro y flecha negra idénticos a antes).
- **Archivos:** `frontend/src/styles/variables.css`, `frontend/src/styles/components.css`.
- **Gotchas:** Un data URI no puede usar `currentColor`, por eso la flecha requiere token con doble definición. La lección general: no definir en `:root` un token compuesto con `var()` de un token que luego se sobreescribe en scopes descendientes — la resolución ocurre en la declaración. Los lints pre-existentes de `components.css` (límite 600 líneas, especificaciones visuales locales en `.notificaciones__item`/`.notificacionesAdmin__item`, selector `.comercio__producto h3`, botón fuera de Button.css) quedan registrados como deuda para un bloque de refactor separado.
- **Sentinel:** cubierto por el gate `task:check -- 018A-71` (mismo bloque, scope full PASS); VarSense sin errores.
- **GLORY:** el patrón de token invertible (componer borde con `var()` de un token que se redefine en el mismo scope de tema) es agnóstico; los tokens viven en `variables.css` del proyecto.

## 018A-74 — Editor de contenido (TipTap) como campo consistente
- **Qué:** El área de contenido del editor de artículos (TipTap) se veía como texto pelado con una sola línea inferior mientras el campo "extracto" se veía como un campo real con caja completa. Causa raíz: `.campo-textarea` (extracto) tiene CSS propio en `components.css` (borde completo `var(--borde)`, padding `--espacio-sm`, min-height 120px, foco 2px), pero `.article-editor__content` (contenido) no tenía ninguna regla CSS de app — solo heredaba la utilidad genérica `.border-bottom`, que pinta únicamente la línea inferior. Fix: hoja dedicada `desktop-article-editor.css` (patrón de `desktop-media-library.css`) que da al contenido el tratamiento de campo: borde completo, padding y `min-height: 200px`; `:focus-within` engrosa el borde a 2px como `:focus` de los campos legacy; ProseMirror sin outline propio; listas `ul`/`ol` restauradas (el reset global las deja sin marcador) con tokens del sistema, incluidas anidadas; bloques `pre`/`code` con caja; `audio` en bloque con `max-width`. Se retiró la clase `border-bottom` redundante del contenedor en TS y se importó la hoja en `main.ts` (después de media-library, antes de properties; los desktop-*.css son CSS plano, gana el orden de importación). Verificado en navegador: claro (caja `0.8px solid black`, padding 8px, min-height 200px) y oscuro (borde blanco, sin regresión del 018A-72).
- **Archivos:** `frontend/src/styles/desktop/desktop-article-editor.css` (nuevo), `frontend/src/main.ts`, `frontend/src/features/desktop/apps/article-editor/article-editor.ts`, `roadmap.md`.
- **Gotchas:** El editor se cierra al recargar la página (solo Perfil se restaura de sesión); para verificar hubo que reabrir Admin → "editar". El editor quedó con una sola línea por la utilidad `.border-bottom`; un campo requiere borde completo + padding + min-height explícitos. No se tocó `components.css` (deuda 018A-73) para respetar su refactor pendiente.
- **Sentinel:** gate `task:check -- 018A-74` PASS (scope incremental, 4 archivos; sentinel/varsense/frontend/docs/custom PASS; warnings no bloqueantes de `main.ts` pre-existentes y ya documentados).
- **GLORY:** patrón generalizable: cada app de escritorio debe tener su hoja `desktop-{app}.css` importada en `main.ts` y consumir los tokens del sistema; el CSS de campos vive en recetas compartidas, no ad-hoc.

## 018A-75 — El Reader renderiza el contenido TipTap como HTML
- **Qué:** El Reader de artículos convierte `article.content` (JSON ProseMirror) a HTML con `generateHTML` y las mismas extensiones del editor (StarterKit + Image) cuando no es string legacy, conservando la sanitización allowlist (`appendSanitizedHtml`). Se añadió la tipografía del contenido en `desktop-apps.css`: listas `ul`/`ol` restauradas (el reset global las dejaba sin marcador, incluidas anidadas), `pre`/`code` con caja, e imágenes/media con ancho acotado y márgenes con tokens. Verificado en navegador: el artículo con contenido TipTap se renderiza como HTML real (párrafo) en la ventana Reader, no el JSON crudo.
- **Archivos:** `frontend/src/features/desktop/apps/reader/reader-preview.ts`, `frontend/src/styles/desktop/desktop-apps.css`.
- **Gotchas:** El reset global de listas requería restaurar `list-style` y `padding-left` explícitamente en el scope `.desktop-reader__body`. Las reglas viven en el scope de la app Reader para no afectar otros contextos.
- **Sentinel:** type-check y suite frontend pasan; gate del bloque cubierto junto a 018A-76.
- **GLORY:** No aplica; es la presentación del contenido específica de wandori.us.

## 018A-76 — Artículos publicados aparecen en la carpeta "Notas" del escritorio
- **Qué:** Módulo `article-notas-sync` (idempotente, con teardown) que al guardar un artículo publicado garantiza la carpeta real "Notas" (nodo `folder` con ID estable, renombrable/movible) en el escritorio y añade el artículo como nodo `resource` dentro (locator `reader` + slug, `requires: public`). Al dejar de publicar retira el icono (tombstone) para no dejar accesos muertos; sincroniza la etiqueta por `fieldOverride` si el título cambia tras publicar. La carpeta es un nodo normal: se puede mover, renombrar, crear subcarpetas y sacar artículos con las mecánicas existentes. Conectado en `main.ts` con `stopArticleNotasSync` en teardown. Verificado en navegador por el usuario: publicar → icono en Notas → abrir Reader desde el icono.
- **Archivos:** `frontend/src/features/runtime/workspace/article-notas-sync.ts` (nuevo), `frontend/src/features/runtime/workspace/article-notas-sync.test.ts` (nuevo), `frontend/src/main.ts`.
- **Gotchas:** El sync debe ser idempotente y no sobrescribir la posición personal de la carpeta (renombrada/movida) — garantiza existencia, no reordena.
- **Sentinel:** type-check y test unitario del sync pasan; gate del bloque junto a 018A-75.
- **GLORY:** No aplica; el puente artículo→escritorio depende del modelo de workspace de wandori.us.

## 018A-77 — La navegación del Finder sincroniza el windowStore (taskbar + reapertura)
- **Qué:** Bug reportado por el usuario: abrir "Notas", navegar a "Escritorio" desde el finder y reabrir "Notas" no hacía nada, y la taskbar conservaba "Notas" aunque la barra de la ventana sí se actualizaba. Causa raíz: `navigateTo` del Finder actualizaba **solo el DOM** del título, nunca el `windowStore` (`title`/`params`/`_paramKey`). La taskbar (fiel al store) quedaba desincronizada, y `findExistingWindow` matcheaba por `_paramKey` viejo → al reabrir "Notas" creía que seguía abierta y solo enfocaba (no-op). Fix: nueva operación `updateWindowInstance(instanceId, {title?, params?})` en `window-manager.ts` que recalcula `_paramKey` con `stableParamsKey`; el `onNavigate` del Finder la invoca con `folderId` + label; el shell deriva el título de la barra desde el store en su update (se eliminó la mutación DOM a mano de `app-registration.ts`). Verificado en navegador: la taskbar pasa de "Notas" a "Escritorio" al navegar por breadcrumb, y reabrir "Notas" desde el escritorio abre una ventana nueva.
- **Archivos:** `frontend/src/features/runtime/window-manager.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/desktop/desktop-shell.ts`.
- **Gotchas:** La deduplicación del Finder sigue por `_paramKey`; al navegar a otra carpeta la ventana existente cambia de clave y la carpeta de origen se reabre en ventana nueva (comportamiento tipo explorador multiventana). El match de ventana en `onNavigate` usa `content === content` del store.
- **Sentinel:** `npx tsc --noEmit` PASS; gate del bloque ejecutado al cierre.
- **GLORY:** No aplica; `updateWindowInstance` vive en el runtime del OS (aunque actualizar identidad de instancia es un patrón agnóstico).

## 018A-78 — Iconos del Finder compactos y con trazo del sistema
- **Qué:** Los iconos del Finder flotaban en celdas de ~177px (columnas `1fr`) separados ~201px centro a centro, con el trazo 2px por defecto de Lucide. La rejilla pasó a celdas fijas de `--sistema-icono-celda` (88px, el mismo tamaño de los iconos del escritorio) con `justify-content: center` y gap/padding de `--espacio-md`: la distancia centro a centro bajó a 104px y el grupo queda centrado. Los iconos usan ahora `--sistema-finder-documento-tamano` (48px) y `stroke-width: var(--sistema-icono-trazo)` (1px, identidad del OS) vía CSS (sobreescribe el atributo `stroke-width="2"` de Lucide). Se retiró la regla muerta `.desktop-finder__document-icon` (nunca aplicada desde TS; su propósito vive ahora en `.desktop-finder__icon`) y el breakpoint móvil usa 2 columnas fijas. Verificado en navegador a 1440×900 (14 iconos en 88px, trazo 1px) y 390×844 (móvil usa la presentación launcher, sin ventana Finder).
- **Archivos:** `frontend/src/styles/desktop/desktop-apps.css`.
- **Gotchas:** Con columnas `1fr` reducir el gap no acerca los iconos: el espacio liberado se redistribuye a las celdas (los centros quedan igual). La solución real es celda fija. El trazo 1px se aplica por CSS (la propiedad `stroke-width` CSS gana al atributo). En móvil (<768px) el shell cambia a la presentación launcher y la ventana Finder no se renderiza; la regla de 2 columnas es segura de todos modos.
- **Sentinel:** gate `task:check -- 018A-78` PASS (scope incremental, 2 archivos; sentinel/varsense/frontend/docs/custom PASS). Hallazgos INFO/WARNING pre-existentes de `desktop-apps.css` (`.desktop-finder__item` con especificación local, selectores `h1-h6` del Reader de 018A-75) ya documentados como deuda.

## 018A-79 — Registro único de iconos por tipo de recurso (escritorio, Finder y móvil)
- **Qué:** El icono del artículo no correspondía porque existían tres mapas de iconos locales divergentes (escritorio `SHELL_ICON_MAP`, Finder `RESOURCE_ICON_MAP`, móvil derivado del icono de app) y el escritorio nunca leía `resourceKind` (fallback `Folder`). Fix: `ResourceTypeRegistry` es ahora la única fuente de verdad de iconos por `resourceKind` (`resolveResourceIcon`/`resolveResourceIconType`, fallback `File`/`document`); los tres consumidores delegan en ella. El Finder resuelve recursos por `resourceKind` y apps por `AppRegistry` (icono registrado, fallback `Package`); el escritorio resuelve recursos por `resourceKind`; el móvil ya no hereda el icono de app para recursos. Verificado en navegador: artículo "test" = FileText en escritorio y Finder; Admin = ShieldUser (icono registrado) en el Finder.
- **Archivos:** `frontend/src/features/runtime/resource-type-registry.ts`, `frontend/src/features/desktop/workspace-icon-grid.ts`, `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/features/mobile/mobile-launcher.ts`.
- **Gotchas:** Antes el escritorio mostraba Folder para cualquier recurso sin entrada en `SHELL_ICON_MAP` (el artículo parecía carpeta): la clase pictogram era correcta pero el SVG no. Las apps del Finder ahora muestran su icono registrado en `AppRegistry` (efecto lateral correcto del registro único). Un snapshot anterior del Finder mostraba `Box` en las apps — era DOM obsoleto de un módulo viejo; con código fresco muestran el icono registrado.
- **Sentinel:** gate `task:check -- 018A-79` PASS (scope incremental, 7 archivos; sentinel/varsense/frontend/docs/custom PASS; warnings no bloqueantes pre-existentes de `window-reference-outside-platform` en `workspace-icon-grid.ts`).
- **GLORY:** el patrón "registro único de iconos por tipo" es agnóstico y candidato a glory-rs; los iconos concretos y el `resourceKind` son del proyecto.

## 018A-80 — Iconos del Finder más pequeños y rejilla automática
- **Qué:** El usuario pidió iconos un poco más pequeños y columnas automáticas (no fijas). El grid del Finder pasó de `repeat(3, var(--sistema-icono-celda))` a `repeat(auto-fill, var(--sistema-icono-celda))` (mismo patrón del escritorio): las columnas se adaptan al ancho de la ventana manteniendo celdas fijas de 88px (compactas, sin redistribuir espacio). El icono bajó de 48px a 40px con un token nuevo `--sistema-finder-icono-tamano` (el `--sistema-finder-documento-tamano` de 48px sigue usado por media-library/layout/pages). El override móvil de 2 columnas quedó redundante (auto-fill ya produce 2 columnas en ventanas estrechas) y se retiró. Verificado en navegador: 5 columnas automáticas de 88px en el ancho actual, icono 40px, y consistencia escritorio↔Finder intacta (About/test = FileText en ambos).
- **Archivos:** `frontend/src/styles/desktop/desktop-apps.css`, `frontend/src/styles/variables.css`.
- **Gotchas:** Con `auto-fill` + celda fija las columnas son automáticas SIN re-distribuir el espacio (el problema que motivó las celdas fijas en 018A-78 era usar `1fr`). `auto-fill` y `auto-fit` coinciden cuando hay más items que columnas visibles. En móvil el Finder no se renderiza (presentación launcher), por lo que el override de 2 columnas era código muerto.
- **Sentinel:** gate `task:check -- 018A-80` PASS (scope incremental, 3 archivos; sentinel/varsense/frontend/docs/custom PASS; INFO/WARNING pre-existentes ya documentados: `.desktop-finder__item` con especificación local y selectores `h1-h6` del Reader).
- **GLORY:** No aplica; el patrón de grid automático con celda fija ya es el del escritorio.
- **GLORY:** No aplica; son tokens y rejilla del OS de wandori.us (aunque el patrón "celda fija en vez de 1fr para grids de iconos" es una lección agnóstica).

## 018A-81 — El extracto de los artículos no se ve al reabrir el editor
- **Qué:** El usuario reportó que "de los artículos no se guarda el extracto". La cadena completa (editor → autosave → service → handler → repository) guardaba bien el campo (verificado: `GET /api/articles/slug/test` devuelve `"excerpt":"hbhb"`), pero el textarea del editor se abría vacío. Causa raíz: `createEl` (compartido por input/textarea/select/option) asignaba `value` con `el.setAttribute('value', ...)`, que es un no-op para `<textarea>` (su contenido vive en la propiedad `.value`, no en un atributo) — también afectaba a `<select>` y a descripciones de producto/proyecto y aboutArea de admin. Fix de raíz en `createEl`: asigna la **propiedad** `.value` para `HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement` y conserva el atributo para el resto (`option`, `li`, etc.). Regresión añadida en `dom.test.ts` (3 casos: textarea por propiedad, input por propiedad, option por atributo). Verificado en navegador: al abrir "Editar" del artículo "test", el campo extracto muestra "hbhb".
- **Archivos:** `frontend/src/utils/dom.ts`, `frontend/src/utils/dom.test.ts`, `roadmap.md`.
- **Gotchas:** `setAttribute('value')` sí funciona para `<input>` (atributo default value) pero NO para `<textarea>`/`<select>`. El `textarea` de `components/ui/textarea.ts` ya pasaba `value` a `createEl` — el bug estaba aguas abajo en `createEl`. Beneficiarios del fix: extracto de artículo, descripción de producto (`product-editor.ts`), descripción de proyecto (`project-editor.ts`) y aboutArea de admin (`pages/admin.ts`).
- **Sentinel:** gate `task:check -- 018A-81` PASS (scope incremental, 3 archivos; sentinel/varsense/frontend/docs/custom PASS; INFO pre-existentes de `cssInlineScript` en `modal.ts` ya documentados).
- **GLORY:** No aplica; `createEl` es utilidad del proyecto (el patrón "propiedad vs atributo para value" es una lección agnóstica registrada en memoria).

## 018A-82 — Fix guardado de proyectos (contrato url) + select nativo del OS
- **Qué:** Dos sub-tareas. (a) **Bug de guardado de proyectos:** el frontend enviaba `url: { Set: "..." }` (contrato Orval, forma externally-tagged del enum `ProjectUrlUpdate`), pero el `Deserialize` custom del backend solo aceptaba string/null → HTTP 422 "invalid type: map, expected a string" en update Y autosave cuando la URL tenía valor. Fix en `src/models/project.rs`: el deserializer ahora acepta el formato de contrato `{ "Set": "<url>" }` además de los legacy (`"Unchanged"`, `"Clear"`, string cruda, null→Clear). Test ampliado (`distingue_url_omitida_nula_y_con_valor`) con los 3 casos de contrato; `cargo test --lib models::project` 2/2 PASS. (b) **Select nativo del navegador:** `createSelect` (antes un `<select>` nativo, control del navegador ajeno a la identidad OS) se reescribió como dropdown custom: botón `.campo-select` + menú `.campo-select__menu` que reutiliza el patrón `.desktop-context-menu` (hover inverse, tokens B&W, JetBrains Mono, flecha de `--color-select-flecha`). Misma API `{ label?, options, value?, onChange? }` → los 3 editores (artículo/proyecto/producto) no requirieron cambios. Navegación por teclado (Escape, ↑/↓ con wrap, Enter/Espacio), `aria-expanded`/`aria-selected`, cierre por click fuera. Nuevo CSS en `styles/desktop/desktop-select.css` importado en `main.ts`; 6 tests en `select.test.ts`.
- **Archivos:** `src/models/project.rs`, `frontend/src/components/ui/select.ts`, `frontend/src/components/ui/select.test.ts`, `frontend/src/styles/desktop/desktop-select.css`, `frontend/src/main.ts`, `roadmap.md`.
- **Gotchas:** (1) El deserializer custom de un enum con `#[derive(ToSchema)]` puede derivar del contrato que Orval genera (forma externally-tagged `{ Set: ... }`) → 422 silencioso en update/autosave; el backend debe cumplir su propio contrato OpenAPI. (2) El CSS base de `.campo-select` en `components.css` (border-bottom + flecha) sigue aplicando al botón nuevo — convive bien con el CSS de desktop. (3) En dev, `dev.mjs` resetea el schema de la BD si detecta migraciones incompatibles → se pierde la sesión admin y hay que recrearla (flujo documentado en memoria `local-admin-creation`); además `dev.mjs` no tiene auto-restart (matar el árbol con `taskkill /PID <pid> /T /F` y relanzar `npm run dev`). (4) Los clicks de Playwright por coordenadas/ref fallan por el drag handler de los iconos; `element.click()` JS directo ejecuta el handler real.
- **Verificación E2E (navegador):** login admin → Admin → editor de proyectos: crear "Proyecto E2E 018A-82" con `url: https://nakomi.studio` → toast "proyecto creado", `GET /api/admin/projects` devuelve la URL; update a `https://wandori.us` → toast "proyecto actualizado" + persistido; autosave sin errores. Select custom en los 3 editores: abre listbox con opciones, selección actualiza el botón y cierra el menú; 0 `<select>` nativos en la página. Datos de prueba eliminados.
- **Sentinel:** gate `task:check -- 018A-82` PASS (scope incremental, 6 archivos; sentinel/varsense/rust/frontend/docs/custom PASS; WARNING/INFO pre-existentes en `main.ts`/`models`/`modal.ts` ya documentados).
- **GLORY:** el patrón dropdown OS (botón + listbox sobre `desktop-context-menu`, navegación por teclado) es agnóstico y candidato al submodulo Glory; el fix del contrato `ProjectUrlUpdate` es específico de este proyecto.

## 018A-83 — Proyecto guardado como "visible" no aparece en la página pública /projects
- **Qué:** El usuario guardó un proyecto con el select "visible" y aparecía en admin pero no en la vista pública `/projects`. Causa raíz confirmada por BD: el proyecto tenía `is_visible=true`, `visibility='public'`, `lifecycle='active'`, pero **`editorial='draft'`** — y `ProjectRepository::list_visible` exige `editorial='ready'` para el catálogo público. Ningún flujo marcaba `editorial='ready'` en proyectos: `create` forzaba `Draft` (comentario: "la publicación editorial tendrá su propio comando") y `update` solo sincronizaba título+visibilidad vía `update_resource_metadata`. El select del editor y el comando `resource:publish` solo enviaban `is_visible`, así que el estado editorial de un proyecto nunca salía de `draft` (estado muerto). Fix de raíz: el control de visibilidad del editor ES el mecanismo de publicación de proyectos — visible ⇒ envelope `editorial='ready'` + `visibility='public'`; oculto ⇒ `editorial='draft'` + `visibility='private'`. `update_resource_metadata` acepta ahora `editorial: Option<EditorialState>` (productos pasan `None` y conservan su comportamiento; su catálogo público no exige `ready`, solo `visibility='public'` + `is_active`). Backfill en dev: proyectos con `is_visible=true` pasan a `editorial='ready'`.
- **Archivos:** `src/services/project_svc.rs`, `src/repositories/resource_repo.rs`, `src/services/product_svc.rs`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** (1) El diseño de estados independientes (editorial/visibility/lifecycle) es válido, pero si un catálogo público exige `editorial='ready'`, el control de UI que publica debe conducir ese estado — un control que solo proyecta `is_visible` deja contenido "visible" pero oculto del catálogo. (2) `product_svc` comparte `update_resource_metadata`; su semántica (`is_active` ⇒ `visibility`) NO incluye editorial y no debe cambiar. (3) No hace falta migración (no cambia schema; solo estado de datos). (4) `dev.mjs` no auto-restarta: hubo que matar el PID del backend (`taskkill /PID 16408 /T /F`) y relanzar `npm run dev:back` para cargar el binario nuevo.
- **Verificación E2E (navegador + API):** `GET /api/projects` devolvía vacío antes; tras el fix devuelve `test proyecto`. En la UI: editor de proyectos → visibilidad "oculto" → guardar → catálogo público 0 items y BD `editorial='draft'`+`visibility='private'`; visibilidad "visible" → guardar → catálogo 1 item y BD `editorial='ready'`+`visibility='public'`. La ventana pública "Proyectos" muestra "test proyecto" con su descripción y link. Admin muestra "test proyecto (oculto)" cuando está oculto. `cargo test` 40/40 PASS. Gate `task:check -- 018A-83` PASS (scope incremental; sentinel/varsense/rust/frontend/docs/custom PASS; hallazgos pre-existentes de `sqlx::query_as`/`sqlx::query` sin macro en `resource_repo.rs` ya documentados — el archivo usa SQL dinámico deliberadamente).
- **Sentinel:** no aplica regla nueva (semántica de dominio, no lint automatizable). Documentado como lección en memoria repo (`project-visibility-editorial-coupling`).
- **GLORY:** No aplica (lógica de dominio de wandori.us; el patrón "el toggle de publicación debe conducir el estado editorial del envelope" es una lección agnóstica registrada en memoria).

## 018A-84 — El link "ver" de proyectos debe abrir la URL del proyecto
- **Qué:** El usuario reportó que al pulsar "ver" en el catálogo público de proyectos no navegaba a la URL del proyecto. Causa raíz: la URL guardada (`nakomi.studio`) no tiene esquema, y `createExternalLink` generaba un `<a href="nakomi.studio">` — un href sin esquema es una ruta **relativa** del sitio, así que el click no salía a la web del proyecto. Fix en `frontend/src/utils/dom.ts`: nueva `normalizeExternalUrl(href)` que antepone `https://` cuando el href no tiene esquema (`/^[a-zA-Z][a-zA-Z0-9+.-]*:/`) y `createExternalLink` la usa. El dato guardado NO se normaliza ni se corrompe (solo el render).
- **Archivos:** `frontend/src/utils/dom.ts`, `frontend/src/utils/dom.test.ts`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** (1) Un href sin esquema se resuelve como relativo — es fácil de pasar por alto porque en la API el valor parece "correcto". (2) Normalizar en render (no en guardado) preserva el dato original del usuario. (3) Casos cubiertos en `dom.test.ts`: sin esquema, `http(s)://`, `mailto:`.
- **Verificación E2E (navegador):** catálogo público `/projects` → el link "ver" de "test proyecto" ahora apunta a `https://nakomi.studio`; la API sigue devolviendo `url: "nakomi.studio"` intacto. Vitest `dom.test.ts` PASS (32 tests en total con autosave).
- **Sentinel:** gate `task:check -- 018A-84` PASS (scope full 21 archivos; sentinel/varsense/rust/frontend/docs/custom PASS; WARNING pre-existentes de `dom-access-outside-platform` en `confirm.ts`/`modal.ts` ya documentados). Fue necesario `cargo fmt` por el test nuevo del modelo (018A-85 comparte gate).
- **GLORY:** No aplica (utilidad de render del proyecto; la lección "href sin esquema = relativo" es agnóstica y quedó en memoria repo `external-url-normalization`).

## 018A-85 — Imagen de portada opcional para proyectos
- **Qué:** El usuario pidió que los proyectos permitan imagen de portada igual que los artículos. Implementación end-to-end: migración `cover_image VARCHAR(1000)` en `projects`; modelo/repo/servicio con soporte de **borrado explícito** (`UpdateProjectRequest.cover_image: Option<Option<String>>` — `None` no toca, `Some(None)` borra, `Some(url)` reemplaza) vía `CASE WHEN $X THEN NULL` en el UPDATE; componente compartido `frontend/src/components/ui/cover-field.ts` (subir/cambiar/quitar + preview) extraído del editor de artículos y reutilizado por el editor de proyectos; autosave con `coverImage` ('' = sin portada); vista pública `/projects` renderiza `.proyecto-imagen` cuando hay portada; CSS en `pages.css`.
- **Archivos:** `migrations/20260801090000_018a85_project_cover_image.up.sql`, `migrations/20260801090000_018a85_project_cover_image.down.sql`, `src/models/project.rs`, `src/repositories/project_repo.rs`, `src/services/project_svc.rs`, `frontend/src/api/types.ts`, `frontend/src/api/generated/index.schemas.ts`, `frontend/src/components/ui/cover-field.ts`, `frontend/src/features/desktop/apps/article-editor/article-editor-ui.ts`, `frontend/src/features/desktop/apps/article-editor/article-editor.ts`, `frontend/src/features/desktop/apps/project-editor/project-editor.ts`, `frontend/src/features/desktop/apps/project-editor/project-editor-autosave.ts`, `frontend/src/features/desktop/apps/project-editor/project-editor-autosave.test.ts`, `frontend/src/pages/projects.ts`, `frontend/src/styles/pages.css`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** (1) **Serde colapsa `null` y ausente en `None`**: un `Option<Option<T>>` con `#[serde(default)]` simple no distingue JSON `null` (borrar) de ausencia (no tocar) — requiere `#[serde(default, deserialize_with="deserialize_some")]` con un helper que mapea cualquier valor a `Some(_)`. Test `portada_distingue_omitida_nula_y_con_valor` lo cubre. (2) `codegen:local` regenera solo `frontend/src/api/generated/**`; `frontend/src/api/types.ts` es **manual** y hay que añadir los campos a mano. (3) El UPDATE de artículos usa `COALESCE($4, cover_image)` que NO permite limpiar; projects usa `CASE WHEN $N THEN NULL` (patrón superior). (4) `cargo fmt` es obligatorio tras añadir tests con literales largos.
- **Verificación E2E (navegador + API):** editor de proyectos muestra "imagen de portada" → subir `1785605874-finalfinal.jpg` → preview visible → guardar → toast "proyecto actualizado" → `GET /api/projects` devuelve `cover_image: "/api/media/<uuid>/preview"` → catálogo público renderiza la imagen de portada en la tarjeta; `url` sigue intacta. `npm test` 41/41, vitest 32/32, `tsc --noEmit` PASS. Gate `task:check -- 018A-85` PASS (scope full 21 archivos; sentinel/varsense/rust/frontend/docs/custom PASS).
- **Sentinel:** no aplica regla nueva (feature de dominio). Lecciones registradas en memoria repo (`external-url-normalization` comparte el dominio de campos opcionales).
- **GLORY:** el componente `cover-field` (subir/cambiar/quitar con preview) es reutilizable y candidato a extraerse a glory-rs como receta de editor compartida.

## 018A-87 — Carpeta Documentos + sync de media al workspace
- **Qué:** El usuario preguntó cómo agregar una imagen a la "galería"; al investigar se descubrió que la Biblioteca de media permite subir pero los archivos no tenían ubicación visible en el OS. Diseño confirmado por el usuario: una carpeta OS tipo documentos con subcarpetas por tipo. Se reemplazó la carpeta vacía "Galería" del workspace por `Documentos` (raíz) + `Imágenes`, `Audio`, `Vídeo`, `Documentos`. Backend: release v2 (migración `20260801130000_018a87_documentos`). Frontend: bus de eventos `media-events` (`uploaded`/`deleted`/`restored`), sincronizador `media-gallery-sync` que crea carpetas y nodos `resource` en el overlay (o los tomba al eliminar / restaura), emisión desde `media-library.ts` y `upload.ts`, visor de imagen con descarga en el Finder, y título de la app Finder "Galería" → "Documentos" (+ fallback genérico). La página web pública `/gallery` queda intacta.
- **Archivos:** `migrations/20260801130000_018a87_documentos.up.sql`, `migrations/20260801130000_018a87_documentos.down.sql`, `frontend/src/features/runtime/workspace/default-release.ts`, `frontend/src/features/runtime/media-events.ts`, `frontend/src/features/runtime/workspace/media-gallery-sync.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/runtime/route-app-adapter.ts`, `frontend/src/features/desktop/apps/media-library/media-library.ts`, `frontend/src/utils/upload.ts`, `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/features/runtime/workspace/media-gallery-sync.test.ts`, `roadmap.md`.
- **Gotchas:** (1) La subcarpeta `Documentos/Documentos` queda vacía de forma natural: el backend `classify_media_type` solo clasifica `image`/`audio`/`video`, nunca `document` — los uploads de tipo documento no existen por ahora (el tipo existe en el contrato para futuro). (2) Los eventos usan `MediaUpload`/`MediaAdmin` (con `id`) — `MediaPublic` no expone `id`, por eso el sync se hace desde la Biblioteca y `upload.ts`, no desde el endpoint público. (3) El init de `initMediaGallerySync()` vive en `app-registration.ts` porque `main.ts` tiene cambios unstaged de otro agente (no se toca). (4) Media previo a la sincronización (p. ej. `1785609932-*.jpg`) no tiene nodo en el workspace — no se hizo backfill (fuera de alcance). (5) El título "Galería" en el header móvil era la sesión restaurada (estado viejo persistido); al abrir fresco usa el título nuevo "Documentos". (6) En el Finder la navegación es doble clic; un clic simple solo selecciona.
- **Verificación E2E (navegador):** desktop muestra "Documentos" (sin Galería) → subir imagen en Biblioteca → aparece en `Documentos/Imágenes` con thumbnail → doble clic abre visor modal (img + botón descargar) → Escape cierra → eliminar desde Biblioteca → nodo desaparece (Imágenes queda "Carpeta vacía") → restaurar desde papelera → nodo vuelve. Móvil 390×844: launcher muestra Documentos, subcarpetas accesibles, imagen visible y visor funciona. `type-check` PASS, vitest 26/26 (3 archivos afectados), suite workspace 84/84, gate `task:check -- 018A-87` PASS (13 archivos; warnings pre-existentes de `confirm.ts`/`modal.ts`).
- **Sentinel:** no aplica regla nueva (feature de dominio); el bus de eventos sigue el patrón de `article-editor-events`.
- **GLORY:** el patrón "eventos de catálogo → sync al overlay del workspace" es agnóstico y candidato a glory-rs; el visor de imagen con descarga es reutilizable.

## 018A-88 — Menú contextual y selección en el Finder
- **Qué:** El usuario reportó que dentro de las carpetas no funcionaba el menú contextual y que faltaban acciones de creación (artículo, subir imagen, nueva carpeta); además pedía que al hacer clic en elementos del explorador se seleccionaran con estado visual. Se añadió contexto de menú `finder` para el fondo del grid de carpetas (handler contextmenu que abre el menú con el target = carpeta actual), comandos de creación `article:new`, `product:new`, `media:upload`, `projects:new` y `finder:new-folder` registrados en contextos `['finder','folder']` (fondo y carpetas del Finder; NO en `icon` para no alterar el menú del escritorio), `workspace:paste` ampliado a `finder`, export `MEDIA_ACCEPT` como fuente única de extensiones, y toolbar «Archivo» del Finder ampliado con la misma creación (los adminOnly se auto-ocultan para public). Se eliminó el callback muerto `onCreateFolder` (declarado pero nunca invocado). Selección: `selectionStore.subscribe` cableado en Finder y escritorio — la clase `--selected` + `aria-selected` ya existía en CSS/`createDesktopIcon` pero nadie la consumía; ahora ambos grids reflejan el estado en vivo con los tokens `--sistema-inverso-*` (sin colores ad-hoc, regla 9.1).
- **Archivos:** `frontend/src/features/runtime/commands/finder-commands.ts` (nuevo), `frontend/src/features/runtime/commands/toolbar-commands.ts`, `frontend/src/features/runtime/commands/workspace-commands.ts`, `frontend/src/features/runtime/commands/index.ts`, `frontend/src/features/desktop/apps/media-library/media-library-utils.ts`, `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/features/desktop/workspace-icon-grid.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/styles/desktop/desktop-apps.css`, `roadmap.md`.
- **Gotchas:** (1) `article:new`/`product:new` abren el editor sin parámetros (modo creación confirmado: botón «crear»). (2) `media:upload` es autónomo (`pickAndUpload` crea su propio input y publica el evento) — el nodo aterriza en Documentos vía `media-gallery-sync` de 018A-87, sin bridge. (3) `selectBackground()` limpia la selección global (comportamiento estándar de OS); el clic en el fondo del grid deja de seleccionar. (4) En el escritorio el clic simple abre la app (diseño del OS); la selección se dispara con el clic derecho (el handler de menú selecciona antes de abrir). (5) El toolbar sin separadores: los separadores quedarían huérfanos al ocultarse los items adminOnly para public. (6) El gate `task:check` exige que el ID exista en roadmap/planes/completados — se registró la tarea en el roadmap antes de ejecutarlo.
- **Verificación E2E (navegador):** desktop: clic derecho en el fondo de carpeta → menú con Pegar/Nueva carpeta/Nuevo proyecto/Nuevo artículo/Nuevo producto/Subir archivo → «Nueva carpeta» crea dentro de la carpeta actual → clic derecho sobre carpeta → creaciones presentes → clic en ítem → clase `--selected` + `aria-selected=true` + label invertido (blanco/negro) → toolbar Archivo con la creación → «Nuevo artículo» abre el editor en modo creación → «Subir archivo» abre el file picker → escritorio: clic derecho en icono → `--selected` + label invertido. Móvil 390×844: Finder a pantalla completa, menú contextual y selección funcionan. `type-check` PASS, vitest 25/25 (4 archivos de commands), gate `task:check -- 018A-88` PASS (11 archivos; warnings de `window-reference` pre-existentes del reflow, no bloqueantes).
- **Sentinel:** no aplica regla nueva (feature de dominio); patrón contextmenu+selección sigue convenciones del shell.
- **GLORY:** el patrón «comandos de creación por contexto + selección por store suscrito» es agnóstico y candidato a glory-rs; `MEDIA_ACCEPT` centraliza la lista de extensiones.

## 018A-89 — Menú contextual del Finder en todo el alto del panel
- **Qué:** Corrección post-entrega de 018A-88: el grid del Finder solo medía el alto de sus ítems, dejando espacio vacío debajo (dentro del panel) fuera de su área — el clic derecho en esa zona no abría el menú contextual. Solución: `.desktop-finder` pasa a flex column con `min-height: 100%` (llena el body de la ventana) y `.desktop-finder__grid` gana `flex: 1` (ocupa todo el alto restante bajo el breadcrumb) + `align-content: start` (evita que las filas implícitas se estiren y espacien los iconos verticalmente). El handler `contextmenu` del grid ya existía y ahora cubre todo el panel; el breadcrumb (`.desktop-finder__path`) sigue sin disparar el menú.
- **Archivos:** `frontend/src/styles/desktop/desktop-apps.css`, `roadmap.md`.
- **Gotchas:** (1) Con `flex: 1` en un grid container, `align-content` default (`stretch`) estira las filas implícitas auto y separaría verticalmente los iconos — `align-content: start` lo evita. (2) Se usó `min-height: 100%` (no `height: 100%`) en el contenedor para que el Finder crezca con su contenido cuando hay muchos ítems y el body (overflow auto) siga scrolleando. (3) En la prueba del navegador quedaron artefactos de estado (menús colgados y doble ventana Documentos por restauración de sesión + apertura manual) — no son regresiones del cambio.
- **Verificación E2E (navegador):** con la ventana Documentos abierta, el grid mide ahora todo el alto del panel (384.8px bajo el breadcrumb, igual que el body). Clic derecho en la zona baja del grid (y≈593, debajo de los ítems) → menú contextual con Pegar/Nueva carpeta/Nuevo proyecto/Nuevo artículo/Nuevo producto/Subir archivo. `get_errors` sin errores nuevos (los reportados son pre-existentes: `.desktop-finder__item` y selectores h1-h6 del reader). Gate `task:check -- 018A-89` PASS (3 archivos; warnings de `main.ts` de otro agente).
- **Sentinel:** no aplica regla nueva (fix de layout CSS; `align-content: start` documentado como gotcha para futuros grids).
- **GLORY:** patrón «contenedor flex column + grid flex:1 para área clicable completa» es agnóstico y candidato a glory-rs como receta de panel con fondo interactivo.

## 018A-90 — Gestión de carpetas en el Finder
- **Qué:** El usuario pidió opciones de gestión (borrar, cortar, etc.) en el menú contextual de las carpetas dentro del Finder y auditar el problema de arquitectura detrás. La auditoría confirmó que CommandRegistry/overlay ya soportaban estas operaciones; el problema era de **exposición y semántica de contextos**. Se implementó: menú sobre carpeta (`'folder'`) con Abrir/Renombrar/Cortar/Copiar/Pegar en/Eliminar (crear queda solo en el fondo `'finder'`); borrado seguro con confirmación + `tombstoneSubtree` recursivo + `restoreNode` en cascada; `renameNode` vía `fieldOverrides.label`; `Ctrl+V` con destino (carpeta abierta del Finder como target); consolidación de `workspace:create-folder`; y nuevo diálogo `showPrompt` (el `window.prompt` nativo falla en el navegador integrado). `workspace-node-commands.ts` se separó de `workspace-commands.ts` por el límite de 300 líneas.
- **Archivos:** `frontend/src/features/runtime/commands/workspace-node-commands.ts` (nuevo), `frontend/src/components/ui/prompt.ts` (nuevo), `frontend/src/features/runtime/commands/index.ts`, `frontend/src/features/runtime/commands/toolbar-commands.ts`, `frontend/src/features/runtime/commands/workspace-commands.ts`, `frontend/src/features/runtime/commands/finder-commands.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/features/runtime/commands/keyboard-handler.ts`, `frontend/src/features/runtime/workspace/overlay-mutations.ts`, `frontend/src/features/runtime/workspace/merge.ts`, `frontend/src/features/runtime/workspace/workspace-store.ts`, `Agente/planes/plan-carpetas-finder-gestion-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** (1) `window.prompt()`/`alert()`/`confirm()` no funcionan en el navegador integrado de VS Code → todo diálogo usa componentes del sistema de UI (`showConfirm`, nuevo `showPrompt` con patrón B&W de confirm.ts + `createInput`); por el mismo defecto se migró también `workspace:rollback`. (2) La Papelera muestra el label ORIGINAL del addedItem y no el rename vía `fieldOverrides` (inconsistencia menor de UI pendiente, no bloqueante — al restaurar el merge sí aplica fieldOverrides). (3) El límite de 300 líneas obligó a dividir `workspace-node-commands.ts`. (4) `find_container` de coolify-manager no aplica aquí; nada de backend/deploy en este bloque.
- **Verificación E2E (navegador):** menú de carpeta dentro del Finder con Abrir/Renombrar/Eliminar/Copiar/Cortar/Pegar (Pegar disabled sin clipboard); fondo `'finder'` con crear; renombrar con showPrompt (Enter confirma); eliminar con showConfirm + tombstones recursivos; restaurar desde Papelera (subárbol vuelve, papelera vacía); Copiar → navegar a Imágenes → Ctrl+V → «CarpetaPrueba (copia)» dentro de Imágenes; Abrir navega dentro de la carpeta con breadcrumb y estado vacío; toolbar Finder con Nueva carpeta. Carpetas de prueba eliminadas al final. `get_errors` sin errores; `npx tsc --noEmit` EXIT=0; vitest 46/46 (merge, overlay-mutations, clipboard, overlay-sync); gate `task:check -- 018A-90` PASS (scope 15 archivos; sentinel/varsense/frontend/docs/custom PASS; warnings no bloqueantes de `prompt.ts` dom-access y `workspace-store.ts` mixed-barrel).
- **Sentinel:** no aplica regla nueva; los 2 warnings nuevos (`prompt.ts` dom-access-outside-platform en `entrada.focus()/select()` y `workspace-store.ts` mixed-barrel-logic en el re-export) son falsos positivos del patrón UI/diálogo y de la fachada canónica — no bloquean.
- **GLORY:** no aplica; los comandos y mutaciones dependen del runtime de wandori.us. El patrón «diálogo prompt B&W propio» y «comandos de gestión de nodos por contexto» son candidatos agnósticos a glory-rs si se generalizan.

## 018A-69 — Restauración de ventanas conserva la barra de acciones
- **Qué:** Validación visual (desktop + tablet) de que la restauración de sesión reconstruye el chrome inferior de las apps desde `MountedView.actions`, sin duplicar ventanas ni alterar geometría/taskbar/URL. La propagación y la regresión automatizada ya estaban implementadas (018A-1 / 317A-5); esta tarea era la prueba visual pendiente.
- **Archivos:** ninguno de código (solo validación); actualizado `Agente/completados/tareas-2026-08-01.md` y `roadmap.md`.
- **Verificación E2E (navegador):** desktop 1440×900 — abrir Biblioteca de media → la ventana muestra la franja `.desktop-window__actions` con el botón «subir archivo» → recargar → la ventana restaurada conserva la franja visible (verificado por DOM: `.desktop-window__actions` con `offsetParent != null`, botón «subir archivo» dentro, 1 sola ventana, geometría 640×480 intacta, URL `http://localhost:5174/` y taskbar sin cambios). Tablet 768×1024 — misma comprobación: franja visible con botón, sin duplicado, URL intacta. El foco de la ventana restaurada depende de la ruta: en `/` el shell abre Perfil (presentación inicial del OS, comportamiento deliberado en `main.ts`); al cerrar Perfil y enfocar Biblioteca, la franja sigue visible y funcional.
- **Gotchas:** (1) El snapshot de accesibilidad de Playwright puede quedar stale tras un reload (refs idénticos al árbol previo) — verificar siempre por `page.evaluate` sobre el DOM real. (2) Perfil es la presentación inicial del OS en ruta `/`: reaparece activa tras cada recarga aunque la sesión no la contenga; no es una regresión de la restauración. (3) El flush de sesión es debounce 200 ms + `pagehide`; tras cambios de ventana hay que esperar ~2.5 s antes de recargar para no restaurar un estado previo (artefacto que también afectó a la limpieza de carpetas de prueba de 018A-90). (4) La URL no representa el foco para apps sin recurso (Biblioteca genérica); solo apps con deep link cambian la URL.
- **Sentinel:** no aplica (tarea de validación visual; sin cambios de código).
- **GLORY:** no aplica.

## 018A-92 — Fallback local para los accesos de Bosque 3D
- **Qué:** El escritorio no mostraba Bosque 3D porque `fetchWorkspaceRelease()` reemplazaba el `DEFAULT_RELEASE` con el release local v2 del backend, que todavía contiene el catálogo anterior (`Snake`) y no incluye los nodos `game`/`game3d`. Se añadió un adaptador exclusivo de desarrollo que aporta esos dos nodos solo cuando faltan, conserva la versión/organización del release y deja que los tombstones del overlay sigan ocultándolos. Producción continúa dependiendo del release publicado por el administrador.
- **Archivos:** `frontend/src/features/runtime/workspace/local-development-release.ts`, `frontend/src/features/runtime/workspace/local-development-release.test.ts`, `frontend/src/features/runtime/workspace/workspace-store.ts`, `roadmap.md`.
- **Gotchas:** No se debe fusionar todo `DEFAULT_RELEASE` sobre el catálogo remoto: eso reintroduciría elementos que el administrador pudo quitar. El fallback está limitado a los prototipos visuales y a `import.meta.env.DEV`; para producción habrá que publicar una release que incluya ambos nodos.
- **Verificación:** Endpoint local confirmado: `/api/workspace/release` devolvía v2 sin Bosque. En navegador `http://127.0.0.1:5174/` el escritorio ahora muestra `Bosque` y `Bosque 3D`; la ruta `/forest-3d` y la ventana restaurada siguen funcionando. Pruebas del adaptador 2/2, contrato de workspace 5/5 y `tsc --noEmit` PASS. `npm run task:check -- 018A-92` PASS (Sentinel/VarSense/Rust/frontend/docs/custom; warnings preexistentes en `confirm.ts`/`modal.ts`).
- **Sentinel:** no se añadió una regla porque es compatibilidad de datos del prototipo, no un patrón lintable.
- **GLORY:** No aplica; el fallback conoce los nodos específicos de wandori.us.

## 018A-94 — Auditorías de calidad por fase para GAME-01
- **Qué:** Se actualizaron los planes del bosque 3D y de `Assets 3D`/terreno para que cada fase tenga un checkpoint obligatorio antes de avanzar. Cada checkpoint cubre SOLID/OCP/DIP y límites de módulos, rendimiento medido, escalabilidad/coste, seguridad/contratos, accesibilidad cuando aplica, teardown y observabilidad/privacidad. También se exigió que el gate de GAME-01 incluya esta auditoría y evidencia reproducible, no solo una prueba funcional.
- **Archivos:** `Agente/planes/plan-juego-bosque-multijugador-2026-08-01.md`, `Agente/planes/plan-assets-terreno-bosque-3d-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** Las auditorías son criterios de salida por fase; no autorizan optimizaciones especulativas ni duplican las reglas de Sentinel/VarSense. Un presupuesto no medido queda como hipótesis y bloquea la fase siguiente hasta obtener evidencia.
- **Sentinel:** no se añadió regla; el cambio es de gobernanza documental y usa el quality gate existente.
- **GLORY:** No aplica; los criterios están adaptados al plan específico de wandori.us.

## 018A-91 — Finder: sin texto de carpeta vacía + botón volver
- **Qué:** Ajustes del Finder pedidos por el usuario: (1) una carpeta vacía ya no muestra ningún texto — el grid queda en blanco y el clic derecho sobre el fondo sigue abriendo el menú porque `target === grid` (la comprobación residual de `.desktop-finder__empty` en el handler es código muerto inofensivo); (2) botón "volver a la carpeta anterior" en la barra de ruta con historial de navegación (`backStack`), deshabilitado en la raíz, que reconstruye en cada `render` (pathEl se vacía) y navega con `push=false` para no re-empujar la carpeta de la que se vuelve.
- **Archivos:** `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/styles/desktop/desktop-apps.css`, `roadmap.md`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** (1) El botón se reconstruye en cada render porque `pathEl.innerHTML = ''`; el listener debe recrearse cada vez. (2) `navigateTo(prev, false)` evita duplicar la carpeta de origen en el historial al volver. (3) `backBtn.disabled` con `backStack.length === 0` marca la raíz; el CSS usa `:disabled` con opacidad según el patrón canónico de botón de barra. (4) El estado vacío eliminado dejó referencias muertas a `.desktop-finder__empty` en el handler de contextmenu (target ahora siempre es el grid) — inofensivas, documentadas con comentario [018A-88].
- **Verificación E2E (navegador):** botón "Volver a la carpeta anterior" visible en la barra de ruta y deshabilitado en la raíz de Documentos; el usuario confirmó manualmente que el botón funciona («ya probé, funciona el botón»). `type-check` PASS; gate `task:check -- 018A-91` PASS (scope full 33 archivos; sentinel/varsense/rust/frontend/docs/custom PASS; warnings pre-existentes de `confirm.ts`/`modal.ts` dom-access).
- **Sentinel:** no aplica regla nueva (ajustes UI del Finder; patrón de botón de barra ya canónico).
- **GLORY:** patrón «barra de ruta con botón volver + historial de navegación» es agnóstico y candidato a glory-rs como receta de breadcrumb con historial.

## 018A-93 — Grid del Finder alineado al inicio
- **Qué:** El usuario pidió que el grid del explorador (`.desktop-finder__grid`) quede alineado al inicio en vez de centrado. Cambio de `justify-content: center` → `justify-content: start`, igual que el grid del escritorio (que no define `justify-content` y por defecto queda a `start`). Se conservan `flex: 1` y `align-content: start` del 018A-89 (alto completo del panel + filas sin estirar). Comentario `[018A-78][018A-80]` actualizado: la frase «grupo centrado» quedó obsoleta y se añadió la nota [018A-93].
- **Archivos:** `frontend/src/styles/desktop/desktop-apps.css`, `roadmap.md`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** El grid del escritorio (`desktop-shell.css`) no define `justify-content` — el valor por defecto de grid es `start`, así que el Finder centrado era la inconsistencia. Verificado por estilo computado (`justify-content: start`) y por geometría: el primer ítem arranca a 16px del borde del grid (padding `--espacio-md`), no centrado.
- **Verificación E2E (navegador):** ventana Finder en raíz con 11 ítems — primer ítem a 16px del borde izquierdo del grid (con `center` arrancaría en ~111px); `align-content: start` y `flex: 1` intactos; 5 columnas de 88px. `get_errors` sin errores nuevos (solo los pre-existentes de `.desktop-finder__item` y `.desktop-reader__body h1-h6`).
- **Sentinel:** no aplica regla nueva (ajuste de alineación puntual).
- **GLORY:** no aplica (ajuste de layout específico del Finder).

## 018A-95 — Selección del Finder aislada del escritorio
- **Qué:** Bug de UX confirmado: con el Finder en la raíz (mismos node-ids que el escritorio), seleccionar un ítem en el explorador marcaba el mismo objeto en el escritorio. Causa raíz: `selectionStore` era global y sin scope de superficie; ambas superficies suscribían y reflejaban `isSelected(nodeId)` con los mismos IDs. Solución (no parche): la selección se escala por `source` (`'desktop' | 'finder' | null`) — `selectSingle`, `toggleSelect`, `extendSelect` y `selectBackground` reciben la superficie, el estado guarda `source`, y `isSelected(id, source)` solo es verdadero cuando coincide. `clearSelection()` y `getSelectedIds()` quedan sin scope (navegación limpia todo; copiar/cortar por teclado usan la última selección como fallback sin targets, intacto). ID renumerado de 018A-94 por colisión con la auditoría GAME-01 del otro agente (ya archivada bajo 018A-94).
- **Archivos:** `frontend/src/features/runtime/selection-store.ts`, `frontend/src/features/desktop/workspace-icon-grid.ts`, `frontend/src/features/desktop/apps/finder/finder-preview.ts`, `frontend/src/features/desktop/desktop-shell.ts`, `roadmap.md`, `Agente/completados/tareas-2026-08-01.md`.
- **Gotchas:** (1) `toggleSelect`/`extendSelect` no tienen consumidores actuales, pero se actualizaron con el mismo contrato para no dejar la API partida. (2) El mousedown en el Finder re-renderiza el grid (subscribe → render), así que la validación por `page.evaluate` debe releer el DOM vivo tras el dispatch, no el elemento previo. (3) `setTimeout` no existe en el contexto top-level de `run_playwright_code` — usar `page.waitForTimeout`. (4) Comandos `workspace:copy`/`workspace:cut` leen `getSelectedIds()` (fallback global) — verificado que siguen intactos y con targets explícitos del menú.
- **Verificación E2E (navegador):** ventana Finder en raíz (11 ítems, mismos node-ids que el escritorio). Antes del fix: clic en "About" del Finder → icono `about` del escritorio pasaba a `aria-selected=true` (bug). Después: mousedown en "About" del Finder → el ítem del Finder queda `desktop-finder__item--selected` + `aria-selected=true` y el icono `about` del escritorio permanece `aria-selected=false`. El usuario confirmó manualmente («ya lo probé, funciona»). `npx tsc --noEmit` PASS (0 errores); gate `task:check -- 018A-95` PASS (scope full 52 archivos; sentinel/varsense/rust/frontend/docs/custom PASS; warnings pre-existentes de `confirm.ts`/`modal.ts` dom-access).
- **Sentinel:** no aplica regla nueva (bug de estado compartido resuelto por contrato de superficie, no lintable). Patrón «store global con scope de origen» documentado como lección.
- **GLORY:** el patrón «selección por superficie con source en un store global» es agnóstico y candidato a glory-rs como receta de multi-surface selection.

## 297A-31 — Fixture e integración real de publicación de mapas

- **Qué:** Se añadió un fixture de integración PostgreSQL para el flujo admin de `MapVersion`. Las pruebas ejercitan sesión opaca, rol admin/no-admin, CSRF, ausencia de sesión, `mapId` incoherente, publicación inicial, lectura pública, límite 413, revisión obsoleta, segunda versión, concurrencia, unicidad de versión activa y trigger de inmutabilidad en `UPDATE` y `DELETE`.
- **Archivos:** `tests/game_map_publish.rs`, `migrations/20260801150000_297a30_game_map_author_fk.up.sql`, `migrations/20260801150000_297a30_game_map_author_fk.down.sql`, `Agente/planes/plan-juego-bosque-multijugador-2026-08-01.md`, `roadmap.md`.
- **Gotchas:** Los snapshots de prueba usan IDs únicos y no se borran porque son inmutables; sus autores se conservan mediante `ON DELETE RESTRICT`. La BD de rama ya existente se preparó aplicando solo las migraciones faltantes, porque `prepare-ci-db.mjs` reaplica la migración inicial y no es idempotente sobre una BD persistente. El target Cargo compartido estaba bloqueado por un servidor local, así que la validación usó `GLORY_CARGO_TARGET_DIR` alternativo.
- **Sentinel:** `npm run task:check -- 297A-31` PASS con Sentinel/VarSense, Rust, frontend, docs y custom; `cargo fmt --check`, `cargo check --tests`, 7/7 tests de integración PostgreSQL, type-check, tests MapVersion/game-core, build y `git diff --check` PASS. Quedan únicamente warnings no bloqueantes preexistentes.
- **GLORY:** No aplica; el fixture depende del `AppState`, autenticación y dominio de mapas de wandori.us. La política de snapshots inmutables y autoría restrictiva queda como contrato específico del producto.

