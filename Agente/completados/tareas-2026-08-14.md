# Tareas completadas — 2026-08-14

## 138A-4 — Constructor de mundo del Bosque

**Contexto (decisión del usuario, 13-14-ago):** "necesito que planifiquemos un
constructor de mundo, en base a lo que ya tenemos, ya podemos cambiar opciones
de terreno, pero necesitamos un constructor completo". El usuario aprobó el
plan ("continua con el plan") y descargó el pack de assets de Synty
(Polygon Meadow Forest) como referencia futura, sin cambiar el alcance: se
sigue construyendo el motor/toolkit propio (138A-1) y el estilo final se
decide probando (Genshin-like low poly, sin indicadores, mundo único cap 32).

### Qué

1. **Contrato puro `TerrainOptions`** (`game-core/procedural/terrain-options.ts`):
   seed, forma (`isla`/`continente`/`archipielago`/`valle`), estilo de render
   (`actual`/`bloques`/`suave`), width/depth (múltiplos de 16, 16..256 con
   cuota de chunks), cellSize, maxHeight, waterLevel, coast, warp, octaves y
   vegetationDensity, con `TERRAIN_OPTIONS_DEFAULTS/LIMITS`, presets de forma
   y mundo, `validateTerrainOptions` y `normalizeTerrainOptions` fail-closed.
   Sin Three/DOM/red; exportado por `procedural/index.ts` y `game-core/index.ts`.
2. **Generador parametrizado** (`heightmap.ts`): `generateTerrainHeightfield`
   con máscara por forma (`shapeMask`) que reproduce EXACTAMENTE la fórmula
   histórica de la isla (48×32, maxHeight 4) sin romper consumidores;
   `generateIslandHeightfield` queda como caso particular. Continente con
   interior elevado y esquinas océano; archipiélago con clusters; valle con
   lago central.
3. **Pipeline opciones → MapVersion** (`game-core/map-builder.ts`):
   `buildMapVersionFromOptions` materializa chunks (bordes de vértices
   compartidos), manifiesto de assets con los ids del fixture, instancias de
   vegetación con presupuestos por densidad y spawn sobre tierra; valida con
   `assertValidMapVersion`. `mapBuilderStats` da métricas de presupuesto y
   `serializeWorld`/`parseSerializedWorld` exportan/importan el mundo en JSON
   (`{format:'wandorius-map',version:1,options,map}`).
4. **Comparador parametrizado** (`game-procedural-comparator.ts`, 300 líneas):
   acepta `TerrainOptions` completas, regenera bloques/suave sobre la misma
   base (`regenerateFromOptions`), escala tamaño, vegetación y agua; sin
   opciones mantiene la isla clásica. Fix de montaje: `buildToonWaterPlane`
   se llama UNA vez (antes la doble llamada filtraba un material por montaje).
5. **Sección Constructor en el panel** (`game-world-constructor.ts`, 289
   líneas + test DOM): forma, seed + aleatorio, ancho/profundo/celda,
   altura/agua/costa/warp/octaves/vegetación, "Generar mundo", "Exportar
   JSON" (descarga local) e "Importar JSON" (file input), con métricas del
   documento. `game-curved-island-panel.ts` la monta opcionalmente y expone
   `setConstructorStats`/`setConstructorOptions`/`setTerrainMode`.
6. **Cableado en la escena** (`game-playable-scene.ts`): al generar, construye
   el MapVersion con el pipeline puro, regenera el comparador, sincroniza el
   panel y muestra el resultado en el comparador (bloques/suave); exporta el
   JSON de `serializeWorld` y el import valida con `parseSerializedWorld`.
   CSS mínimo monocromo (`juegoPanelTerreno__doble/__entrada`) con los tokens
   del OS.

### Archivos

- `frontend/src/features/game-core/procedural/terrain-options.ts` + `terrain-options.test.ts`.
- `frontend/src/features/game-core/procedural/heightmap.ts` (+ `heightmap-shapes.test.ts`).
- `frontend/src/features/game-core/map-builder.ts` + `map-builder.test.ts` y exports en `procedural/index.ts`/`game-core/index.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-world-constructor.ts` + `.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-panel.ts`, `game-procedural-comparator.ts`, `game-playable-scene.ts`.
- `frontend/src/styles/desktop/desktop-game-playable.css`.
- `Agente/planes/plan-constructor-mundo-2026-08-14.md` (→ `Agente/planes/completados/`).
- `roadmap.md` (entrada 138A-4 cerrada y plan en Fuentes canónicas).

### Gotchas

- **Off-by-one de chunks:** el loop pedía la fila de vértices `lz=16` fuera
  del heightfield (NaN); se clampea a la última fila/columna real.
- **Continente pegado al mar:** el interior usaba `LAND_FLOOR` y el centro
  quedaba bajo el agua; se eleva a 0.5 en el preset continente y la máscara
  `1 - 0.9*d + warpV` garantiza esquinas océano.
- **Instancias fuera de bounds:** el redondeo a 3 decimales empujaba
  posiciones 0.0005 fuera del límite exacto; clamp con margen `half/bound`.
- **`maxHeight` sin usar en destructuring** (TS6133) y constantes
  `WIDTH/DEPTH/MAX_LEVEL` muertas tras parametrizar el comparador: retiradas.
- **Doble material del agua (dos fugas):** al montar, la doble llamada a
  `buildToonWaterPlane` filtraba un material (corregido en 138A-4); el
  `supervisor_reviewer` detectó que `rebuildWater` seguía creando un material
  nuevo en cada regeneración y descartaba el de `next` sin `dispose()`. Se
  extrajo `buildToonWaterPlaneGeometry` (única fuente de verdad de subdivisión
  y orientación) y el comparador reutiliza UN material de agua por montaje,
  regenerando solo geometría; test de ciclo de vida con spy sobre
  `buildToonWaterPlane` (1 llamada tras N regeneraciones) en
  `game-procedural-comparator.test.ts`.
- **Heightfield duplicado por rebuild:** bloques y suave generaban el heightfield
  por separado (2× coste y riesgo de bases distintas); ahora `rebuild` lo calcula
  una vez y lo comparte entre ambos builders.
- **Input de archivo fuera del DOM:** el file input oculto del import no se
  appendaba (el test lo destapó); se monta en la fila de botones.
- **Gate docs fail (esperado):** el plan activo no tenía checklist
  (`docs-plan-no-checklist`) y el roadmap no mencionaba `138A-4`
  (`docs-task-missing`); se añadió checklist, se archivó el plan y se cerró la
  entrada en roadmap antes del rerun.
- **Fase 4 diferida (documentada):** el retoque fino con el editor 2D y el
  fixture del runtime aprobado por el usuario quedan como siguiente bloque;
  el constructor ya exporta MapVersion listo para ese editor.

### Sentinel / gate

- `npm run gate:check -- 138A-4`: **PASS** (incremental) — sentinel PASS,
  varsense PASS, frontend PASS (type-check), docs PASS. Evidencia en
  `.quality-reports/check/138A-4/`.
- Verificación base: `npx tsc --noEmit` limpio; vitest completo
  **114 archivos / 804 tests PASS** (`npm run test:full` desde `frontend/`;
  selección incremental del runner: 8 archivos / 59 tests).
- Límites de líneas cumplidos: constructor 289, panel 292, comparador 300,
  map-builder 263, terrain-options 206; `game-toon-water.ts` 48 y el test de
  ciclo de vida 34.
- Sentinel 0.7.4 (`0349485c…`), VarSense 2.2.1 (`88f281f…`), rama `wandorius`.

### GLORY

- Commit explícito de implementación (`06928559`) sin `git add .` + commit
  documental de cierre.
- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS**.
  Reserva material: fuga de `MeshToonMaterial` por regeneración del agua
  (hallazgo IMPORTANTE); autorizado a corregir, repetir tsc/vitest/gate y
  continuar. Fix aplicado (helper de geometría pura + material único por
  montaje + test de ciclo de vida) en commit posterior; observaciones menores
  (cellSize sin propagar al preview, `style` no consumido, validación cruzada
  opciones↔mapa en import, heightfield duplicado —este último ya corregido— y
  error de lectura de import silencioso) quedan como deuda menor para el
  siguiente bloque.
- Revisión `sentinel_inspector` (`inspector_138a4`): **OK CON OBSERVACIONES,
  ACCION: ninguna** — gate 138A-4 PASS con scope de 3 archivos exactos del
  fix, doctor con Sentinel 0.7.4 / VarSense 2.2.1 fijados, rama `wandorius`,
  sin locks ni excepciones. Observación: `npx sentinel` resuelve al paquete
  npm ajeno y no al runtime fijado; decisión: usar scripts del proyecto, ya
  registrado en `Agente/lecciones/lecciones-aprendidas.md` (138A-4).
- Push a `origin/wandorius` según autorización del usuario ("subelos"),
  incluyendo los 4 commits pendientes de 138A-3 (`3abb13c6`, `0a5170f2`,
  `8684af12`, `87872fa7`).

## 138A-5 — Constructor de mundo v2: UI por iconos + tiempo real + persistencia

**Contexto (decisión del usuario, 14-ago):** "planifica todo esto a
continuacion … cuando se cambie un valor, se cambie en tiempo real … dividir
el panel de terreno en paneles pequeños … en un panel lateral con iconos como
en blender … que los valores no se pierdan a recargar". El plan
`plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md` (138A-5..9) quedó
aprobado y se implementa por bloques con gate propio; este bloque sienta la
base de UI/persistencia para 138A-6/7/8.

### Qué

1. **Rail lateral tipo Blender** (`game-world-constructor.ts`, 296 líneas):
   barra vertical de iconos Lucide (Mountain=Terreno, Globe=Mundo/Estilo) con
   `title`/`aria-label`/`aria-pressed`, un solo subpanel abierto a la vez y
   toggle al reclicar el icono activo. Registro declarativo de subpaneles
   (OCP): 138A-6/7/8 añadirán Cámara/Objetos/Color/Textura/Assets sin tocar
   el rail. Acciones Generar/Exportar/Importar + stats fijos bajo el rail.
2. **Controles compartidos** (`game-constructor-controls.ts`):
   `createRangeControl`, `createSelectControl`, `createSegmentControl` y
   `createSeedRow` con `setValue` para sincronizar desde fuera sin disparar
   eventos; sliders/inputs emiten `input`.
3. **Tiempo real con debounce** (`game-realtime-debounce.ts` + test):
   `createDebouncedRegenerator(200)` agrupa cambios rápidos en UNA
   regeneración (última gana), con `cancel()` y `dispose()` para teardown;
   la escena lo cablea en `onChange` y conserva el modo visible del
   comparador al regenerar (antes cada "Generar" volvía a `bloques`).
4. **Persistencia local** (`game-constructor-persistence.ts` + test):
   clave versionada `wandorius:constructor:v1` con `{version:1, options,
   mode}`; save fail-safe (quota/privacidad → false, sin catch vacío) y load
   fail-closed (JSON corrupto/opciones inválidas → null; modo inválido →
   `bloques`). La escena restaura opciones+modo al montar y guarda en cada
   generación, import y cambio de modo.
5. **Panel ensanchado solo con constructor** (`game-curved-island-panel.ts`
   + CSS): clase `juegoPanelTerreno--constructor` (320px) y estilos
   `juegoConstructor__*` con los tokens B&W del OS, sin radios/sombras.

### Archivos

- `frontend/src/features/desktop/apps/game-playable/game-world-constructor.ts` + `.test.ts` (9 tests).
- `frontend/src/features/desktop/apps/game-playable/game-constructor-controls.ts` (nuevo).
- `frontend/src/features/desktop/apps/game-playable/game-realtime-debounce.ts` + `.test.ts` (4 tests, nuevo).
- `frontend/src/features/desktop/apps/game-playable/game-constructor-persistence.ts` + `.test.ts` (7 tests, nuevo).
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts`, `game-curved-island-panel.ts`.
- `frontend/src/styles/desktop/desktop-game-playable.css`.
- `Agente/planes/plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md` (checklist 138A-5) y `roadmap.md`.

### Gotchas

- **`aria-pressed` booleano en `setAttribute`:** TypeScript rechaza `boolean`;
  hay que `String(key === panel.key)`.
- **`ctx.state` estancado:** el contexto del panel se crea una vez; usar un
  getter `get state()` (no destructuring) para que los syncers de
  `applyOptions` lean el estado vivo tras cada commit.
- **File input fuera del DOM:** el input oculto del import no se appendaba en
  el nuevo contenedor de acciones (el test lo destapó).
- **Vegetación en otro subpanel:** el slider de vegetación vive en
  "Mundo/Estilo"; el test que lo ejercita debe cambiar de icono primero.
- **localStorage en jsdom:** espiar `window.localStorage.setItem` no siempre
  aplica; se mockea el getter completo de `window.localStorage` con un objeto
  Storage que lanza, y se verifica el camino catch sin catch vacío.
- **Modo visible conservado:** `showConstructorWorld` usa
  `comparatorVisible ? comparatorMode : 'bloques'` para que editar un slider
  en tiempo real no resetee el estilo elegido en el comparador.

### Sentinel / gate

- `npm run gate:check -- 138A-5`: **PASS** (incremental, 14 archivos) —
  sentinel 0 errores/4 warnings, varsense 0 errores/2 warnings, frontend
  PASS y docs PASS. Evidencia en `.quality-reports/check/138A-5/`.
- `npx tsc --noEmit` limpio; vitest completo **116 archivos / 820 tests PASS**
  (`npm run test:full` desde `frontend/`); dirigidos **3 archivos / 21 tests**
  (constructor 9, debounce 4, persistencia 8).
- Límites de líneas: constructor 296, controls helper 158, debounce 69,
  persistencia 82; CSS aditivo con tokens del OS.
- Rama `wandorius`; Sentinel 0.7.4 y VarSense 2.2.1 fijados (sin cambios).

### GLORY

- Revisión `supervisor_reviewer` (`revisor_138a4`), ronda 1: **RECHAZADO** —
  hallazgo CRÍTICO: el bloque de restauración del estado persistido corría
  antes de la inicialización de `applyPick`, provocando `ReferenceError`
  (temporal dead zone) al montar la escena con `wandorius:constructor:v1`
  guardado. El gate pasaba porque ningún test cubría ese camino. 4 menores:
  `onGenerate` sin cancelar el debounce pendiente, `dispose()` dependiente de
  `this`, persistencia que normalizaba payloads parciales a defaults y error
  de lectura de import silencioso.
- Fixes aplicados: restauración movida después de `applyPick` (invariante
  comentada en `game-playable-scene.ts`); `regenerateDebounced.cancel()` en
  `onGenerate`; `cancel` capturado en closure para `dispose()`; validación
  estricta con `validateTerrainOptions` antes de normalizar (parcial → null,
  +1 test) y error visible `no se pudo leer el archivo` en stats.
- Ronda 2: **APROBADO CON RESERVAS** — verificado el fix crítico y los 4
  menores; reserva menor de documentación (contadores obsoletos, corregidos
  aquí). Test de integración de restauración con WebGLRenderer mockeable
  queda pendiente por límite jsdom, registrado en la prevención.
- Prevención creada:
  `Agente/prevencion/prevencion-constructor-restauracion-tdz-2026-08-14.md`
  (regla: restauración de montaje siempre después de todos los consts que
  invoca; test de integración pendiente documentado).
- Pendiente del usuario: validación visual en `/forest-playable` (cambiar
  valores en tiempo real, recargar y verificar que el estado persiste).
  Push a `origin/wandorius` según autorización del usuario ("subelos"); si
  las credenciales fallan, reportar el bloqueo sin reintentar a ciegas.

**Corrección 14-ago (feedback del usuario sobre la jerarquía del panel):**
el usuario reportó que veía `juegoPanelTerreno > juegoPanelTerreno--constructor
> juegoConstructor`, cuando el constructor debía ser el panel exterior y el
panel del terreno una sección más de sus iconos.

### Qué (corrección)

1. **`game-world-constructor.ts` pasa a ser el panel exterior:** raíz
   `section.juegoConstructor` posicionada en la esquina del viewport con
   cabecera colapsable (`juegoConstructor__cabecera` + `aria-expanded`,
   título "Constructor"), stopPropagation de puntero/rueda en la raíz y
   contrato OCP exportado: `WorldConstructorSubpanel`, `WorldConstructorOptions`
   y `mountWorldConstructor(host, controls, { extraPanels, title })`.
2. **Nuevo `game-curved-island-controls.ts`:** extrae `buildIslaGroup`
   (curva del mundo + lluvia + árboles/rocas + cámara sigue + crecer isla) y
   `buildEstilosGroup` (comparador Actual/Bloques/Suave + métricas) desde el
   panel clásico; tipo `IslandTerrainMode` para el comparador (hay una unión
   paralela `ConstructorTerrainMode` en persistencia, pendiente de
   consolidar en 138A-6).
3. **`game-curved-island-panel.ts` como orquestador:** con constructor monta
   el rail exterior y registra "Isla" (`Waves`) y "Estilos" (`Layers`) como
   secciones; sin constructor conserva el panel clásico `juegoPanelTerreno`
   (legacy). `setTerrainMetrics` solo escribe si la línea sigue conectada.
4. **Renombrado `constructor` → `worldConstructor`** en
   `CurvedIslandPanelControls` (y escena): la propiedad opcional `constructor`
   chocaba con el `constructor` implícito de `Object` en literales de
   TypeScript cuando se omitía.
5. **CSS:** `.juegoConstructor` toma el posicionamiento/ancho/borde del panel
   antiguo; se elimina `juegoPanelTerreno--constructor`; cabecera, estado
   `--cerrado` y paddings del cuerpo/acciones nuevos con tokens B&W.

### Archivos (corrección)

- `frontend/src/features/desktop/apps/game-playable/game-world-constructor.ts` + `.test.ts` (+1 test OCP).
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-controls.ts` (nuevo).
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-panel.ts` + `.test.ts` (nuevo, 7 tests).
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts` (renombrado del campo).
- `frontend/src/styles/desktop/desktop-game-playable.css`.
- `Agente/planes/plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md` (checklist 138A-5).

### Gotchas (corrección)

- **`constructor` como campo opcional:** TypeScript compara el `constructor`
  implícito de `Object` (`Function`) con el tipo del campo al asignar
  literales sin esa clave; renombrar a `worldConstructor` elimina la fricción
  sin cambiar el contrato funcional.
- **Subpanel con métricas al cerrar/abrir:** `setTerrainMetrics` guarda con
  `isConnected` para no escribir en un elemento desmontado; al reabrir
  "Estilos" se re-registra el elemento nuevo.

### Sentinel / gate (corrección)

- `npm run type-check` limpio; vitest dirigido 2 archivos / 17 tests PASS y
  suite completa **117 archivos / 828 tests PASS**.
- `npm run gate:check -- 138A-5` (corrección): **PASS** — 9 archivos,
  sentinel 0 errores/4 warnings, varsense 0 errores/3 warnings, frontend y
  docs PASS. Las 3 warnings `claseHuerfana` son clases usadas por
  `classList.toggle` (`juegoPanelTerreno--cerrado` y `juegoConstructor--cerrado`)
  o preexistentes (`boton--activo`); no bloquean.

### GLORY (corrección)

- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO** — la
  jerarquía cumple la petición (rail exterior, terreno como sección), OCP de
  `extraPanels` correcto, sin pérdida de funcionalidad, teardown sin fugas y
  gate validado. 3 menores: mock muerto en el test (eliminado en esta
  corrección), unión de modos duplicada (`ConstructorTerrainMode` en
  `game-constructor-persistence.ts` y `IslandTerrainMode` en
  `game-curved-island-controls.ts`; consolidar en un solo tipo antes de
  138A-6) y `game-world-constructor.ts` a 334 líneas (extraer builders si
  138A-6/7/8 lo hace crecer).
- Pendiente del usuario: validación visual en `/forest-playable` (el rail
  ahora es el panel exterior y Terreno/Mundo-Estilo/Isla/Estilos son sus
  secciones; cabecera colapsable "Constructor").

## 138A-6 — Dos estilos, sin árboles en suave, cellSize real, escala menor

**Contexto (decisión del usuario, 14-ago):** "eliminar los arboles de suave",
"dejar 2 estilos, bloque y suave", "planificar poder cambiar el tamaño de los
bloques" y "las cosas van mas pequeñas". La interpretación de "más pequeñas"
se resuelve como escala base de vegetación/props ~0.5× + `cellSize` visible en
el preview (decisión documentada en §7 del plan; pendiente de confirmar en
navegador). Deuda del revisor de 138A-5 (unión de modos duplicada) se
consolida aquí.

### Qué

1. **2 estilos (`bloques`/`suave`):** `RenderStyle` en
   `terrain-options.ts` queda restringido a `'bloques'|'suave'` (default
   `bloques`) y se retira `actual` de `RENDER_STYLES`. La unión paralela se
   consolida: `ConstructorTerrainMode` e `IslandTerrainMode` desaparecen y
   todo consume `RenderStyle` de game-core (persistencia, panel, controles,
   comparador, escena). El modo histórico `actual` guardado migra a `bloques`
   al cargar (fail-closed, `VALID_MODES=['bloques','suave']`).
2. **Sin árboles en suave:** el comparador (`buildSmooth`) y
   `map-builder.buildInstances` pasan `maxTrees: 0` cuando
   `style==='suave'`; se conservan césped y rocas.
3. **`cellSize` real en el comparador:** suave usa heightfield con
   `cellSize` y escala placements x/z (altura intacta); bloques escala el
   grupo `(cellSize, 1, cellSize)` (huella x/z, sin re-escalar geometría);
   agua `width*cellSize*2.4`; `cellAtWorld` divide por `cellSize` y
   `pickTerrain` reporta `worldX/worldZ` en unidades de mundo (paridad
   preview↔documento, corrige hallazgo menor del revisor de 138A-4).
4. **Escala menor:** `VEGETATION_BASE_SCALE = 0.5` en `vegetation.ts`
   (game-core, no mágica en adaptadores); instancias `0.5*(0.8..1.25)` →
   `0.4..0.625`, consumida por documento y preview vía `scale`.
5. **Escena:** `applyTerrainMode` simplificado — elegir estilo siempre muestra
   el comparador; la isla curva queda solo como vista inicial/referencia.

### Archivos

- `frontend/src/features/game-core/procedural/terrain-options.ts` + `.test.ts`.
- `frontend/src/features/game-core/procedural/vegetation.ts` + `.test.ts`.
- `frontend/src/features/game-core/map-builder.ts` + `.test.ts`.
- `frontend/src/features/game-core/procedural/heightfield-mesh.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-procedural-comparator.ts` + `.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-constructor-persistence.ts` + `.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-controls.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-panel.ts` + `.test.ts`.
- `Agente/planes/plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md` (checklist 138A-6) y `roadmap.md`.

### Gotchas

- **El test de "menos props en suave" falló por presupuestos:** bloques=60
  (`PROP_COUNT` fijo) < suave=98 (incluye césped), así que comparar recuentos
  era inválido. Se sustituyó por `vi.mock('../../../game-core')` (mock de
  módulo completo en ese archivo) + `expect(gameCore.placeVegetation)
  .toHaveBeenCalledWith(anything, anything, objectContaining({ maxTrees: 0 }))`.
- **Consolidar el modo por ambos lados:** `IslandTerrainMode` (panel) y
  `ConstructorTerrainMode` (persistencia) vivían en capas distintas; la
  unión única se define en game-core y la persistencia valida con
  `VALID_MODES` propio para no importar UI desde storage.

### Sentinel / gate

- `npm run type-check` limpio; suite completa **117 archivos / 835 tests PASS**
  (`npm run test:full` desde `frontend/`); dirigidos **7 archivos / 41 tests**
  (terrain-options 3, vegetation 4, map-builder 4, heightfield-mesh 4,
  comparator 10, persistence 9, panel 7).
- `npm run gate:check -- 138A-6` (corrida final tras docs): **PASS**
  (incremental, 18 archivos) — sentinel 0 errores/3 warnings/1 info (2
  `dom-access-outside-platform` y `limite-lineas` 569 en
  `game-playable-scene.ts`, deuda preexistente no introducida por este bloque;
  1 info `todo-pendiente` falso positivo verificado), varsense 0 errores/0
  warnings, frontend 0/0/0 y docs 0/0/0. Evidencia en
  `.quality-reports/check/138A-6/`.
- Rama `wandorius`; Sentinel 0.7.4 y VarSense 2.2.1 fijados (sin cambios).

### GLORY

- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS**
  — código cumple contrato y gate PASS. Hallazgo IMPORTANTE documental (roadmap
  y completada sin actualizar; corregido aquí antes del cierre). 2 menores:
  falso positivo `todo-pendiente` (info) en `vegetation.ts:66` por la palabra
  "pendiente" de la máxima pendiente entre vecinos (verificado, no hay TODO;
  refinar la regla VarSense en upstream con fixtures como mejora opcional) y
  deuda preexistente de líneas en `game-playable-scene.ts` (569, no tocar en
  este bloque). Comparador leído completo sin hallazgos (build/apply/pick/
  dispose correctos, un solo heightfield compartido y material único).
- Push a `origin/wandorius` según autorización del usuario ("subelos").
- Pendiente del usuario: validación visual en `/forest-playable` (Celda en
  tiempo real, suave sin árboles, cosas más pequeñas, solo Bloques/Suave).

## 138A-7 — Tres modos de cámara

**Contexto (decisión del usuario, 14-ago):** ítem 11 de la lista del
constructor ("planificar poder elegir entre 3 modos de camara, primera
persona, libre y 3er persona"). Se implementa sobre la persistencia y el rail
de iconos de 138A-5 y los 2 estilos de 138A-6.

### Qué

1. **Módulo de modos** (`game-camera-modes.ts`, nuevo): `CameraMode =
   'libre'|'primera'|'tercera'`, `CAMERA_MODES`, `DEFAULT_CAMERA_MODE =
   'libre'`, `CAMERA_CYCLE_KEY = 'c'`, `isCameraMode`, `cycleCameraMode`
   (libre→primera→tercera→libre), `isCameraCycleEvent` (ignora
   input/textarea/select y `contenteditable`, con fallback por atributo
   porque jsdom no refleja la propiedad) y `attachCameraModeShortcut` con
   teardown.
2. **Persistencia:** `ConstructorPersistedState` incluye `readonly camera:
   CameraMode`; `loadConstructorState` es fail-closed a `libre` si el campo
   falta o es inválido.
3. **UI:** `buildCamaraGroup` (segmentos Libre/Primera/3ª persona + hint
   "Atajo: tecla C") en `game-curved-island-controls.ts`; sección `Cámara`
   (icono Lucide `Camera`) en el panel cuando `controls.setCameraMode`;
   `setCameraMode(mode)` en la interfaz del panel.
4. **Escena** (`game-playable-scene.ts`): constantes `CAMERA_EYE_HEIGHT=1.6`,
   `CAMERA_PITCH_MIN/MAX=±1.2` y `CAMERA_GROUND_CLEARANCE=1.1`; estado
   `cameraMode` + `look {yaw,pitch}`; `setCameraMode` actualiza el modo,
   resetea `look` al entrar en primera persona, sincroniza el panel y
   persiste; `updateCamera` en primera persona usa los ojos del personaje
   (`currentPlayerY + 1.6`) con dirección derivada de yaw/pitch y niebla fija
   a `CAMERA_DISTANCE`; 3ª persona reusa la órbita con clamp al suelo
   (`y >= groundHeightAt + 1.1`); libre = órbita actual. `onOrbitMove` gira
   `look` en primera y órbita en el resto; `onWheel` en primera solo hace
   `preventDefault`. Restauración: `cameraMode = restored.camera` antes de
   mostrar el mundo y `setCameraMode` tras el bloque (invariante de orden con
   el resto de la restauración). `getCameraAzimuth()` devuelve `look.yaw` en
   primera (clave para que `rotateInputToWorld`/W alejen la cámara del
   jugador). `stopCameraShortcut` se llama en `destroy`.

### Archivos

- `frontend/src/features/desktop/apps/game-playable/game-camera-modes.ts` (nuevo) + `.test.ts` (nuevo).
- `frontend/src/features/desktop/apps/game-playable/game-constructor-persistence.ts` + `.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-controls.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-island-panel.ts` + `.test.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts`.
- `Agente/planes/plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md` y `roadmap.md`.
- Referencias de diseño del usuario incorporadas al plan:
  `Agente/documentacion/design-system/referencia-contour-terrain-editor-2026-08-14.md`
  y `Agente/documentacion/design-system/referencia-skydome-clouds-2026-08-14.md`.

### Gotchas

- **`contenteditable` en jsdom:** la propiedad no se refleja; el test usa
  `setAttribute('contenteditable','true')` y `isCameraCycleEvent` consulta el
  atributo como fallback para no interceptar la tecla `C` al editar.
- **Sentido del movimiento en primera persona:** `rotateInputToWorld` usa
  forward `(-sin(a), -cos(a))`; `getCameraAzimuth()` debe devolver `look.yaw`
  (no la azimut de órbita) para que W aleje la cámara del jugador.
- **Orden de montaje:** `setCameraMode` se define ANTES de
  `mountCurvedIslandPanel` (para pasarlo en controles) y el sync del panel
  (`syncCameraSegment`) se asigna tras montarlo; restaurar cámara antes de
  `showConstructorWorld` evita sobrescribir el modo al mostrar el mundo.
- **3ª persona sin atravesar el terreno:** clamp `y >= groundHeightAt(x,z) +
  1.1` en la órbita; primera persona reusa `currentPlayerY` (misma base de
  colisión que el jugador).
- **Límite conocido de colisión en 3ª persona (aceptado):** el clamp es de
  punto único bajo la cámara; no hace raycast de segmento contra cerros
  intermedios entre jugador y cámara. Si en la validación visual se ve
  clipping con relieve intermedio, el fix es acortar la distancia o muestrear
  el segmento; queda registrado como deuda para 138A-11.

### Sentinel / gate

- `npx tsc --noEmit` limpio y suite completa **118 archivos / 842 tests PASS**
  (`npm run test:full` desde `frontend/`); dirigidos **3 archivos / 23 tests**
  (camera-modes 4, persistence 11, panel 8), de los cuales **7 son nuevos del
  bloque** (4 + 2 + 1).
- `npm run gate:check -- 138A-7`: **PASS** — evidencia en
  `.quality-reports/check/138A-7/`. `game-camera-modes.ts` < 150 líneas;
  `dom-access-outside-platform` ×2 preexistentes (líneas 204/207) sin
  cambios. **`limite-lineas` en la escena subió de 569 (138A-6) a 624
  efectivas (ALTO nivel 2, duplica el límite de 300)** por la lógica de
  cámara añadida: deuda registrada en el plan (138A-11) para extraer un
  controlador de cámara por modo (`updateCamera` + handlers), no se toca en
  este bloque.
- Rama `wandorius`; Sentinel 0.7.4 y VarSense 2.2.1 fijados (sin cambios).

### GLORY

- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS
  — AUTORIZADO PARA CONTINUAR** con el commit local tras corregir la
  precisión documental (deuda 569→624/ALTO y conteos de tests 4/11/8=23, 7
  nuevos) y registrar la deuda de extracción del controlador de cámara hacia
  138A-11. Solidez verificada: contrato puro, persistencia fail-closed,
  orden de montaje sin TDZ, teardown idempotente y azimut consistente para
  WASD; sin lógica de cámara en `game-core`. Reserva menor aceptada: la
  colisión de 3ª persona es de punto único (límite documentado en Gotchas).
- Commit explícito 138A-7 (implementación + plan adaptado con las dos
  referencias de diseño) y push a `origin/wandorius` según autorización del
  usuario ("subelos"), incluyendo los archivos de referencia del usuario.
- Pendiente del usuario: validación visual en `/forest-playable` (tecla `C` y
  selector Cámara: libre, primera persona con WASD+mouse look y 3ª persona
  sin atravesar el terreno).

## 138A-8 — Panel-ventana, transform de objetos y paneles de Color, Textura y Assets

**Contexto (usuario, 14-ago):** "termina todo lo pendiente y subelos"; la
referencia Skydome (nubes pintadas procedurales) quedó incorporada en el
bloque 138A-7. Este bloque ejecuta la lista de ajustes del usuario sobre el
constructor v2: ventana lateral completa sin título, colapsable a los lados y
redimensionable, transform de objetos (sin editar modelos, "para eso tengo
Blender"), manejo de assets con arrastrar/quitar y paneles de color/textura.

### Qué

1. **Panel-ventana** (`game-world-constructor.ts` + CSS `desktop-game-playable.css`):
   `juegoConstructor` es ventana lateral de alto total, sin título en la
   cabecera, colapsable izquierda/derecha (cabecera vertical al plegar),
   ancho redimensionable por arrastre del borde (240..520 px, default 320) y
   estado colapsado/lado/ancho persistido con 138A-5 (`normalizePanelState`
   fail-closed; `constructorPanelState` opcional en storage).
2. **Transform de objetos puro** (`game-core/map-edits.ts`): `MapEditOp`
   (`move`/`add`/`remove`/`setScale`) y `editMapVersionObjects` con clamping
   a bounds locales (±w/2·cellSize), cuotas `maxInstances` fail-closed,
   escala default 0.5 y `assertValidMapVersion` al final; sin geometría.
3. **Panel de Color** (`game-constructor-color.ts`): 13 pickers sobre la
   paleta unificada `world-palette.ts` (tokens serializables, defaults y
   `validate/normalize` fail-closed), "Restaurar" y sync externo; se aplica
   en tiempo real a bloques/suave/agua y tiñe cielo + niebla de la escena.
4. **Panel de Textura** (`game-constructor-texture.ts`): file (data URL, máx
   4 MiB) o URL validada (http/https/data) → rampa toon global muestreada a
   8 píxeles desde la fila central (carga con `crossOrigin='anonymous'` y
   mensaje claro ante fallo CORS); "Restaurar rampa" vuelve a la procedural.
   Deuda documentada: el mesher pinta por vértices, así que la textura se
   aplica como `gradientMap` global, no por material.
5. **Panel de Assets** (`game-constructor-assets.ts`): inventario por
   categoría/asset/instancia (máx 60 filas), Quitar/Limpiar todo y drag&drop
   (`ASSET_DRAG_MIME`) con drop en el host: raycast → pick del terreno
   visible → `add` en el documento (el pick de escena se convierte a frame
   local restando los centros de la isla).
6. **Orquestador** (`game-playable-scene.ts`): `constructorPalette`,
   `constructorPanelState`, `applyToonRamp` (libera la textura anterior en
   todos los materiales toon sin regenerar mallas), documento inicial del
   comparador desde el primer frame, `onEditObjects`/`onPaletteChange`/
   `onToonRampChange`, persistencia ampliada y restauración de documento/
   paleta/ventana al recargar.
7. **Fix de coordenadas** (`game-procedural-comparator.ts`): las instancias
   del documento viven en el frame local del terreno; `rebuildDocumentProps`
   ahora traduce a escena (`+centerX/+centerZ`) para posicionar el prop y
   buscar su celda de anclaje.

### Archivos

- `frontend/src/features/game-core/world-palette.ts` (+test), `map-edits.ts`
  (+test) y re-export en `game-core/index.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-constructor-persistence.ts`
  (+test): `ConstructorPanelState` opcional persistido.
- `game-world-constructor.ts` (+test): ventana, ctx de paleta/documento/
  rampa y sincronizadores.
- `game-constructor-color.ts` (+test), `game-constructor-texture.ts`
  (+test), `game-constructor-assets.ts` (+test).
- `game-procedural-comparator.ts` (+test): `setPalette`, `setToonRamp`,
  `setDocument` y fix de coordenadas.
- `game-curved-island-controls.ts` / `game-curved-island-panel.ts`:
  callbacks y montaje de los paneles nuevos en el rail.
- `game-playable-scene.ts`: orquestador (paleta, rampa, drop, persistencia).
- `desktop-game-playable.css`: `.juegoConstructor` ventana, `__resize`,
  docks y pickers.
- `roadmap.md`, plan v2 (checklist 138A-8) e índice documental actualizados.

### Gotchas

- `rebuildDocumentProps` requiere `blocks && smooth` ya construidos; el
  documento se setea en el comparador tras su `setOptions` inicial.
- `setPalette` NO regenera el heightfield (solo `rebuildMeshes`); regenerar
  con `setOptions`/`regenerate` vuelve a ejecutar `rebuildDocumentProps`.
- El drop entrega coordenadas de escena; el documento usa el frame local
  (bounds ±w/2·cellSize), así que el handler resta `islandCenterX/Z`.
- `createEl` no acepta `tabindex`/`aria-orientation`: se asignan
  post-creación (`el.tabIndex`, `el.setAttribute`).
- `AssetInstance.position` es readonly: la instancia `add` se construye con
  la posición ya clampada, no se muta después.
- `applyToonRamp` no persiste la textura (deuda documentada); el estado vive
  en la escena y el panel es solo la puerta de entrada.
- La carga de textura usa data URLs (FileReader), no object URLs: no hay
  revocación pendiente.
- `Agente/documentacion/estilo-sakura-crossing/` (bloque 138A-13) se
  incorporó en el commit documental `49049d79`; no forma parte del commit de
  138A-8.

### Sentinel / gate

`npm run gate:check -- 138A-8` **PASS** (incremental, **28 archivos** según
`changed-files.txt`; 4 etapas): sentinel 0 errores/8 warnings, varsense
0 errores/5 warnings/1 info, frontend 0/0/0, docs 0/0/0. Warnings reales de
deuda: `limite-lineas` en `game-playable-scene.ts` (775 líneas efectivas,
ALTO nivel 2), `game-world-constructor.ts` (429) y
`game-procedural-comparator.ts` (356); la extracción hacia módulos queda
registrada para la auditoría 138A-11. `claseHuerfana` ×5: `boton--activo` es
falso positivo (se usa vía `classList.toggle` en `game-map-editor-toolbar.ts`)
y `juegoPanelTerreno--cerrado`/`juegoConstructor--cerrado/-izquierda/-derecha`
también se aplican dinámicamente. `css-adhoc-button-style` ×1 y
`dom-access-outside-platform` ×3 repetidos de bloques previos. `npm run
type-check` PASS y `npm run test:full` **123 archivos / 878 tests PASS**.
Vitest dirigido del alcance del gate: **8 archivos de test / 60 `it(`** (assets
6, color 4, persistence 14, textura 3, comparador 6, world-constructor 15,
map-edits 7, world-palette 5); corrida ampliada de 13 archivos: 90 tests PASS.

Deuda adicional documentada: `rebuildDocumentProps` omite las categorías del
manifiesto sin mesher propio (agua, personajes, genéricos) — solo árboles y
rocas se colocan como props del documento.

### GLORY

- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS —
  AUTORIZADO PARA CONTINUAR**. Reservas aplicadas antes del commit:
  `crossOrigin='anonymous'` + mensaje claro ante imagen CORS no cargable en
  la rampa por URL; debounce de 200 ms real para la paleta (se generalizó
  `createDebouncedRegenerator<T>` reutilizando el patrón 138A-5) y cifras
  documentales exactas del gate (28 archivos, 60 `it(` en los 8 archivos de
  test del alcance). Falsos positivos de `claseHuerfana` verificados y
  documentados; deuda de extracción de líneas registrada para 138A-11.
- Commit explícito 138A-8 (implementación + plan/roadmap/completada) y push a
  `origin/wandorius` según autorización del usuario ("termina todo lo
  pendiente y subelos").
- Pendiente del usuario: validación visual en `/forest-playable` (ventana
  colapsable/redimensionable, pickers de Color, rampa de Textura y
  arrastrar/soltar de Assets).

## 138A-9 — Editor de mapa por capas (visor + pinceles), cámara libre/primera
con bounds del constructor y assets grid

**Contexto (usuario, 14-ago, noche):** "necesito que planifiquemos un
constructor de mundo" con ajustes al plan: el editor de mapa debe ser un
**visor de capas tipo Blender** (nunca un colocador de assets sobre el mapa),
el panel de Assets debe ser un **explorador en cuadrícula con miniatura**, la
cámara libre debe ser vuelo libre real, la primera persona debe ocultar la
figura local y no debe quedar uno "encerrado en un chunk" cuando el terreno
del constructor es más grande que el fixture. Se incorporan las referencias
Contour Terrain Editor (capas del terreno), Skydome (138A-12) y
GrassSystemThreeJS (138A-10) al plan.

### Qué

1. **Módulo de capas puro** (`game-core/terrain-layers.ts`): stack
   serializable de capas (superficies camino/arena/agua y elevación), SDF de
   forma (círculo/polígono/pintada), falloff con curva elegible, blend
   `set/add/max/min` y taper; cuotas fail-closed (32 capas, 64 puntos,
   16 384 celdas pintadas, 4 096 por sesión); `validate/normalize` descartan
   campos desconocidos, ids duplicados y valores fuera de rango;
   `applyTerrainLayerStack` devuelve heights + surfaces; `mergePaintedCells`
   acota al límite. Re-export en `game-core/index.ts`.
2. **Pipeline del mundo** (`map-builder.ts`):
   `buildMapVersionFromOptions(value, mapId, layers?)` aplica el stack SIEMPRE
   sobre la base generada; `SerializedWorld.layers` opcional (roundtrip
   export/import); los `surfaces` de los chunks vienen del stack.
   `heightfield-mesh.ts` + `world-palette.ts`: `surfaces`/`surfaceColors`
   colorean los vértices por superficie cuando el stack pintó.
3. **Comparador por estilo** (`game-procedural-comparator.ts`): `setLayers`
   reaplica el stack sobre la base en cada regeneración; la vegetación
   generada solo crece sobre hierba cuando hay superficies (paridad con
   map-builder); los props del documento se renderizan según el estilo
   (`bloques` → `BlockPropPlacement` del mesher, `suave` → low-poly) →
   **fix del bug de recarga**: una sola fuente de props, sin duplicar
   vegetación ni pintar low-poly sobre bloques.
4. **Pinceles** (`game-layer-brush.ts` + `game-layer-painter.ts`): estado de
   pincel tipado (`DEFAULT_BRUSH_STATE`, `normalizeBrushState` fail-closed),
   trazos pointer → celdas del comparador con throttle 120 ms, cuota por
   sesión y teardown completo (listeners + pointer capture).
5. **Visor de capas** (`game-layer-editor.ts` + sección "Capas" del rail en
   `game-curved-island-panel.ts`): lista del stack tipo Blender con ojo de
   visibilidad, orden, duplicar/eliminar/añadir y pincel; nunca coloca assets
   sobre el mapa.
6. **Orquestador** (`game-playable-scene.ts` + `game-world-constructor.ts`):
   `ConstructorPanelContext` con `layers/commitLayers/syncLayers`,
   `brush/commitBrush/syncBrush`; `applyConstructorLayers`/
   `applyConstructorBrush`/`applyBrushStroke`. **Fix de wiring:** `commitLayers`
   y `commitBrush` ahora ejecutan sus syncers y `applyBrush` re-renderiza el
   visor (bug real destapado por los tests). Persistencia del stack en
   `game-constructor-persistence.ts` (roundtrip; capas inválidas omitidas) y
   export/import JSON.
7. **Cámara libre/primera y bounds** (`game-camera-controls.ts` + escena):
   `libre` = vuelo libre desacoplado del personaje (WASD/arrows + Space/Shift,
   arrastre gira la mirada, guard `isEditableTarget`); `primera` = ojos del
   personaje ocultando la figura local; `tercera` = órbita. `activeCameraBounds`
   usa los bounds del MapVersion del constructor (+ offset de la isla) cuando
   el comparador está activo → **fix de "encerrado en un chunk"**.
8. **Assets grid** (`game-constructor-assets.ts` + CSS): explorador en
   cuadrícula con miniatura 2D por asset (glifo monocromo por categoría
   adaptado a bloques/suave; canvas 2D opcional en jsdom), tarjetas
   arrastrables al mundo, recuento por asset y quitar por asset/categoría o
   instancia individual.
9. **Refactor de la escena para el gate** (`game-playable-scene.ts` de 1041 a
   859 líneas efectivas): extracción a `game-camera-controls.ts`,
   `game-scene-utils.ts` (toon ramp + dispose), `game-world-io.ts` (export
   JSON) y `game-scene-gpu-estimate.ts`; desaparece el error nivel-3 de
   `limite-lineas` (queda warning ALTO nivel-2 para 138A-11).

### Archivos

- `frontend/src/features/game-core/terrain-layers.ts` (+test), `map-builder.ts`
  (+test), `procedural/heightfield-mesh.ts`, `world-palette.ts`, `index.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-layer-brush.ts`
  (+test), `game-layer-painter.ts` (+test), `game-layer-editor.ts` (+test).
- `game-procedural-comparator.ts` (+test), `game-world-constructor.ts`,
  `game-curved-island-panel.ts`, `game-constructor-assets.ts` (+test),
  `game-constructor-persistence.ts` (+test), `game-constructor-color.test.ts`,
  `game-constructor-texture.test.ts`.
- `game-camera-controls.ts`, `game-scene-utils.ts`, `game-world-io.ts`,
  `game-scene-gpu-estimate.ts` (nuevos).
- `game-playable-scene.ts`, `desktop-game-playable.css`, `roadmap.md` y plan
  v2 (checklist 138A-9).

### Gotchas

- `mergePaintedCells` firma con `limit: number = TERRAIN_LAYER_LIMITS.maxPaintedCells`
  (el literal 16384 rompía el tipo del test de límite).
- Los props de bloque del documento viven en frame local de celdas:
  `rebuildDocumentProps` divide por `cellSize` y resta `centerX/Z` antes de
  `buildBlockPropsMeshData`; en suave se usan coordenadas de mundo.
- El stack se reaplica SIEMPRE sobre la base generada (no es mutación
  destructiva); `currentHeightfield` queda como la base editada y
  `pick/groundHeight` reflejan las capas.
- `drawAssetThumbnail` usa canvas 2D y es opcional en jsdom (sin canvas se
  omite la miniatura, no el panel).
- `test:full` (vitest secuencial) mostró 1 fallo flaky en `map-edits.test.ts`
  (archivo NO tocado en este bloque): pasó aislado y en la rerun completa
  (127 archivos / 927 tests PASS); no es regresión del bloque.

### Sentinel / gate

`npm run gate:check -- 138A-9` **PASS** (incremental, **32 archivos** según
`changed-files.txt`; 4 etapas): sentinel 0 errores/13 warnings/6 info, varsense
0 errores/5 warnings/1 info, frontend 0/0/0, docs 0/0/0. Deuda documentada
para 138A-11: `limite-lineas` en `game-playable-scene.ts` (859 efectivas,
ALTO nivel-2, ya sin error nivel-3), `game-world-constructor.ts` (476),
`game-procedural-comparator.ts` (408), `game-layer-editor.ts` (370),
`terrain-layers.ts` (593) y CSS (640); `claseHuerfana` ×5 (dinámicas;
`boton--activo` preexistente), `dom-access` ×3 (scene y world-io),
`window-reference` ×2, `css-adhoc-button-style` ×1 y `non-null-assertion` ×6
(info, en tests). `npm run type-check` PASS; suite completa `test:full`
**127 archivos / 927 tests PASS** (con el flake documentado arriba); Vitest
del alcance del gate: **10 archivos de test / 92 `it(`** (terrain-layers 14,
map-builder 14, persistence 16, brush 13, comparador 9, editor 7, assets 6,
painter 6, color 4, textura 3).

### GLORY

- Revisión `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS** —
  reservas de rendimiento diferidas a 138A-11 (el commit intermedio del pincel
  dispara full-rebuild del comparador; `containsCell` O(n) → Set/hash), no
  bloqueantes; confirmó que el pendiente CORS de 138A-8 quedó corregido
  (`crossOrigin='anonymous'` en `onToonRampChange`).
- Veredicto `sentinel_inspector` (`inspector_138a4`): **OK CON OBSERVACIONES** —
  gate usado correctamente (PASS con evidencia en
  `.quality-reports/check/138A-9/`, sin `--allow-heavy` ni silenciamientos,
  doctor previo OK y rama `wandorius` = primaryBranch); observaciones
  aplicadas en esta pasada final: re-run del gate con el estado documental
  completo (la completada entra en el scope) y redacción del cierre corregida
  (HEAD ya estaba sincronizado con `origin/wandorius`; no había commits
  previos pendientes de push).
- Commit explícito 138A-9 y push a `origin/wandorius` según autorización del
  usuario ("termina todo lo pendiente y subelos"); el push lleva únicamente
  el commit de este bloque (138A-8 y docs 138A-13 ya están en origin).
- Pendiente del usuario: validación visual en `/forest-playable` (visor de
  capas, pinceles suave/bloques, vuelo libre con WASD, primera persona sin
  figura, assets grid con miniaturas y persistencia tras recargar).

## 138A-10 — Generador de pasto optimizado y pintado

**Contexto (decisión del usuario, 14-ago):** el usuario pidió el generador de
pasto con densidad/tamaño/color elegibles, pincel para poner y quitar pasto,
lo más óptimo posible, y añadió `GrassSystemThreeJS` (Soil Studio) como
referencia de técnica con la advertencia de que "no está muy bien
optimizado", por lo que la adaptación es estrictamente orientada a
rendimiento (presupuestos, chunking y teardown; no copiar el mesh global sin
límites del repo).

### Qué

1. **Pipeline puro `grass-field.ts`** (`game-core/procedural/`, sin
   Three/DOM/red): `buildGrassField` divide el grid en chunks de `chunkSize`
   celdas y produce una lista de briznas instanciadas por chunk con
   determinismo por seed; presupuestos **fail-closed** `GRASS_FIELD_LIMITS`
   (≤1024 chunks, ≤10000 briznas, tamaño 0.25..4); máscara de vegetación
   `Int8Array` (1 fuerza pasto, -1 lo prohíbe) y superficie hierba por
   defecto; `affectedChunksForCells`/`chunkFilter` para regenerar solo la
   zona pintada; `validate/normalizeGrassFieldOptions` (defaults ante campos
   ausentes, throw ante inválidos).
2. **Capa de vegetación del stack de 138A-9** (`terrain-layers.ts`):
   `TerrainLayerKind 'vegetation'` con `mode: 'add'|'remove'` y
   `TerrainLayerStackResult.vegetationMask`; validación actualizada.
3. **Comparador** (`game-procedural-comparator.ts`): `grassGroup` dentro del
   grupo suave, un `InstancedMesh` por chunk con `MeshToonMaterial`
   `gradientMap` + vertexColors y `bend.apply()`; `rebuildGrass(filter?)`
   regenera solo la zona afectada; `setGrassOptions`; `clearGrassMeshes`
   solo `mesh.dispose()` (la geometría compartida se libera UNA vez en
   `dispose`, antes de `disposeBuiltMode`); stats `grassChunks/grassBlades`.
4. **Pincel poner/quitar pasto** (`game-layer-brush.ts` +
   `game-layer-editor.ts`): `ConstructorBrushKind 'grass'`, `mode` en el
   estado y segmento Poner/Quitar en la UI; `terrainLayerKindOfBrush`
   mapea grass→vegetation; `surfaceIdOfKind` grass → -1.
5. **Panel y persistencia** (`game-constructor-grass.ts` +
   `game-world-constructor.ts` + `game-curved-island-panel.ts` +
   `game-constructor-persistence.ts`): subpanel Pasto (icono `Leaf`,
   enabled + densidad 0..100% + tamaño 0.25..4 + color) con commit debounced
   200 ms en `game-playable-scene.ts`; estado `grass` opcional en
   `localStorage` (fail-closed: inválido se omite sin bloquear el resto) y
   restauración al recargar.
6. **Referencia de diseño incorporada al plan**: sección
   "GrassSystemThreeJS / Soil Studio" con la adaptación a presupuestos del
   proyecto (una draw call por chunk, sin viento en shader → deuda anotada a
   138A-11).

### Archivos

- Nuevos: `game-core/procedural/grass-field.ts` (+ test), panel
  `game-constructor-grass.ts`.
- Modificados: `terrain-layers.ts`, `procedural/index.ts`,
  `game-procedural-comparator.ts` (+ test), `game-layer-brush.ts` (+ test),
  `game-layer-editor.ts` (+ test), `game-constructor-persistence.ts`
  (+ test), `game-curved-island-panel.ts`, `game-world-constructor.ts`,
  `game-playable-scene.ts` y stubs de contexto de tests ajenos
  (color/textura/assets) por el contrato nuevo del pincel.
- Documentales: plan v2 (estado, checklist y referencia), `roadmap.md` y esta
  completada.

### Gotchas

- Geometría de pasto compartida: en rebuilds/dispose solo se llama
  `mesh.dispose()` sobre cada `InstancedMesh`; `grassGeometry.dispose()` se
  ejecuta UNA vez en `dispose()` del comparador y `clearGrassMeshes()` debe
  correr ANTES de `disposeBuiltMode`.
- `buildGrassClumpMeshData` devuelve `VegetationMeshData` sin uvs → el
  comparador usa `toIndexedGeometry`, nunca `toGeometry`.
- `normalizeGrassFieldOptions` acepta `undefined/null` → defaults (los
  `(value ?? {})` previos eran código muerto: la validación rechazaba
  no-objetos antes de normalizar).
- El grid supera la cuota si `width*depth > maxChunks*chunkSize²`: con el
  preset 48×32 hacen falta ≥6 chunks, no 2 (test de presupuesto corregido
  con `maxChunks: 6`).
- Fix del pasto fantasma (exigencia del reviewer): `setLayers` guarda
  `lastLayers` y, al regenerar solo vegetación, une los chunks de las capas
  previas y actuales (`previousChunks ∪ currentChunks`); sin esa unión, quitar
  o deshabilitar una capa dejaba su mesh en el chunk.

### Sentinel / gate

- `npm run type-check` PASS; suite completa `npm run test:full` **128 archivos
  / 950 tests PASS** (incluye 14 tests nuevos de `grass-field.test.ts`, casos
  grass en brush/editor/persistence, el test del comparador de regeneración
  por chunks y el test de retirada del pasto al eliminar su capa). Gate
  `npm run gate:check -- 138A-10` re-corrido con el estado final; veredictos
  de inspector/revisor en GLORY.

### GLORY

- Commit explícito 138A-10 y push a `origin/wandorius` según autorización del
  usuario ("termina todo lo pendiente y subelos").
- Veredicto `sentinel_inspector` (`inspector_138a4`): **OK CON
  OBSERVACIONES** — gate re-corrido con el estado documental final antes del
  commit (el fileHash del reporte anterior ya no era fiel).
- Veredicto `supervisor_reviewer` (`revisor_138a4`): **APROBADO CON RESERVAS**
  — bug IMPORTANTE de pasto fantasma corregido (unión de chunks previos y
  actuales en `setLayers` al regenerar vegetación) con test de regresión
  añadido; reservas menores anotadas como deuda 138A-11 (cuota global de
  instancias no acumulada en pasadas filtradas, doble regeneración del pasto
  en `showConstructorWorld` y césped en estilo bloques como pregunta abierta
  al usuario).
- Pendiente del usuario: validación visual en `/forest-playable` del pasto
  (densidad/tamaño/color en tiempo real, pincel poner/quitar y persistencia
  al recargar).

## 138A-13 — Guía del estilo Sakura Crossing (documentación)

**Contexto (decisión del usuario, 14-ago):** el usuario pidió *"necesito
entender a la perfección todo, con ejemplos código"* sobre cómo el juego de
referencia
[Kenton-GMI/sakura-crossing](https://github.com/Kenton-GMI/sakura-crossing)
logra su estilo anime/cel, para forzarlo sobre los assets existentes de su
constructor de terrenos/mundos en JS (`/forest-playable`) sin rehacer los
assets: materiales → toon/cel, iluminación cálida/fría, sombras teñidas a
violeta, outlines, color grading y paleta. Autorizó descargar el repositorio
de referencia y revisar el código actual del juego (en progreso).

### Qué

- Clon de solo lectura del repo de referencia en `C:\tmp\sakura-crossing`
  (fuera del repo WANDORIUS, no se commitea); dependencias compatibles con el
  proyecto (three ^0.180 vs 0.185.1 del consumidor, vite 6).
- Análisis del pipeline visual real del clon con rutas y líneas exactas: cel
  shading con rampa y tinte violeta (`toon.js`), luces anime 2+1 con sombras
  PCF 2048 (`main.js`, `seatLight`), outlines ink + hull en dos pasadas
  (`post.js`, `outline.js`), color grading split-tone con FXAA (`post.js`),
  paleta y texturas procedurales Canvas2D (`palette.js`, `textures.js`) y
  cielo pintado con nubes billboard (`sky.js`).
- **8 guías + índice** en `Agente/documentacion/estilo-sakura-crossing/`
  (00..08) con código real GLSL/JS, presupuestos de rendimiento y referencias
  a los archivos WANDORIUS donde aterrizar cada técnica.
- Guía de replicación sobre el constructor actual (08): gap analysis (sin
  shadow map, sin postprocesado, sin tinte violeta en el material), 6 pasos
  concretos con archivos, tabla de equivalencias de paleta, presupuestos y
  tests/teardown a respetar.
- **Nota de tensión:** el roadmap 13-ago decidió "Genshin-like low poly verde
  stylized, **sin tinta como destino**" para el Bosque. La carpeta es
  referencia/investigación y no cambia la decisión visual vigente hasta que el
  usuario la revierta.

### Archivos

- `Agente/documentacion/estilo-sakura-crossing/00-indice-pipeline.md`
- `Agente/documentacion/estilo-sakura-crossing/01-analisis-sakura-crossing.md`
- `Agente/documentacion/estilo-sakura-crossing/02-materiales-toon-cel.md`
- `Agente/documentacion/estilo-sakura-crossing/03-paleta-texturas-procedurales.md`
- `Agente/documentacion/estilo-sakura-crossing/04-iluminacion-sombras.md`
- `Agente/documentacion/estilo-sakura-crossing/05-outlines-tinta.md`
- `Agente/documentacion/estilo-sakura-crossing/06-color-grading-postprocesado.md`
- `Agente/documentacion/estilo-sakura-crossing/07-cielo-atmosfera.md`
- `Agente/documentacion/estilo-sakura-crossing/08-replicacion-constructor-wandorius.md`
- `roadmap.md` (entrada 138A-13), `Agente/documentacion/indice-documentacion-2026-07-29.md`
  (fila y estado de consolidación) y enlace desde el plan v2
  (`plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md`).

### Gotchas

- La copa de los árboles usa `receiveShadow=false` a propósito: los tonos
  blob de la copa no deben oscurecerse por la sombra del sol (mantiene el look
  plano); solo el suelo y objetos bajos reciben sombra.
- El tinte violeta de sombras no se logra con luces de color: es un parche al
  `uShadowTint` del shadow map en el shader toon (función `applyShadowTint`).
- `NOTIFYICON`-style gotcha no aplica aquí; los gotchas reales son de Three:
  el pipeline de postprocesado es una clase `Pipeline` propia sobre
  `FullScreenQuad` + `WebGLRenderTarget` (no `EffectComposer`), con orden
  `rtScene → ink → grade → fxaa`; `setSize` re-crea targets para no perder
  resolución en resize.
- `three ^0.180` del clon vs `0.185.1` del consumidor: API estable en lo
  usado (ShaderMaterial, FullScreenQuad, `PCFShadowMap`); verificar al
  portar, no copiar ciegamente.
- La escena del constructor no tiene shadow map ni postprocesado hoy; el paso
  1 de la replicación es activarlos antes de tocar materiales (gap más grande
  del análisis).

### Sentinel / gate

- `npm run gate:check -- 138A-13 --profile docs`: **PASS** — evidencia en
  `.quality-reports/check/138A-13/` (sentinel PASS y docs PASS, 32 archivos
  de alcance incremental). Es el stack afectado por esta tarea: 9 MD +
  roadmap + índice + completada + plan; sin cambios de código fuente. Rama
  `wandorius`; Sentinel 0.7.4 y VarSense 2.2.1 fijados (sin cambios).
- `npm run gate:check -- 138A-13` (incremental completo, sin perfil):
  **falla en la etapa `frontend` por 4 errores TS en código ajeno sin
  commitear de 138A-8..12** (constructor en curso), no de esta tarea:
  `game-world-constructor.test.ts(298,29)` `Property 'sky' does not exist on
  type 'never'`, `game-world-constructor.ts(205,5)` y `(250,5)` `'tabindex'` /
  `'aria-orientation'` no existen en `DomAttrs`, y `map-edits.ts(69,16)`
  `Cannot assign to 'position'` (read-only). Verificado: `map-edits.ts` no
  existe en HEAD y `tabindex`/`aria-orientation` tampoco; son cambios ajenos
  en progreso. Sentinel/varsense/docs pasan en esa corrida; el fallo se
  documenta, no se silencia ni se arregla dentro de este bloque documental.

### GLORY

- Commit explícito en español de la documentación (sin push; no hay
  autorización de push en este turno). Los cambios de código fuente en el
  worktree pertenecen a 138A-8..12 y no se tocan ni se incluyen.
- Veredicto `sentinel_inspector` (`inspector_138a13`): **OK** — gate usado
  correctamente (`--profile docs` real en `gate-check.mjs:28`), fallo ajeno
  del gate completo documentado en `frontend.json` con los 4 errores TS y no
  silenciado; sin acciones.
- Revisión `supervisor_reviewer` (`revisor_138a13`): **APROBADO CON RESERVAS
  MENORES, AUTORIZADO PARA CONTINUAR** — 3 hallazgos corregidos antes del
  commit: (1) cita de `EffectComposer`/`PCFSoftShadowMap` en el Gotcha de la
  completada → `FullScreenQuad` + clase `Pipeline` propia y `PCFShadowMap`;
  (2) estado de cierre reconciliado entre roadmap y completada con los
  veredictos reales; (3) "ni un solo asset binario" acotado a "cero assets
  binarios visuales" en `00-indice`, `01-analisis` y `03-paleta` (el clon sí
  tiene audio `.mp3` y JPGs de docs). Citas técnicas del clon verificadas
  contra `toon.js`, `post.js`, `outline.js`, `sky.js`, `palette.js`,
  `textures.js`, `main.js` y `trees.js`; gap analysis verificado contra el
  constructor real (sin shadow map/post/tinte, rampa 4 bandas, hemi/sun/rim
  1.0/1.2/0.4, teardown con `forceContextLoss`).
- Pendiente del usuario: revisar la carpeta y decidir si revierte la decisión
  "sin tinta" para probar el estilo tipo Sakura Crossing sobre `/forest-playable`.

## 138A-11 — Auditorías SOLID/arquitectura y rendimiento del constructor

**Contexto (decisión del usuario, 14-ago):** "planifica una auditoría de
principios solid y arquitectura a todo lo relacionado con el juego" y
"planifica una auditoría pensada solo en el rendimiento". Bloque del plan
v2 (`plan-constructor-mundo-v2-toolkit-edicion-2026-08-14.md`), después de
138A-10.

### Qué

1. **Fix — doble rebuild del pasto:** la escena llamaba `setGrassOptions`
   (rebuild de pasto) y `regenerateFromOptions` (rebuild completo) seguidos;
   `regenerateFromOptions` acepta ahora `(options, grass?)` y
   `showConstructorWorld` regenera una sola vez. Test con spy de
   `buildGrassField` (1 llamada por cambio, usa el pasto nuevo).
2. **Fix — cuota global de briznas:** `rebuildGrass` concedía 10000 a cada
   pasada filtrada (pincel); ahora reparte `remaining = 10000 − conservadas`
   como `maxInstances` y sin cupo no genera (la retirada ya liberó sus
   meshes). Test: la 2ª pasada filtrada recibe `10000 − kept` y el total
   nunca supera el presupuesto.
3. **Fix — colisión de 3ª persona por segmento:** antes solo se muestreaba el
   punto de la cámara y se hundía en colinas intermedias; nuevo helper puro
   `resolveThirdPersonCollision(camera, cameraTarget, groundHeightAt,
   clearance, samples)` en `game-camera-controls.ts` (muestreo del tramo
   jugador→cámara, eleva al máximo suelo+despeje) usado por `updateCamera`.
   Tests unitarios con `groundHeightAt` inyectado.
4. **Fix — validación cruzada del import:** `parseSerializedWorld` validaba
   opciones y MapVersion por separado; nueva `assertWorldMatchesOptions`
   (`map-builder.ts`) exige bounds = ±width×cellSize/2, mismo cellSize y
   chunks completos 0..chunksX/Z. Tests negativos (bounds, cellSize,
   opciones de otro mundo, chunks faltantes/duplicados).
5. **Benchmark reproducible:** test en `map-builder.test.ts` genera 25 mundos
   (4 shapes × 8 seeds × 5 tamaños 48..256) y verifica presupuestos
   (chunks ≤1024, instancias ≤10000, triángulos ≤524288) con cotas de tiempo
   generosas. En esta máquina: media 15.83 ms, máx 46 ms, mundo mayor 256×256
   (256 chunks, 131 072 triángulos).
6. **Ciclo de vida GPU (test nuevo):** geometrías/materiales vivos por
   `scene.traverse` antes/después de 8 regeneraciones (mismo número, sin
   acumulación), geometría de un mesh anterior liberada al regenerar
   (`spyOn(dispose)`) y escena vacía tras `dispose()`.
7. **Informes:** `auditoria-solid-constructor-mundo-2026-08-14.md` y
   `auditoria-rendimiento-constructor-mundo-2026-08-14.md` con hallazgos por
   módulo, fixes, deuda y presupuestos.

### Archivos

- `frontend/src/features/desktop/apps/game-playable/game-procedural-comparator.ts`
  (+ test): contrato `regenerateFromOptions(options, grass?)`, cuota global y
  `EMPTY_GRASS_FIELD`.
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts`:
  una sola regeneración en `showConstructorWorld`; colisión de cámara por
  segmento.
- `frontend/src/features/desktop/apps/game-playable/game-camera-controls.ts`
  (+ test nuevo): `resolveThirdPersonCollision`.
- `frontend/src/features/game-core/map-builder.ts` (+ test): validación
  cruzada del import y benchmark.
- `Agente/documentacion/arquitectura/auditoria-solid-constructor-mundo-2026-08-14.md`,
  `auditoria-rendimiento-constructor-mundo-2026-08-14.md`.

### Gotchas

- `buildGrassField` es fail-closed con `maxInstances ≤ 0` (lanza): sin cupo
  se devuelve `EMPTY_GRASS_FIELD` en vez de llamar con 0.
- El pasto comparte geometría entre chunks: en rebuilds solo
  `mesh.dispose()` de los InstancedMesh; `grassGeometry.dispose()` UNA vez en
  `dispose()` y `clearGrassMeshes()` ANTES de `disposeBuiltMode`.
- En los tests del comparador, el grupo suave actual se localiza con
  `[...world.children].reverse().find(...)` (quedan grupos antiguos vaciados
  en el árbol); el pasto es el índice 2 del grupo.
- Vitest debe correrse desde `frontend/` (config `src/**/*.test.ts`); desde la
  raíz arrastra los worktrees de `.sentinel/` y falla en tests ajenos.
- `assertValidMapVersion` ya rechaza cellSize incoherente y chunks duplicados
  ANTES de la validación cruzada; los tests lo documentan (rechazo = objetivo).

### Sentinel / gate

- `npm run gate:check -- 138A-11`: **PASS** — 0 errores; 8 warnings
  documentados (límites de líneas de la escena 874 efectivas y del test del
  comparador 479, DOM/window fuera del boundary) y 6 info (non-null
  assertions) en `.quality-reports/check/138A-11/`. `npx tsc --noEmit`
  limpio y `npm run test:full` PASS (129 archivos, 959 tests).

### GLORY

- Commit explícito con ID (`e1a06be2`) y push a `origin/wandorius`
  (autorizado por el usuario: "termina todo lo pendiente y subelos").
- Veredicto `supervisor_reviewer` (14-ago): **APROBADO CON OBSERVACIONES** —
  evidencia del gate (PASS, 0 errores), diff completo y tests revisados; sin
  defectos bloqueantes. Observaciones menores no bloqueantes: muestreo de
  colisión 3ª persona con 9 puntos (colina más angosta que el espaciado puede
  atravesarse; se eleva pero no se clampa horizontal — validación visual final
  del usuario), mensaje genérico al rechazar un save legacy en la UI,
  benchmark con cotas CI generosas (medición real documentada) y deuda de
  líneas ya ticketizada. Prevención candidata si la colisión reaparece en la
  validación: "la colisión de cámara debe muestrear segmento, no punto"
  (`Agente/prevencion/`). Autoriza continuar a 138A-12 cuando el usuario lo
  pida.
- Pendiente del usuario: validación visual en `/forest-playable` (colisión de
  3ª persona y pasto) y decidir si el estilo bloques debe mostrar césped
  (pregunta abierta documentada, no se cambió unilateralmente).
