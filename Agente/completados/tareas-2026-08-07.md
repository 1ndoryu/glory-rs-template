# Tareas completadas — 2026-08-07

## SNT-16d — Preflight reproducible y recuperación segura

### Qué
Se incorporó al core upstream un preflight read-only fail-closed y una recuperación explícita de tareas interrumpidas. El gate real ahora verifica el workspace antes de ejecutar stages; `task recover` valida expiración, proceso muerto, namespace, heads registrados y worktree limpio. El modo `--dry-run` no muta y la recuperación real deja auditoría JSON.

### Archivos
- `tools/sentinel/src/core/diagnose.ts`: diagnóstico de submódulo/gitlink, CLI, `--version`, package metadata/dependencias/scripts, capacidades, symlink escapes, checkout/package-lock dirty, commit/versiones y lock.
- `tools/sentinel/src/core/gateRun.ts`: preflight antes de etapas reales.
- `tools/sentinel/src/core/taskRecovery.ts`: recuperación segura, auditoría y snapshots contra carreras de metadata.
- `scripts/quality/setup.mjs`: provisionamiento de CLI faltante mediante staging temporal aislado.
- `tools/sentinel/src/cli/index.ts`: `task recover` y `--dry-run` explícito.
- `tools/sentinel/src/test/suite/taskRecovery.test.ts`: expiración, PID muerto y dry-run.
- `tools/sentinel/src/test/suite/cliContract.test.ts`: contrato CLI.
- `quality-tools.json`, `sentinel.lock.json`: regenerados en el worktree de tarea contra el commit probado.
- `Agente/planes/plan-preflight-recuperacion-sentinel-2026-08-07.md`.
- `Agente/documentacion/herramientas/inventario-scripts-adapters-sentinel-2026-08-06.md`.

### Evidencia
- Commits upstream: `e1493c3` (gate/recovery), `ff0649c` (doctor reforzado) y `8583b41` (SNT-16f completo) sobre el submódulo; el checkout consumidor fija gitlink/config/lock a `8583b41` con los commits `4782c37c` (pin) y `32a1b0f4` (lock). Sin push.
- `tsc -p ./ --noEmit`: PASS.
- Suite upstream disponible: **499 passing, 1 pending**; suite focalizada SNT-16f: **502 passing, 1 pending**.
- `node scripts/quality/lock-generator.mjs --write --json` seguido de `--check --json`: PASS con `8583b41` (gitlink/lock/config coherentes).
- `quality:setup` final: compile + suite en staging aislado PASS para **sentinel (502 passing, 1 pending)** y **varsense (60 passing)**; evidencia `.sentinel/release-evidence/*.json` ligada al commit con `cleanStaging: true`.
- Doctor final: capacidades completas (7/7, `missing: []`), checkouts limpios; issue único `tool-release-unpublished` (`8583b41` no alcanzable desde refs de release). `quality:test` del consumidor: **228 passing, 0 fail, 1 skipped** (incluye integración real `varsense-parity.mjs`).

### Gotchas y rollback
- `npm run compile` del upstream intenta un `quality-command-guard.mjs` que no existe en ese checkout; no se ocultó como PASS. Se validó con `tsc` directo y la suite compilada.
- El setup aislado fuerza `npm_config_script_shell`/entorno limpio (sin `~/bin`, shims GlorySentinel ni `scripts/quality` en PATH, sin `GLORY_*` heredadas) porque el wrapper `~/bin/npm` inyectaba el guard legacy y buscaba `quality-command-guard.mjs` dentro del staging; el staging quedó aislado y compile+suite pasan. En Windows, la extracción de `git archive` exige `--force-local` y rutas con `/` para que bsdtar no trate `C:` como host remoto.
- `npm run quality:test` bloqueó las integraciones mientras el checkout Sentinel estuvo dirty; tras commitear el submódulo (`8583b41`) y re-fijar gitlink/lock, la suite completa pasa (**228 passing, 0 fail, 1 skipped**).
- El commit fijado `8583b41` es local y no publicado; la release pública/rollback del consumidor sigue siendo `20c13a2`/`0.5.0` hasta publicar upstream, validar un clon limpio y completar la matriz multi-proyecto. El doctor mantiene fail-closed por `tool-release-unpublished` (comportamiento diseñado, no oculto).
- Se conservan `scripts/quality`, adapters y scripts de dominio; no hay retirada prematura.
- Rollback: mantener Sentinel `20c13a2`/`0.5.0`, el adapter local y el lock anterior hasta completar release.
