# Tareas completadas — 2026-08-07

## SNT-16d — Preflight reproducible inicial

### Qué
Se diseñó el hardening para evitar fallos tardíos por submódulos no inicializados, CLIs no compilados, checkouts modificados, locks divergentes e interrupciones de agentes. Se implementó en el worktree upstream de SNT-16c un diagnóstico read-only que expone `ready`, problemas codificados y estado por herramienta.

### Archivos
- `tools/sentinel/src/core/diagnose.ts` en el worktree de tarea: sourcePath, CLI, respuesta `--version`, checkout dirty, gitlink, commits configurados/instalados/lock y compatibilidad del campo `commit`.
- `tools/sentinel/src/test/suite/diagnose.test.ts`: fixtures de prerequisitos ausentes y lock divergente.
- `Agente/planes/plan-preflight-recuperacion-sentinel-2026-08-07.md`.
- `Agente/planes/plan-migracion-scripts-adapters-sentinel-2026-08-06.md`.
- `Agente/documentacion/herramientas/inventario-scripts-adapters-sentinel-2026-08-06.md`.
- `roadmap.md`.

### Evidencia
- Commit upstream de tarea: `f1d2c62` — `SNT-16d: diagnosticar prerequisitos del gate`.
- TypeScript compilado sin errores.
- Suite focalizada + CLI/task coordinator: **497 passing, 1 pending**.
- VarSense fue provisionado en el worktree y compiló su CLI; su lock todavía no se puede declarar coherente porque el Sentinel upstream del worktree está sucio durante el desarrollo y el consumidor estable no se actualizó.
- `git diff --check`: PASS en los cambios documentales y del worktree.

### Límites y rollback
- El diagnóstico aún no está conectado al gate real para evitar romper el modo de transición y fixtures legacy; esa es la siguiente corrección.
- `task recover` aún no existe: `task status` sigue siendo diagnóstico y el cleanup exige el flujo seguro existente.
- No se integró `origin/main`, no se creó release/tag y no se modificó el gitlink/lock primario.
- Rollback: conservar Sentinel `20c13a2`/`0.5.0`, `task-check`, adapter y scripts de dominio.
