# Tareas completadas — 2026-08-04

## 028A-8 (tramo 3) — Fijación del upstream y activación del índice persistente en el gate

- **Qué:** se activó el índice persistente de VarSense en el gate real:
  - Merge fast-forward `858ec62`→`11f0932` de la rama `028A-8/persistent-index` al `main` consumido de VarSense (`GLORY_VARSENSE_SOURCE_PATH`); el worktree de desarrollo queda intacto.
  - Recompilación del `dist/` del checkout consumido (`esbuild --minify`); `dist/` está en `.gitignore`, por lo que el lock (basado en `git archive HEAD`) no cambia.
  - `quality-tools.json`: `varsense.commit=11f0932de5e66d88a79fdaa960ca77e5774d0a33` y `capabilities={ filesFrom: true, persistentIndex: true }`.
  - `sentinel.lock.json` regenerado con `npm run quality:lock -- --write` (backup `.bak` conservado) y `--check` PASS.
- **Pruebas:** gate real `task:check -- 028A-8` PASS con `varsenseScope.persistentIndex={ enabled: true, indexDir: <branchCache>/varsense }`; snapshot persistente creado en `.quality-reports/branches/<key>/cache/varsense/varsense-index.json` con 363 entradas; segunda ejecución: `loaded=363, reused=364, reparsed=0` (ningún archivo se re-parseó). El tiempo de varsense sigue ~11 s dominado por el análisis documental de 682 candidatos (los índices globales se conservan para exactitud).
- **Archivos:** `quality-tools.json`, `sentinel.lock.json`, checkout upstream varsense (merge), `roadmap-sentinel.md`, `roadmap.md`, plan 028A-8, `Agente/completados/tareas-2026-08-04.md`.
- **Sentinel/GLORY:** contrato de capacidad versionado en lock; el flag se activa solo con la capacidad declarada.
- **Límites:** selección de dependencias con el índice inverso (ampliar `--files-from` con consumidores), watchers/LSP, índices globales de Sentinel y métricas RSS/p50-p95 siguen pendientes.

## 028A-8 (tramo 2) — Índice persistente de VarSense entre ejecuciones

- **Qué:** persistencia por archivo de los resultados del análisis en el CLI de VarSense (upstream), en rama `028A-8/persistent-index` del repo (`C:/Users/Owner/.../.agent/varsense`), worktree `C:/tmp/varsense-persistent-index`:
  - `src/core/persistentIndex.ts` (nuevo): `PersistentIndexStore` (getEntry/setEntry/removeEntry + stats), `FilePersistentIndexStore` con carga validada por `schemaVersion`+identidad, guardado atómico (`tmp`+rename) y **reconciliación con disco al cargar**: en un proceso nuevo las caches en memoria están vacías, así que cada entrada se valida con `fs.stat` y las de archivos eliminados entre ejecuciones se expulsan (el snapshot no crece sin límite y el índice inverso no referencia consumidores inexistentes). Identidad = `toolVersion + configHash + parserVersion` (`indexIdentity`/`configHashFor` con claves ordenadas). Índice inverso `buildReverseIndex` (token/class → consumidores) y `pathKeyForIndex` normalizado por plataforma.
  - `src/core/classIndexBuilder.ts` y `src/core/variableIndexBuilder.ts`: **store-first** — se calcula el SHA-256 del contenido en disco antes de abrir/parsear; si `stored.hash === hash` se reutiliza la entrada (stats.reused) y el archivo nunca se vuelve a parsear; si cambia, se re-parsea y se actualiza (stats.reparsed). `invalidateFile` y la poda de archivos ausentes también expulsan la entrada persistente (stats.removed).
  - `src/cli/index.ts`: flag `--index-dir <path>` en `scan`/`orphan-classes`/`all`; `createIndexStore` crea `FilePersistentIndexStore` con identidad completa, carga y guarda al finalizar; el JSON del reporte expone `cache: { enabled, identity, loaded, reused, reparsed, removed, entries }`. Sin `--index-dir` el comportamiento es idéntico al previo (retrocompatible con `858ec62`).
  - `package.json` + `scripts/smoke-persistent-index.mjs` (nuevo): smoke end-to-end — run1 construye, run2 reutiliza todo (`reparsed=0`, findings idénticos), run3 re-parsea solo el archivo modificado, run4 expulsa la entrada del archivo borrado (poda por disco) y run5 invalida todo por cambio de config (identidad distinta, `loaded=0`). Fixture `fixtures/persistent-index/`.
  - Gate local: `scripts/quality/adapters/varsense-contract.mjs` añade `--index-dir <cacheRoot>/varsense` y `scope.persistentIndex` **solo cuando la capacidad está declarada**; `policy.mjs` y `lockfile.mjs` validan la clave de capacidad `persistentIndex`; test nuevo en `varsense-contract.test.mjs`. La capacidad NO está activa en `quality-tools.json` (el checkout fijado `858ec62` no soporta el flag), por lo que el gate sigue verde con el upstream actual.
- **Pruebas:** upstream — `tsc`, `compile:tests` (esbuild), `eslint` (0 problemas), `check:core` (sin vscode en core), `smoke:lsp` y `smoke:persistent-index` PASS. Gate — `npm run quality:test` **133/133 PASS**. Gate real `task:check -- 028A-8 --profile docs` PASS (sin `--index-dir` porque la capacidad no está fijada, como se diseñó).
- **Archivos:** upstream `src/core/{persistentIndex,classIndexBuilder,variableIndexBuilder}.ts`, `src/cli/index.ts`, `scripts/smoke-persistent-index.mjs`, `fixtures/persistent-index/`, `package.json`; gate `scripts/quality/adapters/varsense-contract.mjs`, `policy.mjs`, `lockfile.mjs`, `tests/varsense-contract.test.mjs`. Commit upstream: `11f0932` en `028A-8/persistent-index` (el `main` consumido queda en `858ec62` intacto).
- **Sentinel/GLORY:** contrato CLI agnóstico del analizador (editor-agnostic, sin vscode); la identidad de cache y la política de invalidación viven en VarSense; el gate solo pasa el directorio por rama. Revisión: 3 hallazgos atendidos — poda de entradas obsoletas entre ejecuciones (stat al cargar), documentación del bump obligatorio de `PARSER_VERSION` al cambiar semántica de parseo/extracción, y nota de que el store es solo CLI (hash de disco; no conectar a LSP con documentos en memoria).
- **Límites:** falta sync/fijación del upstream para activar `capabilities.persistentIndex` en `quality-tools.json` y regenerar el lock; selección de dependencias con el índice inverso (ampliar `--files-from` con consumidores); watchers/LSP; índices globales de Sentinel; métricas RSS/p50-p95.

## 028A-8 (tramo 1) — Alcance efectivo honesto y manifiesto compartido

- **Qué:** se corrigió la degradación full→local-light del quality gate y se generó un único manifiesto de alcance compartido entre etapas:
  - `scripts/quality/scope.mjs`: `resolveFullDecision` puro separa `requestedFull` (--full/--ci), `automaticFull` (fullPatterns/migraciones/config), `effectiveFull` (lo que realmente se ejecuta) y `heavyDeferred` (guard). Un full diferido ya no vuelve a ser full por `automaticFull`. Se registran `deletedFiles` y se escribe `scope-manifest.json` (schemaVersion 1, cambiados, eliminados, hashes SHA-256 de contenido, perfiles, dependencias locales, decisión full y motivo) de forma atómica; `changed-files.txt` se conserva como transporte plano compatible con el contrato `--files-from` de Sentinel/VarSense.
  - `scripts/quality/task-check.mjs`: el lease pesado se solicita cuando `scope.effectiveFull && runsAllStages && !args.heavyDeferred`, incluyendo automaticFull (antes solo con --full/--ci); si el guard bloquea, se re-detecta el alcance con `heavyDeferred` y `effectiveFull=false` real.
  - `scripts/quality/cache.mjs`: fingerprint v5 — el modo usa `scope.effectiveFull` (un automaticFull permitido queda como full aunque `context.full` sea false; un full diferido queda local-light) y marca borrados/renombres.
  - `scripts/quality/reporter.mjs`: `formatScope` muestra el motivo (`full · ejecución incremental (heavy-deferred)`); el reporte JSON expone `requestedFull`, `automaticFull`, `effectiveFull`, `fullReason`, `heavyDeferred` y `deletedFiles` sin romper el schema público.
  - `scripts/quality/adapters/custom.mjs` y `adapters/rust.mjs`: consumen el alcance efectivo (custom escopeado en full diferido; rust con clippy/test solo cuando `effectiveFull`).
  - `scripts/quality/run-frontend-tests.mjs`: acepta `--scope-manifest <path>` para reutilizar el manifiesto del gate y no repetir descubrimientos Git/glob.
- **Pruebas:** `npm run quality:test` **131/131 PASS** (nuevos tests: matriz de `resolveFullDecision`, cache por alcance efectivo, reporter con full diferido y motivo, rust con alcance efectivo). `node --check` de los módulos modificados PASS.
- **Gate real:** `npm run task:check -- 028A-8` con los mains externos fijados → el automaticFull (edición de `scripts/quality/`) intentó el lease, el guard lo difirió y el reporte quedó `Scope: full · ejecución incremental (heavy-deferred) · 24 archivos`; sentinel/varsense/custom/docs PASS y VarSense aplicó `--files-from` (`requestedScopedAnalysis: true, applied: true`). El stage frontend quedó en ERROR por `frontend/src/api/generated` ausente en este checkout (TS2307 en 4 services; preexistente, sin relación con el tramo).
- **Archivos:** `scripts/quality/{scope,task-check,cache,reporter,stage-definitions,run-frontend-tests}.mjs`, `scripts/quality/adapters/{custom,rust}.mjs`, tests `{scope,cache,reporter,rust-adapter}.test.mjs`, `roadmap.md`, `roadmap-sentinel.md`, plan 028A-8.
- **Sentinel/GLORY:** contrato local del orquestador agnóstico; sin reglas nuevas ni cambios en el core de Sentinel/VarSense.
- **Límites:** índices persistentes de VarSense entre ejecuciones, watchers/LSP, índices globales de Sentinel, métricas por etapa (RSS/CPU/cache hit) y presupuestos p50/p95 quedan para los siguientes tramos de 028A-8. El `scope-manifest.json` se escribe dentro de `.quality-reports/branches/<branch-key>/<task-id>/`, por lo que queda cubierto por la retención por rama.

## 297A-67 — Pincel de altura del Editor de mapa (vértices compartidos)

- **Qué:** el editor 2D del Bosque añade la tool `height`: `TERRAIN_HEIGHT_VALUES` discretos allowlisted (0/0.25/0.5/1/1.5/2/2.5/3/4, no negativos, dentro del rango del contrato) con `isAllowedHeight` fail-closed y `activeHeight` en el estado. `terrainVertexAt` resuelve mundo → vértice de la malla (chunkSize+1)² devolviendo refs a TODOS los chunks que comparten el vértice (interior, borde X/Z o esquina de 4), fail-closed fuera de bounds. `paintHeight` actualiza el vértice en todos los chunks compartidos (bordes sincronizados, sin descuadres) y commitea solo si algo cambia (arrastre limpio). El canvas sombrea las celdas por el promedio de sus 4 vértices y marca los puntos de vértice solo con la tool altura; el toolbar expone el botón "altura" y un select de nivel.
- **Archivos:** `frontend/src/features/desktop/apps/game-playable/game-map-editor-height.ts` (nuevo, core puro), `game-map-editor-height.test.ts` (12 tests), `game-map-editor-core.ts` (tool, valores, `activeHeight`, export `commit`), `game-map-editor-canvas.ts` (sombreado/puntos), `game-map-editor-toolbar.ts` (botón+select), `game-map-editor.ts` (wiring pointer/select), `desktop-game-playable.css` (clase compartida del select), `game-map-editor.test.ts` (asserts de vista).
- **Gate:** `task:check -- 297A-67` PASS en modo full/heavy-deferred (sentinel, varsense, type-check frontend, custom, docs). 12/12 tests nuevos + 4/4 de vista PASS; 84/88 de la selección (4 fallos de `game-map-editor-core.test.ts` son PREEXISTENTES del WIP ajeno 038A-3: el fixture `game-fixture-map.ts` cambió `terrainSurfaces` tras el cierre de 297A-66 y los asserts de superficie asumen la celda (0,0) en suelo; se verificó que fallan también contra HEAD sin mis cambios).
- **Entorno reparado:** `frontend/node_modules` estaba incompleto (faltaba vitest) → `npm install` restauró devDependencies (no cambió package-lock). Los clientes Orval de `frontend/src/api/generated/` estaban ausentes (fallo preexistente TS2307 en 4 services, ya documentado por 028A-8) → `npm run codegen:local` los regeneró para desbloquear el type-check.
- **Sentinel/GLORY:** sin reglas nuevas; `commit` se exporta por segundo consumidor real; `isAllowedHeight`/`TERRAIN_HEIGHT_VALUES` viven en el core junto a los valores de superficie (decisión del bloque).
- **Límites:** el pincel pinta solo vértices de chunks existentes (no crea terreno ni redimensiona bounds); la altura aún no se representa en 3D (Assets 3D); el sombreado del canvas es local del editor; sin caminos ni tipos de superficie adicionales.
