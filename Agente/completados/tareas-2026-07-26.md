# Tareas completadas — 2026-07-26

## 267A-2 — Evitar pérdida del contacto capturado por la IA
- **Que:** se separó la actualización de nombre de la captura atómica de email,
  normalización y consentimiento. Guardar el nombre ya no puede borrar un correo
  existente; los errores dejan de descartarse y los logs no exponen la dirección.
- **Archivos:** `src/repositories/chat.rs`, `src/services/ai_tools.rs` y MDs de
  estado relacionados.
- **Gotchas:** el correo de continuación aún no funciona extremo a extremo; sus
  piezas backend no tienen disparador durable ni integración frontend. Desplegado
  en Studio; health HTTP/app correcto, `restart_count=0` y `OOM=false`.
- **Sentinel:** pendiente evaluar una regla contra writes con sentinelas vacíos en
  campos opcionales persistentes.
- **GLORY:** lógica agnóstica dentro del núcleo Rust compartido; no aplica traslado.

## 267A-4 — Continuación durable de conversación
- **Que:** se conectó la presencia durable por última pestaña, epoch de
  desconexión, outbox diferida y cancelable, token de un uso, correo SMTP y
  restauración exacta de sesión desde un fragmento eliminado inmediatamente.
- **Archivos:** migración `20260726200000_chat_continuation_delivery`, backend
  de chat/outbox y coordinador frontend de continuación.
- **Gotchas:** falta la canary humana en navegador limpio; el frontend no pudo
  repetir `tsc` local porque el binario nativo de `lightningcss` estaba bloqueado,
  pero el build desplegado y el health de producción pasaron.
- **Sentinel:** pendiente una regla que prohíba timers por conexión WebSocket para
  seguimientos diferidos que deben vivir en outbox durable.
- **GLORY:** comportamiento agnóstico implementado en el núcleo Rust compartido.

## 267A-5+267A-7+267A-8+267A-9 — Alertas WhatsApp inmediatas
- **Que:** se creó el gateway HMAC de WordPress, outbox separada, worker `wacli`,
  runner robusto, lock asesor MySQL y timer systemd `oneshot` cada cinco segundos.
- **Archivos:** `InternalAlertApiController.php`, firma, schema/repositorio/worker
  saliente, runner y configuración operativa de Coolify.
- **Gotchas:** WordPress normaliza headers REST a snake_case; buscar claves con
  guiones rechazaba firmas válidas. Un restart Docker tampoco recarga variables
  nuevas: se necesita redeploy/recreación o el flujo de env del gestor.
- **Sentinel:** recomendable detectar acceso directo a claves de
  `WP_REST_Request::get_headers()` sin canonicalización.
- **GLORY:** gateway reutilizable implementado en `glorytemplate`; el cliente
  Nakomi conserva únicamente contrato/outbox propios.
- **Evidencia:** canary en estado `sent`, intento 1 y recibida físicamente por la
  usuaria en el número administrativo; timer activo y health final verde.
