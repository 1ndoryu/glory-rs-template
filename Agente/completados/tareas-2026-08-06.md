# Tareas completadas — 2026-08-06

## SNT-15 — Endurecimiento del slice local de adapter

### Qué
Se cerró el siguiente bloque reversible de la migración sin tocar Sentinel upstream, retirar scripts ni modificar la skill global. El manifest local es ahora una frontera estricta y físicamente contenida: gobierna la selección/paridad de stages, rechaza schema incompleto o claves desconocidas, no permite symlink/junction escape y mantiene una allowlist portable sin credenciales.

### Archivos
- `scripts/quality/adapter-manifest.mjs`
- `scripts/quality/stage-definitions.mjs`
- `scripts/quality/tests/adapter-manifest.test.mjs`
- `scripts/quality/tests/stage-definitions.test.mjs`
- `Agente/planes/plan-migracion-scripts-adapters-sentinel-2026-08-06.md`
- `Agente/documentacion/herramientas/inventario-scripts-adapters-sentinel-2026-08-06.md`
- `Agente/completados/tareas-2026-08-06.md`

### Gotchas
- `manifestStageNames`, `adapterStageNames` y `assertImplementedStages` validan el manifest completo antes de usar stages/perfiles.
- Cada definición de stage solo acepta `timeoutMs`; los typos fallan cerrado.
- `quality-adapter.json` y su entrypoint deben ser archivos regulares contenidos en el workspace; los symlink/junction y realpaths fuera se rechazan, incluso en la ruta síncrona usada por `task-check`.
- La allowlist efectiva deduplica case-insensitive para Windows (`PATH`/`Path`) y rechaza nombres sensibles tanto en la declaración como en la allowlist base heredada.
- El fallback legacy de `stageDefinitions` solo se conserva cuando no se proporciona `projectRoot`, como compatibilidad explícita de tests/transición. El camino real carga el manifest.
- No se retiró ningún script ni se publicó ningún cambio upstream: faltan schema upstream, fixtures independientes, paridad multi-proyecto y rollback.

### Sentinel / GLORY
- `node --check scripts/quality/adapter-manifest.mjs`: PASS.
- `node --check scripts/quality/stage-definitions.mjs`: PASS.
- Suite focalizada final: **19/19 PASS, 0 FAIL**.
- `git diff --check`: PASS.
- `tools/sentinel` y `tools/varsense` quedaron en los gitlinks fijados, sin cambios de contenido.
- `glory-rs` conserva el gitlink histórico `ec33d520`; el objeto no está disponible en el remoto actual, por lo que no se sustituye ni se fuerza su checkout.
- No se declara gate completo ni observe end-to-end: el worktree no contiene `.env`/`DATABASE_URL`, `frontend/node_modules` ni CLI compilado de VarSense.
- Skill global: no modificada; el bloque no aporta todavía evidencia para una regla global o publicación upstream.

### Pendiente
Schema upstream del manifest, fixtures Node/Vite y Rust/PostgreSQL independientes, paridad CLI/LSP/editor, cinco gates reales, rollback/GC y dos releases consecutivos antes de retirar duplicaciones.
